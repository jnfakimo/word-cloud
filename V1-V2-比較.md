# 北農巡檢系統 V1 / V2 差異整理

整理日期：2026-08-18　依據 repo 內 `V2-MIGRATION-MATRIX.md`、`ARCHITECTURE_V2.md` 與實際檔案結構彙整。

## 一、架構層級差異

| 項目 | V1（`system/*.html`） | V2（`web/app`） |
|---|---|---|
| 前端 | 多頁靜態 HTML，各頁獨立 | Next.js App Router + React + TypeScript，靜態匯出 |
| 路由 | `/Inspection/system/*.html` | `/Inspection/v2/systems/{system}/{module}`（`basePath` 設於 `web/next.config.ts`） |
| 寫入路徑 | 多數頁面直連資料表 | 新版功能改走 `app-api`（JWT/RBAC）或 SECURITY DEFINER 函式，RLS 為最後防線 |
| 資料庫 | 共用同一個 Supabase 專案 | 與 V1 共用同一份資料與權限，未建第二套 |
| 部署 | GitHub Pages | GitHub Pages（同站台，不同路徑） |

兩版並存，V1 不會被刪除；等各模組在 V2 驗收完成，才會把入口切換到 V2。

## 二、各系統模組移植狀態

| 系統 | V2 狀態 | V1 對應檔案 | V2 對應元件 |
|---|---|---|---|
| 維修／派工 | ✅ 五個模組完整搬移（報修、派工、工單、附件、分析） | `workorder.html`、`dispatch.html`、`repair.html`、`analytics.html` | `workspace.tsx`、`workorder-extras.tsx` |
| 設備建置 | ✅ 八個模組完整搬移 | `equipment.html`、`materials.html`、`materials-archived.html` | `equipment-workspace.tsx` |
| 專案關係／圖臺 | ✅ 六個模組完整搬移 | `structure_map.html`、`b1plan.html`、`floor3d.html`、`b1_integrated_marker_system.html`、`modeler.html`、`locations.html`、`arealist.html` | `structuremap-workspace.tsx`、`structuremap-viewers.tsx`、`floor-stack-3d.tsx` |
| 公務車派車 | ✅ 五個模組完整搬移 | `vehicle-dispatch.html` | `vehicle-workspace.tsx` |
| 會議室預約 | ✅ 四個模組完整搬移（會議預約、會議室主檔、變更申請、預約提醒） | `meetingroom.html` | `meeting-workspace.tsx` |
| 駐衛警巡檢 | ✅ 六個模組完整搬移（含 QR 標籤列印） | `guardpatrol.html`、`guardpatrol3d.html`、`patrolcheckin.html`、`patrolshifts.html`、`patrollist.html`、`patrol-notifications.html` | `patrol-workspace.tsx`、`operations-workspace.tsx` |
| 電子交接簿 | ✅ 三個模組完整搬移（含案件建立與附件） | `handover.html`、`handover-login.html` | `handover-workspace.tsx` |
| 後台管理 | ✅ 十一個模組完整搬移 | `admin.html`、`rbac.html`、`notices.html` | `AdminWorkspace.tsx` 與 `components/admin/*` |

✅ = 完整搬移（含列表、建立/編輯、狀態流程、附件、權限、手機版）。

> 八個系統的模組現在都有專屬元件與寫入路徑，不再有只靠 `MODULE_SOURCES` 唯讀查詢撐場的模組。

## 三、已知落差與風險

**尚未走 `app-api` 的直接寫表**（僅靠 RLS 把關，未達「API + RLS 雙層防線」）：
`workspace.tsx`（報修建立與附件）、`operations-workspace.tsx`（巡檢打卡）、`handover-workspace.tsx`（交接單、案件與附件）、`handover-pilot/page.tsx`、`NoticesAdmin.tsx`、`CostsAdmin.tsx`（費用記錄）、`CyclesAdmin.tsx`（巡檢週期）。
這幾處的資料表 RLS 都要求對應的 `has_system_access` 與角色條件，伺服器端把關存在；依 `ARCHITECTURE_V2.md`「第 3 條的實際落差」的判斷準則，再包一層 Edge Function 只會多一次轉發。

**前端直呼 SECURITY DEFINER 函式**（風險較低、屬刻意取捨）：
`apply_repair_workflow`、`save_dashboard_layout_version`、`publish_dashboard_layout_version`、`vehicle_request_action`、`complete_vehicle_trip`。

**已清理**：4 支正式環境已部署但原始碼不在 repo 的舊函式（`hyper-worker`、`smart-function`、`bright-function`、`dynamic-processor`）已移除；其中 `smart-function`／`bright-function` 曾可讓任一登入使用者繞過 RLS 推播訊息到官方 LINE 群組，該路徑已隨移除關閉。原始碼留底於 `docs/removed-edge-functions/`。

**權限落差**：`sys_vehicle` 目前只開放 `mgmt_supervisor` 與 `sysadmin`，一般司機在 V2 進不了公務車派車系統（既有設定問題，非本次搬移造成）。

**圖臺資產路徑**：V1 用相對路徑 `plans/tex/...`（掛在 `/Inspection/system` 下）；V1／V2
的自訂樓層圖改用私有 Storage bucket `floorplans` 的 5 分鐘 signed URL，避免永久網址
與匿名讀取暴露場域配置。

## 四、交付順序（既定計畫）

1. 維修／派工：報修、派工、工單、附件、分析。
2. 駐衛警巡檢與電子交接簿：現場操作優先，支援手機與離線提示。
3. 後台與設備主檔：帳號、權限、場域、設備履歷。
4. 圖臺、派車、會議室與通知／稽核。

（2026-08-18：四項全部完成主要模組搬移。八個系統共 51 個模組皆有專屬元件與寫入路徑。）

**刻意不搬的兩頁**：`patrolcheckin.html`（掃 QR 進入的單頁簽到流程，含 MFA，改寫成 V2 路由會讓現場已張貼的標籤失效）與 `admin.html` 的 3D 建模導覽頁（五個目的地在 V2 都已是獨立模組，再做一頁只是多一層轉跳）。

## 五、系統模組以外的頁面（2026-08-18 全站比對）

八個系統的模組之外，V1 還有幾頁不屬於任何系統。逐頁比對後的處置：

| V1 頁面 | 處置 |
|---|---|
| `dashboard.html` 戰情儀表板 | **已搬移**。V2 首頁改為讀 `dashboard_layouts` 已發布版面依 `widget_key` 算繪，補上先前「版面設定得了卻沒有地方生效」的斷點 |
| `login.html` 忘記密碼 | **已搬移**。補上「Email 寄重設連結 → 設定新密碼」兩段流程 |
| `index.html`、`guardpatrol-index.html` | 入口卡片牆，對應 V2 的 `/systems` 與各系統 hub |
| `app.html`、`inspection-archived.html`、`materials.html` | 頁面本身即為「已關閉／已封存」公告，不需搬移 |
| `structure_map.html` 專案關係地圖 | **已搬移**。V2 `relations` 模組上方補上系統關係圖（內嵌 SVG，不引入 vis-network），節點換成現行八個系統與後端相依，可點擊進入；下方保留原有的樓層／區域清單 |
| `login.html` 的 PWA 安裝引導 | **部分**。V2 已補上自己的 `manifest.webmanifest`（V1 那份 scope 是 `/Inspection/system/`，掛在 V2 頁面上不符合 scope 而失效），iOS 可手動「加入主畫面」；Android 的一鍵安裝需要 Service Worker，刻意不做——本系統對 SW 有踩雷紀錄（`patrolcheckin.html` 還特地移除舊 SW 與快取），要加得先定好快取策略，屬另一個決定 |
| `api.html` | **不搬**。給外部系統看的整合文件，不是系統功能 |
| `setup.html` | **不搬**。頁面本身寫明「正式環境已停用瀏覽器初始化」，只剩一頁說明 |
| `handover-login.html` | **不搬**。V2 已是統一登入＋AuthGate，未登入進交接簿會自動導向 `/login`，不需要專用登入頁 |
| `dashboard.html` 的臺灣即時氣象 | **已搬移**。資料仍走既有的 `cwa-weather` 函式（警特報、縣市觀測、鄉鎮預報）；V1 用來選縣市的臺灣 SVG 地圖改為下拉選單，那張圖只是選取介面、不影響數據 |

**注意事項**：V2 忘記密碼的 `redirectTo` 是 `/Inspection/v2/login/`，與 V1 的
`/Inspection/system/login.html` 不同，必須在 Supabase Auth 的 Redirect URLs 白名單
另外加上，否則信中的連結會被拒絕。

同時修掉兩個 V2 既有缺陷：`/mobile` 的「掃描設備二維碼」原本指向 `app.html`（已關閉公告頁），
五個動作中有三個仍走 V1，一律改指 V2 模組；`/equipment-map` 原本用
`x = 10 + (index*23) % 78` 排出與實際位置無關的「平面圖」，已移除該畫布，
只保留確實來自設備主檔的樓層分布，需要看位置就導向 SYS-06 的圖臺。

## 六、V2 新增系統（V1 沒有對應頁面，2026-08-31 補記）

`web/lib/modules.ts` 目前是 11 大系統、55 個子系統，比第二節整理的 8 個系統多出 SYS-09～11，
這三個是 V2 才有、V1 沒有對應頁面的全新系統，不算「搬移」，補記於此避免比較文件漏項：

| 系統 | V2 路由 | 說明 | V1 對應 |
|---|---|---|---|
| SYS-09 公文傳送系統 | `/Inspection/v2/systems/officialdocs/` | 公文送出、依序會辦、陳核、核決與原申請人收訖紀錄，`official-docs-workspace.tsx` | 無 |
| SYS-10 市場營運分析系統 | `/Inspection/v2/systems/marketanalytics/` | 交易行情彙整、同期比較、可配置指標與戰情摘要 | 無 |
| SYS-11 戰情儀表板 | V2 首頁 | 設備、巡檢、異常與報修的即時彙總，版面由後台戰情版面設定驅動（見第五節「已搬移」的 `dashboard.html`） | `dashboard.html`（已列入第五節） |

### SYS-10 市場營運分析系統 5 個模組現況

| 模組 | V2 元件 | 現況 |
|---|---|---|
| 市場戰情儀表板（command-center） | `market-command-center.tsx` | 期間切換（日/週/月/季/年）、市場→大類→品項下鑽、7 種圖表切換（趨勢/長條/K線/圓餅/甜甜圈/柏拉圖/明細） |
| 行情分析工作台（overview） | `market-analytics-workspace.tsx` 的 `AnalysisWorkspace` | 自訂維度／指標／圖表類型（bar/pie/doughnut/line/area/table/cards）、可存成模板 |
| 資料介接中心（sources） | 同檔 `SourcesWorkspace` | 資料來源管理、CSV／JSON／XLSX 匯入、欄位定義 |
| 分析模板（templates） | 同檔 `TemplatesWorkspace` | 保存常用維度、指標、比較期間與圖表色卡設定 |
| **同期比較（comparison，2026-08-31 新增）** | 同檔 `ComparisonWorkspace` | 近 12 個月交易量年度同期長條圖（每月標年增率）、各蔬果大類價格分布箱型圖、近 12 週「週×星期幾」交易熱力圖；三者皆重用既有 `market_analysis` API 多次呼叫組成，未新增後端 action |

`同期比較` 是依使用者提供的 claude.ai Design 稿新增，補上系統描述裡本來就有、但先前 4 個模組
沒有對應畫面的「同期比較」功能。實作方式與待驗證事項：

- **不動既有 4 個模組**，只在 `modules.ts` 的 `marketanalytics.modules` 陣列尾端新增一筆
  `comparison`；`workspace-router.tsx` 不需改動（`marketanalytics` 系統本來就把非
  `command-center` 的模組全部導去 `market-analytics-workspace.tsx`），只在該檔內新增一個
  `module.key === 'comparison'` 分支。
- 後端沒有「依維度拆分的每日序列」action，三個圖表都是**用既有 `market_analysis` 多次呼叫**組出來：
  年度同期比較＝12 次月度呼叫（每次的當期／比較期天數天然相等）；箱型圖＝每個蔬果大類各呼叫一次
  取 `series` 算四分位；熱力圖＝一次長區間呼叫的 `series` 依「週×星期幾」重排。沒有新增或修改
  `supabase/functions/app-api/index.ts`，因此**沒有邊緣函式部署風險**。
- 熱力圖／箱型圖刻意不加新的 chart 套件（如 chartjs-chart-boxplot／chartjs-chart-matrix），
  改用純 CSS Grid／定位 div 手刻，比照 `market-command-center.tsx` 的 K 線圖手法，維持專案不外掛
  額外圖表庫的既有風格。
- 顏色全部走 `color-mix(in srgb, var(--cyan) N%, var(--panel))` 或既有 CSS 變數，沒有寫死深色背景，
  符合 `AGENTS.md`「V2 預設淺色主題、沒有白名單救援」的規範。
- 同步更新了 `tools/system-page-heading-check.mjs`、`tools/v2-button-standard-check.mjs`、
  `tools/security-design-audit.mjs` 三處寫死的「54 個子系統」斷言為 55。
- **尚未驗證**：本次改動未經 `npm run build:v2` 與 `npm run test:page-headings` 實際執行
  （這次工作環境的 shell 無法掛載到本機 repo），已用人工逐段複讀程式碼、比對既有型別與既有
  helper 簽章（`periodText`、`changePercent`、`chartRowLabel`、`finiteNumber`、`rangeLength`、
  `addYears`、`addDays` 均沿用既有定義）確認結構正確，但仍需在本機實際跑一次建置與頁首檢查、
  並登入畫面確認三個圖表的實際算繪與淺色主題對比。**改動目前留在工作目錄，未 commit／push。**
