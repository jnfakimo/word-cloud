# 開發與部署

返回：[[00-專案首頁]]

## 本機預覽

V1 頁面沒有編譯步驟，可從儲存庫根目錄啟動靜態檔案伺服器，再開啟：

`/system/index.html`

V2 為 Next.js 靜態匯出，開發預覽使用 `npm run dev:v2`；正式產物使用
`npm run build:pages` 建立於 `_site/`。

## 修改原則

- 維持既有繁體中文介面與視覺風格。
- 日期使用 `YYYY-MM-DD`，時間使用 `YYYY-MM-DD HH:mm`。
- 樓層名稱可能同時存在 `B1` 與 `B1F`，需先正規化。
- 新頁面需補齊導覽列、登入檢查、Supabase 初始化與跨頁連結。
- 不可刪除 `system/plans/` 圖資。
- `theme.js` 共用導覽的舊連結清理邏輯若只比對 href 檔名，遇到「不同用途但剛好連到同一個檔案」的頁面專屬連結會被誤刪（2026-08-04 踩過，`guardpatrol3d.html` 的「巡檢稽核總覽」曾被誤判成舊版「駐衛警巡檢」重複連結而消失）；新增清理規則時務必連結文字也一併檢查。
- 固定定位（`position:fixed`）搭配寫死像素偏移量（如 `top:62px`）的版面，一旦上方元素高度會變動（例如導覽列改成自動換行），要改用 CSS 變數＋ JS 量測實際高度同步，不能繼續假設固定高度。

## 驗證

- 檢查修改頁面的 inline JavaScript 語法。
- 測試登入保護、資料讀寫與錯誤訊息。
- 涉及 SQL 時確認舊環境也能套用。
- 部署後以版本查詢參數避開 CDN 快取確認結果。

## 部署

推送到 `main` 後，由 GitHub Actions 的 `Hardened Pages deployment` 進行正式建置、
來源簽章、全站掃描與 GitHub Pages 發布；Edge Function 只在相關檔案變更時由獨立流程部署。

## 部署紀錄

### 2026-09-05｜自架站台登入除錯：Google Drive 掛載與純 docker 重建（未完成）

自架站台 `1.34.250.22:5057`（內網主機 `192.168.50.192`，跑 `AI\Antigravity\0705` 那份的
Supabase 本機 Docker stack）登入「驗證碼服務暫時無法連線」。逐層診斷定位主因並留下工具，
最終未修通，卡在伺服器找不到 supabase CLI。交接摘要見 `handoff.md`。

**三層根因**

1. anon key：前端用雲端 anon key 打本機 GoTrue（不同 JWT secret）→ 401。`web/lib/config.ts`
   已有 `useBrowserOrigin` 分流（本機用 issuer=`supabase`、iat 2026-08-31 那把實際 anon key）。
2. IIS 反代：`5057` 只把 `/functions` 反代到本機 54321，`/auth`、`/rest` 沒轉，回 IIS 自己的
   純文字 401。範本：`tools/selfhosted-iis-supabase-proxy.web.config`。
3. edge_runtime boot（主卡點）：functions 從 Google Drive bind mount，容器讀不到。

**踩坑（詳細）**

- **Google Drive 上的專案不能給 Docker bind mount**。`docker inspect` 顯示 functions 掛
  `/run/desktop/mnt/host/g/我的雲端硬碟/AI/Antigravity/0705/supabase/functions`（唯讀）。
  Windows 端檔案 Attributes=Normal、`index.ts` 20789 bytes 都正常，但 Docker Desktop 經 WSL2
  掛 Google Drive 的虛擬串流磁碟時，容器內讀到的是空的 → edge-runtime 報
  `failed to determine entrypoint`。與早先「Drive 弄壞 .git」同一類（Drive 虛擬檔案系統對
  底層存取不可靠）。解法：`robocopy` 到真實磁碟 `C:\supabase-0705`，讓 edge_runtime 掛 C:。
- **純 docker `docker run` 重建 edge_runtime 補不出 main service**。CMD 是
  `edge-runtime start --main-service=/root`，`/root` 的 main router 是 `supabase start`
  啟動時「即時生成注入」的（不在 functions 目錄、也不在 image），純 docker 重建後掛 C: functions
  仍報 `main worker boot error`（找不到 `/root` entrypoint）。→ 必須用 `supabase start` 從
  C: 專案重啟讓 CLI 生成 main service，不能手動 `docker run` 繞過。
- **PowerShell 5.1 讀含中文的 `.ps1` 用 Big5 解碼 → 全亂碼 → parser error**。修復腳本一度
  含中文註解/訊息，在伺服器 `-File` 執行時整批亂碼、引號配對錯而爆掉。解法：腳本純 ASCII
  （訊息英文、來源路徑執行時從 `docker inspect` 動態取得，不寫死中文）。
- **PowerShell 貼多行腳本會在 `>> }` 斷掉**。改單行、base64 一行，或存檔 `-File` 執行。

**環境備忘**

- Claude Code 跑在開發機 `DESKTOP-0CFB6UK`，透過同一個 Google Drive 與伺服器
  `192.168.50.192` 共享檔案；docker/supabase 在伺服器，開發機這端碰不到（找不到 docker.exe）。
- 路由器轉埠：外部 5057→內部 192.168.50.192:443(IIS)、54321/54322/54323 直接對外轉發。
- `supabase_vector_0705` 一直 Restarting（log sink，與登入無關）；`db` 資料在
  `supabase_db_0705` volume（168MB），全程未動。

**未解／下一步**：伺服器上 `supabase` 不在 admin PowerShell 的 PATH（裝在 user PATH：scoop
shims / winget / npx）。找到後 `cd C:\supabase-0705; <supabase 完整路徑> start`（**從 C: 跑，
不可回 Google Drive 目錄**）。一勞永逸：整個 `0705` 專案移出 Google Drive 到本機碟。

### 2026-08-30｜市場行情臺股式紅漲綠跌與趨勢判讀

- 市場分析工作台與中央戰情儀表板統一採臺灣股市閱讀慣例：紅色 `▲ 上漲`、綠色 `▼ 下跌`、灰色 `— 持平`，同時保留方向文字與百分比，避免只靠顏色判讀；無比較基準維持中性，不偽裝成零或上漲。
- KPI、市場比較、變動觀察、快速判讀、彩色圖卡、明細表及情境模擬同步顯示比較期與絕對差異。畫面明確註記此為本期相較比較期的視覺慣例，並非農產品具有漲停／跌停制度。
- 每日趨勢以本期實線、比較期虛線區分期間，線色與外部圖例會同步套用使用者選擇的色卡；紅／綠三角資料點專門表示相對方向。X 軸標示本期日期，Y 軸依指標與單位動態命名，並提供比較日期、兩期數值、絕對差異與漲跌幅的可展開明細表；長期間依實際有值日期抽樣保留最多約 45 組方向標記，不再整批隱藏。
- 上漲／下跌符號使用主題感知的可讀紅、綠，文字維持主文字色。實際建置 CSS 於一般版／科技版及 320px、375px、1280px 驗證：頁面無水平溢出，手機圖卡單欄、圖例無裁切，明細表只在自身容器捲動；紅綠符號對比最低 5.4:1。
- 新增 `marketMovementPresentation` 共用方向模型、方向徽章、單元測試及 `test:market-stock-signal` 靜態契約並納入完整測試。驗證：乾淨依賴環境 `npm run typecheck:v2`、`npm run build:v2`（79 頁）、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）、`npm run security:secrets`、`git diff --check` 均通過。
- 正式提交 `35a0a2fd0`；GitHub Actions `Hardened Pages deployment`（run `33313825783`）、`CI`（run `33313825814`）、`Commercial readiness checks`（run `33313825823`）與 `CodeQL Analysis`（run `33313825867`）全數成功。正式站回應 200，部署後 JS／CSS 資產已確認包含臺股式圖例與方向樣式。

### 2026-08-30｜市場比較長條圖初學者判讀修整

- 「第一、第二市場實際交易行情」比較長條圖新增可見圖例，明確標示上方長條為本期、下方長條為比較期及各自日期；畫面同步說明兩種顏色代表比較期間，不代表第一、第二市場，市場範圍仍以上方篩選條件為準。
- X 軸與 Y 軸名稱依目前指標、單位及分析維度動態產生，補上共同刻度、長條長度意義、右側本期／比較期精確數值，以及「顯示前 20／全部組數、依兩期較大值排序」提示。
- 缺值保留為「—／無資料」，不再被誤讀為零；新增可展開的完整數值表、列標題語意及鍵盤下鑽入口，圖表使用唯一 `useId()` 避免同頁多張圖產生重複識別碼。
- 手機版將圖例、軸說明與數值重新堆疊，品項可顯示兩行、下鑽列至少 44px；完整資料表只在自身容器橫向捲動，不會撐寬頁面。320px、375px、1280px 實測主圖與展開表格均無頁面水平溢出，圖例色與長條一致。
- 新增 `test:market-bar-guide` 契約檢查並納入完整測試；驗證：乾淨依賴環境 `npm run typecheck:v2`、`npm run build:v2`（79 頁）、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）、`npm run security:secrets`、`git diff --check` 均通過。

### 2026-08-30｜共用「上頁」固定父層路由修正

- 原共用頁首以 `window.history.back()` 返回，會受瀏覽器歷史影響；若前一筆是已刪除的本機 QA 暫存檔、外部網站或其他系統頁，就可能出現 `ERR_FILE_NOT_FOUND` 或離開巡檢系統。正式程式並未寫死該本機暫存路徑。
- 「上頁」改依應用程式層級導向固定父頁：子系統回所屬系統入口、系統入口回整合系統入口、整合系統入口回戰情首頁。市場行情 `/systems/marketanalytics/overview/` 因此固定回 `/systems/marketanalytics/`，不再讀取瀏覽器歷史。
- 一般 AppShell 與 3D／平面全螢幕圖資共用頂列同步修正，涵蓋市場分析、後台、設備圖臺、巡檢雲臺與報修 3D 圖等共用頁面。
- 新增純函式路由與元件接線契約測試，防止日後重新加入 `history.back()`；驗證：`npm run test:app-navigation` 4/4、完整 `npm test`、乾淨依賴環境 `npm run typecheck:v2`、`npm run build:v2`（79 頁）、`npm run security:audit`（錯誤 0／既有警告 4）、`npm run security:secrets`、`git diff --check` 均通過。

### 2026-08-30｜市場行情蔬果階層與連動篩選修正

- 正式行情並未缺少水果：49,916 筆戰情資料中含水果 15,383 筆／69 個品項、蔬菜 34,533 筆／127 個品項；2026-08-30 第一、第二市場也各自同時有水果與蔬菜。先前可能載入只有高麗菜、菠菜、山蘇的非正式示範來源，且技術用 `item_key` 被誤顯示成第四個篩選欄位。
- 正式分析工作台不再提供示範來源與示範模板，資料內容篩選改為「市場 → 蔬果大類 → 品項分類」三個連動下拉選單；選擇上層會重載下層合法值並清除舊的下層條件，`item_key` 保留作自然鍵但不顯示。
- 預設分析根層改為「蔬菜／水果」兩大類，再下鑽各自品項；第一、第二市場保留為獨立切片器與市場比較，不再形成「兩市場 × 兩大類」四組而被誤認為只有四項。原始檔沒有可信的葉菜／根莖／柑橘等次分類欄位，本次不以品名猜測，避免決策數字失真。
- 新增安全的 `market_dimension_values_filtered` RPC，父層條件只能使用來源已定義且可篩選的字串維度；前端、Edge Function 與來源 metadata 同步支援設定驅動階層，舊環境尚未套用 RPC 時會安全回退舊查詢。
- 變更檔案：`web/app/systems/[system]/[module]/market-analytics-workspace.tsx`、`web/app/systems/[system]/[module]/market-analytics.css`、`supabase/functions/app-api/index.ts`、`supabase/migrations/20260830192000_market_hierarchical_filters.sql`、`system/sql/market_analytics.sql`。
- 驗證：乾淨依賴環境 `npm run typecheck:v2`、`npm run build:v2`（79 頁）、`deno check --no-lock supabase/functions/app-api/index.ts`、完整 `npm test`（327 組 schema contract）、PostgreSQL migration／RPC 語法解析、`npm run security:audit`（錯誤 0／既有警告 4）、`npm run security:secrets`、`git diff --check`；實際建置 CSS 於 light／tech 主題及 320px／375px／1280px 均為 3 個原生下拉選單且無水平溢出。

### 2026-08-30｜市場行情 Power BI 式逐層下鑽

- 交易行情儀表板新增明確的下鑽模式，依資料來源欄位動態辨識「市場 → 品類 → 品項」層級；點選市場卡、占比圖、變動觀察、快速判讀、詳細圖表或明細表即可展開下一層，沒有寫死特定蔬菜名稱。
- 新增目前路徑麵包屑、向上鑽取與清除下鑽；關閉模式後恢復原分析維度與內容篩選。工具列獨立於分析結果，載入中或請求失敗仍可復原操作，不會卡在失敗層級。
- 圓餅、甜甜圈與折線／面積圖保留滑鼠點擊，並提供可聚焦的資料表按鈕作為鍵盤等價操作；下鑽條件與既有篩選合計超過 API 上限時會先阻止送出並提示調整。
- 變更檔案：`web/app/systems/[system]/[module]/market-analytics-workspace.tsx`、`web/app/systems/[system]/[module]/market-analytics.css`。
- 驗證：乾淨依賴環境 `npm run typecheck:v2`、`npm run build:v2`（79 頁）、完整 `npm test`、`npm run security:secrets`、`git diff --check`；實際建置 CSS 於 320px、375px、1280px 量測無水平溢出。

### 2026-08-30｜第一、第二市場實際行情決策版

- 整併使用者提供的 115 年 1～6 月全場交易行情工作簿（94,703 筆）與 7～8 月 233 份舊式 Excel 每日匯出；每日檔依檔內市場欄位辨識第一／第二市場，排除 49 份空匯出與 8 組內容重複，合計整理出 120,570 筆全場交易紀錄及 49,916 筆「日期 × 市場 × 品類 × 品項」戰情資料。拍賣價檔因沒有成交量，不混入成交量、估算交易額與加權均價。
- 正式來源 `tapmc_market_actual` 涵蓋 2026-01-01～2026-08-30、兩市場各 188 個有交易日；總成交量 370,543,325 公斤，估算交易額 14,495,290,645.09 元。估算交易額明確定義為平均價乘以成交量，非實際結算金額；空白檔與蔬菜 07-24／07-25 缺檔保留為品質缺口，不當成零交易。1～6 月另標記 6 筆價格區間待查及 5 組代號跨品名，長期趨勢以代號加品名辨識。
- 行情分析新增動態市場／品類／品項篩選、完整市場摘要、交易量加權平均、三份正式主管模板與安全分批匯入；分類明細超過 500 組時，市場占比仍由完整資料重新彙總，不從截斷明細回推。文字篩選改為按「套用篩選」後才查詢，避免每輸入一字就重跑兩期資料。
- 資料介接欄位新增必填、加權欄位與代碼唯一驗證；重匯採自然鍵冪等更新並合併既有數值／中繼資料，同批重複粒度會指出列號後整批停止。來源日期由資料庫實際最早、最新與前一交易日動態回傳，後續匯入新日期不會停在固定示範日。
- 中央戰情儀表板改用唯一正式預設來源，顯示成交量、估算交易額、加權均價、比較期變動與資料截止；實際資料分成 28 支小於 512 KiB 的冪等資料 migration，另有基礎 migration 建立正式來源、來源清冊、模板與 RPC。
- 產出 `市場交易行情決策分析.xlsx`，含決策總覽、每日趨勢、兩市場比較、品項異常、資料品質與 49,916 筆清理後資料；使用市場藍綠、綠、琥珀與紅色卡，包含折線、長條及圓環圖，公式錯誤掃描為 0。
- 驗證：乾淨 NTFS 暫存環境 `npm run typecheck:v2`、`npm run build:v2`、`npm run test:page-headings`、`npm run security:audit`，以及 `deno check supabase/functions/app-api/index.ts`、PostgreSQL 語法解析、migration guard、資料列與 CSV 逐欄核對均通過。

### 2026-08-30｜市場交易行情儀表板第二版：戰情總覽與每日行情

- 參考外部戰情儀表板的「首屏先看結論」模式，保留既有 AppShell 與雙主題，將大量分析設定改為可收合區；分析完成後首屏直接顯示資料狀態、比較期間、動態 KPI、每日行情、波動觀察、分類占比與快速判讀。
- 新增真正依 `observed_on` 每日彙整的本期／比較期行情序列，以區間第 N 天對齊；沒有資料的日期保留缺口，不再把分類折線誤當成時間趨勢。
- 波動觀察採透明的 ±10% 規則，使用箭頭、數字與文字共同表達；青色／琥珀色只表示方向，畫面明確說明上升、下降不等同營運好壞。
- 修正缺值被 `Number(null)` 誤當成 0、前一期／後一期／去年同期在本期日期變更後未同步重算，以及編輯既有分析模板未帶識別碼而被當成新增等問題。
- `market_analysis` 改為逐頁完整讀取，每期最多 50,000 筆；超限時明確停止並提示縮短期間，不再以固定 5,000 筆靜默少算。API 同時回傳資料截止日、載入筆數及分類群組完整性；分類超過 500 組時保留完整 KPI／每日序列，但停止用部分分類做占比、排行與漲跌結論。資料來源更新時，前端未傳設定物件不再清空既有設定。
- 本期與比較期強制使用相同天數且最多 366 天；切換資料來源或模板時會先校驗欄位結構，並以請求版本阻止舊來源的慢回應覆蓋新結果。
- 驗證：乾淨依賴環境 `npm run typecheck:v2`、`npm run build:v2`、`deno check --no-lock supabase/functions/app-api/index.ts`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）、`git diff --check`；瀏覽器以一般版／科技版及 320px／375px／桌機檢查，頁面無水平溢出。

### 2026-08-30｜市場交易行情儀表板第一版：多圖表、色卡與情境模擬

- 行情分析新增占比圓餅、甜甜圈、分類比較折線、分類比較面積、比較長條、明細表格與彩色數值卡；可另選圖表指標，圓餅／甜甜圈並列本期與比較期，超過 10 類時彙整為「其他」。
- 新增市場藍綠、蔬果鮮彩、冷色專業、暖色行情、高對比友善及自訂色卡；圖表類型、圖表指標與色卡會保存在分析模板，舊模板自動沿用預設色卡。
- 新增「行情情境模擬」：以實際合計值調整各指標百分比，顯示推估值，並保存情境名稱、期間、基準、假設、結果、建立者與時間；模擬紀錄採不可覆寫的追加式追蹤，畫面明確標示不等同統計預測。
- 新增獨立「示範交易行情（非正式資料）」來源與 2026-08-23～2026-08-30 高麗菜、菠菜、山蘇示範資料，僅供體驗圖表與模擬，不混入正式行情來源。
- 已透過 GitHub Actions 成功套用 `20260830120000_market_analytics_foundation.sql`，排除正式頁面「市場行情資料尚未完成設定」錯誤；後續圖表約束、模擬紀錄與示範資料 migration 隨本次版本依序套用。
- 變更檔案：`web/app/systems/[system]/[module]/market-analytics-workspace.tsx`、`web/app/systems/[system]/[module]/market-analytics.css`、`supabase/functions/app-api/index.ts`、`system/sql/market_analytics.sql`、`system/sql/permanent_data_protection.sql`、`supabase/migrations/20260830143000_market_analysis_chart_types.sql`、`20260830150000_market_simulation_runs.sql`、`20260830151000_market_demo_dataset.sql`。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`（乾淨依賴環境）、`deno check --no-lock supabase/functions/app-api/index.ts`、`npm test`、`npm run security:audit`、`git diff --check`，並於 320px／375px／1280px 檢查圖表與色卡無水平溢出。

### 2026-08-30｜市場營運分析子系統基礎建置

- 新增 SYS-10「市場營運分析系統」，包含行情分析工作台、資料介接中心與分析模板；資料來源欄位、維度與指標採設定驅動，可匯入不同品項與市場資料，並支援前一天、後一天、去年同期及自訂期間比較。
- 新增 `market_data_sources`、`market_data_points`、`market_analysis_templates` 冪等資料庫腳本與 Edge Function API；中央戰情儀表板新增市場交易行情摘要元件。
- 變更檔案：`web/app/systems/[system]/[module]/market-analytics-workspace.tsx`、`web/app/systems/[system]/[module]/market-analytics.css`、`web/lib/modules.ts`、`supabase/functions/app-api/index.ts`、`supabase/migrations/20260830120000_market_analytics_foundation.sql`、`system/sql/market_analytics.sql`、`web/app/dashboard-client.tsx` 等。
- 驗證：`deno check --no-lock supabase/functions/app-api/index.ts`、`npm run typecheck:v2`（乾淨依賴環境）、`npm run test:page-headings`、`npm test`、`git diff --check`。基礎 migration 已於 2026-08-30 透過 GitHub Actions 手動流程成功套用正式資料庫。

### 2026-08-30｜V2 報修單建立來源欄位修正

- 修正 V2 報修建立／上傳流程，將 `repair_requests.source` 改回既有合法值「直接通報」（`direct`），避免正式資料庫的來源檢核約束拒絕新增報修單。
- 變更檔案：`supabase/functions/app-api/index.ts`。
- 驗證：`deno check supabase/functions/app-api/index.ts`、`node --check tools/e2e-repair-flow.mjs`、`npm test`、`git diff --check`。

### 2026-08-30｜公文時間軸每頁 10 筆

- 「不可刪除的時間軸」改為每頁顯示 10 筆，保留上一頁／下一頁分頁與完整筆數資訊；最新事件摘要仍取整份時間軸的最後一筆。
- 變更檔案：`web/app/systems/[system]/[module]/official-docs-workspace.tsx`。
- 驗證：官方公文頁面 TSX 以 esbuild 語法解析、`git diff --check`。

### 2026-08-29｜公文副總經理陳核支援秘書室代收

- 秘書室與副總經理室同為第一階單位；秘書室一般人員在直屬主管為副總經理室主管時，可代收副總經理陳核公文並完成會辦。
- 代收人員只能執行陳核簽收，核決／退回仍限副總經理室實際單位人員；公文清單、通知與逾時提醒同步納入代收人員。
- 變更檔案：`web/app/systems/[system]/[module]/official-docs-workspace.tsx`、`supabase/functions/app-api/index.ts`、`supabase/functions/official-document-timeout-check/index.ts`、`web/types/app.ts`、`web/components/admin/UsersAdmin.tsx`、`supabase/functions/admin-api/index.ts`、`supabase/migrations/20260829120000_secretary_deputy_official_doc_delegation.sql`。
- 驗證：公文頁面與後台人員管理 TSX 以 esbuild 語法解析、三支相關 Edge Function `deno check`、`npm test`、`git diff --check`；資料庫 migration 由 GitHub Actions 的手動流程套用。

### 2026-08-29｜帳號主管階層支援部／室與課／組／隊

- 「直屬課室主管」可選同一課／組／隊主管，也可選所屬上層部／室主管；主管角色本身可掛在部／室或課／組／隊。
- 新增／編輯人員帳號與帳號申請核准共用階層判斷，前端選單與 Edge Function、資料庫觸發器同步限制在同一單位樹，不允許跨部／室指定主管。
- 變更檔案：`web/components/admin/UsersAdmin.tsx`、`supabase/functions/admin-api/index.ts`、`supabase/migrations/20260829100000_hierarchical_supervisor_scope.sql`。
- 驗證：前端 TSX esbuild 語法解析、`deno check supabase/functions/admin-api/index.ts`、`npm test`、`git diff --check`。

### 2026-08-29｜新增人員帳號的部／室與課／組階層選擇

- 新增／編輯人員帳號的「所屬單位」拆為第一階「部／室」與第二階「課／組／隊」兩個欄位；第二欄依所選部／室即時篩選，避免選到其他部門的子單位。
- 編輯既有人員時會依原有單位自動回填對應的部／室與課／組；仍保留「整個部／室（未指定課／組）」的相容選項。
- 變更檔案：`web/components/admin/UsersAdmin.tsx`。
- 驗證：前端 TSX esbuild 語法解析、`npm test`、`git diff --check`。

### 2026-08-29｜公文會辦自動完成、部／室路由與創文單位簽收

- 會辦單位按一次「收文」即同時完成會辦，時間軸仍保留「收文」與「會辦完成」兩筆不可刪除事件；前端不再要求第二個「會辦完成」按鈕。
- 會辦完成後直接顯示下一站下拉選單，選項只列第一階「部／室」，可送出會辦或陳核；已完成節點不再從公文清單隱藏。
- 具 SYS-09 公文傳送權限的人員可建立公文／自動取號，並在自己所屬或已參與的流程範圍內接續送出下一站；跨部／室仍由後端範圍檢查拒絕。後台的 SYS-09 權限勾選也已恢復可寫入。
- 陳核核決後維持「待創文單位簽收」狀態，創文部／室及其下屬單位的啟用人員都會收到通知並可簽收，簽收後才結案；不再限定原申請人本人。
- 變更檔案：`web/app/systems/[system]/[module]/official-docs-workspace.tsx`、`supabase/functions/app-api/index.ts`、`supabase/functions/admin-api/index.ts`、`tools/e2e-official-document-flow.mjs`。
- 驗證：`deno check supabase/functions/app-api/index.ts`、`deno check supabase/functions/admin-api/index.ts`、`node --check tools/e2e-official-document-flow.mjs`、前端 TSX esbuild 語法解析、`npm run test:page-headings`、`git diff --check`。

### 2026-08-29｜公文傳送簽收角色範圍修正

- 修正公文流程節點以第一階「部／室」保存、但使用者帳號掛在下屬「課／組／隊」時，待簽公文不會出現在清單、前端不顯示「簽收」、後端也會拒絕收文／核決的問題。
- 前後端統一沿部門樹判斷收文範圍；會辦／陳核通知改寄送給目標部／室及其下所有啟用人員，逾時通知也涵蓋下屬單位主管。未放寬跨部／室存取，其他單位仍會被拒絕。
- 變更檔案：`web/app/systems/[system]/[module]/official-docs-workspace.tsx`、`supabase/functions/app-api/index.ts`、`supabase/functions/official-document-timeout-check/index.ts`、`tools/e2e-official-document-flow.mjs`。
- 驗證：`deno check` 兩支公文 Edge Function、官方頁面 TSX 以 esbuild 語法解析、`node --check tools/e2e-official-document-flow.mjs`、`npm run test:page-headings`、`git diff --check`；推送後 GitHub Actions 的 CI、Commercial readiness、Edge functions deployment、Hardened Pages deployment、CodeQL 全部成功（`33255231449`、`33255231440`、`33255231436`、`33255231438`、`33255231473`），其中 CI 已在乾淨依賴環境完成 V2 typecheck/build。
- Commit：程式 `3d66eb701`；本次部署紀錄 `9d713bf86`。

### 2026-08-29｜巡檢點 QR／NFC 感應簽到

- 巡邏點清單的 QR 視窗新增 NFC 感應網址、直接寫入 NDEF 標籤與複製網址備援；QR 與 NFC 都導向同一個巡檢點簽到頁。
- NFC 標籤使用 HTTPS URL，不讀取或儲存卡片 UID；不支援 Web NFC 的手機可用 NFC Tools 寫入 URL。
- 簽到頁辨識 qr／nfc 來源並顯示於成功結果；patrol-checkin Edge Function 以伺服器時間寫入來源，驗證登入工作階段、巡檢權限、兩小時期限與防重複簽到。
- 驗證：deno check supabase/functions/patrol-checkin/index.ts、node --check（巡檢簽到頁與 Service Worker）、npm run test:page-headings、npm run test:profile-modal、git diff --check。

### 2026-08-29｜巡檢 NFC 簽到無法建立驗證器修正

- 移除巡檢簽到頁首次碰卡時自動建立 TOTP 驗證器的阻擋流程；該流程在目前 Supabase 專案未啟用 MFA 時會回傳空錯誤，造成「無法建立驗證器」而無法簽到。
- Edge Function 不再強制 AAL2，仍保留登入、駐衛警巡檢權限、兩小時工作階段期限、伺服器時間、限流、停用點檢查與五分鐘防重複等防線。NFC／QR 來源紀錄維持不變。

### 2026-08-29｜49 個子系統手機頁首操作列

- 共用手機版頁首將說明文字與「重新載入／其他操作」按鈕優先排在同列並靠上對齊，並把操作列下方間距收緊。
- 操作按鈕群組在可用空間內靠右排列；可用寬度不足時維持自動換行，避免按鈕與說明文字重疊。
- 以 `flex-wrap` 保護窄螢幕：按鈕與說明若放不下會自動換行，不會互相重疊或產生水平溢出；桌面版維持原配置。

### 2026-08-29｜個人資料設定手機版欄位並排

- 基本資料彈窗在手機版將登入帳號／電子郵件合併為同一列，姓名／聯絡電話／所屬單位／帳號角色也合併為同一列；採用獨立欄位群組與 `minmax(0, …)` 欄寬，避免小螢幕水平溢出。
- 桌面版維持原本兩欄配置，登入安全等其他區段不受影響。

### 2026-08-25｜P0 資安基線：RLS、RBAC、Storage、密碼與 Edge Function 密鑰

#### 已完成的程式與資料庫防線

- 新增 `supabase/migrations/20260825190000_p0_security_baseline.sql`：對使用者歷程、資安告警、預約變更、戰情版面與交接試用表啟用並強制 RLS，撤銷匿名及未授權角色的直接權限。
- RBAC 保留 `sysadmin` 完整權限、禁止保留角色代碼刪除或改名；啟用中的一般人員必須指定啟用中的課室主管，並在可判定單位時限制同單位階層。
- 三個業務附件 bucket 改為私有、上限 10 MB、限制 MIME；Storage 讀取需有對應系統權限且物件已存在業務附件索引，避免任意路徑讀取孤兒檔案。`floorplans` 另經 P0 重新評估後改為私有 bucket，由已啟用帳號取得 300 秒 signed URL，避免完整場域配置暴露給匿名流量。
- V1／V2 的新增帳號、管理員重設、本人修改及忘記密碼流程統一由 `admin-api`／`app-api` 後端驗證；密碼至少 12 個字元、至少符合大寫／小寫／數字／特殊字元 3 類且不得含空白。
- 新增 `npm run security:secrets`，CI 掃描 `web/` 與 `system/` 不得出現 service-role、access token、Edge Function 密鑰或私鑰；`SUPABASE_ACCESS_TOKEN` 僅供 GitHub Actions Secret 使用，不寫入 repository。

#### 驗證

- `deno check`：`app-api`、`admin-api` 與密碼政策共用模組通過。
- V1 `login.html`／`admin.html` inline JavaScript、`theme.js`、`supabase-config.js` 語法檢查通過。
- `npm run security:secrets` 通過；`npm run security:audit` 錯誤 0，保留既有警告 4。
- Migration 已透過受保護的 `Apply database migration` workflow 套用正式 Supabase；不得把 Access Token 寫入檔案或聊天紀錄。

#### 部署證據

- 功能 Commit：`7a5e599c8f4a4b8f4b5dee10245d6d9ca721e92f`
- [CI #32844518621](https://github.com/jnfakimo/Inspection/actions/runs/32844518621)
- [Edge functions deployment #32844518705](https://github.com/jnfakimo/Inspection/actions/runs/32844518705)
- [Hardened Pages deployment #32844518722](https://github.com/jnfakimo/Inspection/actions/runs/32844518722)
- [Commercial readiness checks #32844518715](https://github.com/jnfakimo/Inspection/actions/runs/32844518715)
- [CodeQL Analysis #32844518725](https://github.com/jnfakimo/Inspection/actions/runs/32844518725)
- [Apply database migration #32844524580](https://github.com/jnfakimo/Inspection/actions/runs/32844524580)

### 2026-08-25｜P0 floorplans 私有化與 2D／3D 圖臺授權網址

#### 判定與變更

- 實際檢查 `floorplans` 內的 B1、1F、2F、3F、RF 與手機縮圖；圖檔呈現完整市場樓層配置，屬實體場域敏感資訊，不採匿名讀取。
- 新增 `supabase/migrations/20260825230000_floorplans_private_signed_urls.sql`：`floorplans` 設為 private，移除匿名讀取政策，僅允許已啟用登入帳號讀取；上傳／更新仍沿用 `sys_equipment_manage` 權限。
- V1 `b1plan.html`、`floor3d.html`、`b1_integrated_marker_system.html`、`guardpatrol3d.html` 與 V2 平面／3D／標記／巡檢圖臺，統一呼叫 `createSignedUrl(s)`，網址有效 300 秒，不再拼接 `/storage/v1/object/public/floorplans`。
- `system/sql/floor_models.sql` 的新安裝基線同步改為 private bucket；`web/lib/floorplan-storage.ts` 集中處理路徑清理與簽名網址。

#### 驗證與部署證據

- Migration workflow：[Apply database migration #32862044385](https://github.com/jnfakimo/Inspection/actions/runs/32862044385)（成功）。
- 功能 Commit：`a39b5f7`；型別漏引入修正：`6bc4039`。
- Pages：[Hardened Pages deployment #32862166907](https://github.com/jnfakimo/Inspection/actions/runs/32862166907)（build／deploy／verify 全部成功）。
- CI：[CI #32862167438](https://github.com/jnfakimo/Inspection/actions/runs/32862167438)（成功）；Commercial readiness checks 成功。
- 匿名驗證：對六個原圖／縮圖公開 URL 全部回應 HTTP 400，確認無法匿名取得檔案。
- V2 建置、全站掃描、資安設計稽核與欄位契約檢查均通過；已確認路由可部署。登入後於 2026-08-25 23:03 實測 V2 2D、V2 3D，圖面、標記、樓層切換與 7 層模型均正常，無阻斷性錯誤。V1 僅作歷史相容，不列入目前驗收範圍。

### 2026-08-25｜V2 模組完成度與優先順序盤點

- 依 `web/lib/modules.ts` 與 V2 動態路由盤點：9 大系統、49 個子系統皆有靜態路由參數與對應工作區，程式覆蓋率為 49／49。
- 將「程式已有入口」與「正式業務已驗收」分開標示；目前正式驗收證據最完整的是後台資安、關係地圖與駐衛警 3D 導覽，維修／派車仍需跨角色端到端驗收。
- 處理順序定為：P0 安全與權限 → P0 派車階層與核可 → P0 維修全流程 → P1 資料／樓層一致性與離線衝突 → P1 測試、備份、發布防線 → P2 次要模組與操作手冊。
- 完整矩陣與缺口說明見 [[02-功能模組]]；待辦已同步標註 P0／P1／P2。
- 本次為文件盤點，未修改資料庫與正式業務資料。

### 2026-08-25｜guardpatrol3d 導覽列縮放與 3D 畫布偏移修正

- 修正 `theme.js` 在頁首換行後重新量測時未清除舊 `--system-header-offset`，連續縮放會讓 3D 畫布與控制元件逐次下移的問題。
- `guardpatrol3d.html` 更新 `theme.js` 版本查詢參數，確保正式站不會沿用舊版共用腳本快取。
- 正式站以 390／720／1024px 寬度驗證；3D 畫布均位於頁首下方，樓層選單與立體控制面板均未被頁首遮擋，且無水平溢出。
- Commit：`5a92c122e0ee487314177fec13a57b539c70da3d`、`9a32d7beabe9d55dc948061490d39587590464ca`
- [Hardened Pages deployment #32841355479](https://github.com/jnfakimo/Inspection/actions/runs/32841355479)
- 正式網址：https://jnfakimo.github.io/Inspection/system/guardpatrol3d.html?v=9a32d7b

### 2026-08-25｜資安告警篩選列單列化

- 資安告警頁的搜尋、來源 IP、告警類型、告警等級、處理狀態、起日、迄日與清除按鈕改為桌面版單列排列。
- 控件縮小為 36px 高度、日期欄位固定為緊湊寬度；800px 以下自動改為雙欄，維持行動裝置可用性。
- 正式站驗證：篩選列 `display:grid`、高度 42px，所有 8 個控制項同列；未處理告警 0 筆，頁面載入正常。
- Commit：`f411acaa39e1017d303a4d76e612fa7fdf900ab8`
- [Hardened Pages deployment #32838735510](https://github.com/jnfakimo/Inspection/actions/runs/32838735510)
- 正式網址：https://jnfakimo.github.io/Inspection/v2/systems/admin/alerts/?v=f411aca

### 2026-08-25｜異常流量告警、V2 全站稽核與 LINE 通知補強

#### 變更內容

- 後端對 API 流量限制、疑似暴力登入與前端錯誤爆量建立永久資安告警；相同來源採首次拒絕與指數檢查點記錄，降低大量重複紀錄與 LINE 通知風暴。
- 新增告警 LINE 投遞歷程及流量限制檢查點歷程，限制用戶端直接新增／刪除，保留可追溯證據。
- V2 全站新增頁面進入、功能使用、資料讀取、檔案讀取與拒絕存取稽核；操作者、來源 IP 與時間均由伺服器重新判定，敏感欄位、權杖與查詢字串會先遮蔽。
- 錯誤爆量門檻正式啟用為「15 分鐘內 20 筆、通知冷卻 60 分鐘」；測試通知不會建立正式告警或占用冷卻時間。
- 資安告警頁補齊流量限制、暴力登入、錯誤爆量類型、LINE 送達狀態與繁體中文資源名稱；操作稽核頁可查 V2 系統、功能與資料讀取歷程。
- 修正 Pages、Render Node API、Supabase Edge Function 獨立滾動部署時的相容性：新設定欄位尚未抵達 Node API 時仍維持安全啟用；Node API 回覆「不支援的後台管理動作」時，只對該未執行動作安全改走 Edge Function，避免重複寫入。
- 新增受保護的資料庫 Migration 工作流程，使用既有 `SUPABASE_ACCESS_TOKEN` 呼叫 Management API，包含檔名、交易、大小、破壞性 SQL 與版本防重放檢查。
- 前端錯誤改由受信任的稽核 Edge Function 寫入；伺服器綁定人員與時間、限制速率，且移除密碼、Cookie、JWT、驗證碼與查詢字串。

#### 驗證結果

- 單元測試：安全稽核遮蔽測試 4／4 通過。
- TypeScript：V2 與 Node API 型別檢查通過；Node API 正式建置通過。
- Edge Function：`admin-api`、`app-api`、`audit-event`、`error-threshold-check`、`username-login` 均通過 Deno 檢查、正式部署及端點煙霧測試。
- SQL：Migration 共 82 個敘述可解析，正式資料庫以交易方式成功套用；沒有刪除、清空或重建業務資料。
- 資安稽核：9 大系統、49 個子系統，錯誤 0；既有警告 4。
- 靜態建置：72／72 個 V2 路由成功，安全產物共 1,968 個檔案、112 個頁面；正式發布後全站掃描成功。
- 正式排程：錯誤門檻檢查回傳 `healthy`，最近 15 分鐘最高 0 筆、門檻 20 筆。
- 正式設定頁：資安告警及錯誤爆量告警皆為啟用；門檻顯示 15／20／60；Token 只顯示「已設定」而不回傳內容。
- LINE：於 `2026-08-25 17:44` 由正式設定頁成功傳送一筆錯誤爆量測試訊息，畫面確認未影響正式告警冷卻。
- 資安告警頁：可正常載入及篩選，驗收時未處理告警為 0 筆。
- 操作稽核頁：已看到 `2026-08-25 17:44` 的進入系統、測試錯誤爆量告警、讀取帳號、系統角色、組織部門與帳號申請清單等紀錄，包含操作人員、來源 IP、裝置與唯一紀錄編號；資源名稱已完整繁體中文化。

#### 部署證據

- 部署分支：`main`
- 資料庫工作流 Commit：`a586b8160842aa116a1c60ad07188358f10c0b72`
- 主要功能 Commit：`ba8c99f52b986bb3d61ebdb35e42c7e94064be5d`
- 部署窗口修正：`5824a85dce9f24e7ab5989cfb5779248a8bde679`、`e5375825814e36712c25585973d5b0916109e76f`
- 稽核中文化 Commit：`32d5b674cc7ab35547592349a80f2405371173eb`
- 資料庫 Migration：[Apply database migration #32832416310](https://github.com/jnfakimo/Inspection/actions/runs/32832416310)
- Edge Functions：[Edge functions deployment #32832496347](https://github.com/jnfakimo/Inspection/actions/runs/32832496347)
- 錯誤門檻正式排程：[Error threshold notification #32832627733](https://github.com/jnfakimo/Inspection/actions/runs/32832627733)
- 最終 Pages：[Hardened Pages deployment #32833729892](https://github.com/jnfakimo/Inspection/actions/runs/32833729892)
- 最終 CI：[CI #32833729829](https://github.com/jnfakimo/Inspection/actions/runs/32833729829)
- 最終商用檢查：[Commercial readiness checks #32833729851](https://github.com/jnfakimo/Inspection/actions/runs/32833729851)
- 最終 CodeQL：[CodeQL Analysis #32833729555](https://github.com/jnfakimo/Inspection/actions/runs/32833729555)
- 正式設定網址：https://jnfakimo.github.io/Inspection/v2/systems/admin/settings/?v=32d5b67
- 正式稽核網址：https://jnfakimo.github.io/Inspection/v2/systems/admin/audit/?v=32d5b67
- 正式驗證時間：`2026-08-25 17:47`（Asia/Taipei）
- 回滾基準：`8e2e0090784b6297d8f0ea24387d13ab648b48a3`

#### 監測邊界

- 本次已涵蓋使用者瀏覽器、V2 應用程式 API、登入服務與 Edge Function 的安全事件；完全不載入 JavaScript、直接攻擊 Supabase REST／Storage 或大規模 DDoS 的來源，仍應在 CDN／WAF／Supabase 平台層補充監測與封鎖。

### 2026-08-25｜專案關係地圖後台切換、Node API CSP 與存取權限修正

#### 變更內容

- 「網頁後台」與「行動端」由 SVG 靜態文字改為可用滑鼠及鍵盤操作的連結。
- 關係地圖保留後台側邊選單，可在後台管理與專案關係地圖之間直接雙向切換。
- `relations` 直達頁補上 `structuremap` 系統權限檢查；八個系統節點依帳號權限啟用，未開放者顯示為灰色且不可點。
- V2 CSP 的 `connect-src` 會從 `NEXT_PUBLIC_APP_API_URL` 安全解析 HTTPS origin，允許正式 Node API，並於產物驗證逐頁確認。
- 本次未修改資料庫、Migration、Supabase Edge Function 或正式資料。

#### 驗證結果

- TypeScript：`node node_modules/typescript/lib/tsc.js -p web/tsconfig.json --noEmit` 通過。
- 安全稽核：9 大系統、49 個子系統，錯誤 0；既有警告 4。
- 乾淨暫存工作樹：`npm ci` 無弱點；`npm run build:pages` 成功，產出 1,968 個檔案、112 個頁面。
- 本機全站掃描：38 個頁面、73 段內嵌程式、18 個自有 JavaScript，全數通過。
- 正式站：網頁後台、行動端、後台側邊欄與八個已授權系統入口均可切換。
- 場域資料：共 71 筆；搜尋「冷凍機房」得到 2 筆；B1 篩選共 21 筆且第 2／3 頁正常；重新載入成功。
- Node API：正式 CSP 已包含 `https://beinong-app-api.onrender.com`；健康檢查回傳 HTTP 200，重新載入後未再產生新的 Node API 備援警告。
- 權限：本次正式站使用系統管理員帳號驗收；無 `structuremap` 權限時的拒絕分支已由程式檢查與 TypeScript 建置驗證。
- 本機未載入正式來源簽章私鑰；GitHub Actions 的正式簽章驗證已通過。

#### 部署證據

- 部署分支：`main`
- 功能 Commit：`d1fbef19c09de88655c53623399b9e064929ab5c`
- GitHub Actions：[Hardened Pages deployment #32826070097](https://github.com/jnfakimo/Inspection/actions/runs/32826070097)
- 工作結果：`build`、`deploy`、`verify-deployment` 全部成功；CI、Commercial readiness checks、CodeQL 亦成功。
- 正式網址：https://jnfakimo.github.io/Inspection/v2/systems/structuremap/relations/?v=d1fbef1
- 正式驗證時間：`2026-08-25 16:25:54`（Asia/Taipei）
- 回滾基準：`79e930d09fa04f16b51bc58ce7278369100ff79d`

相關：[[03-資料庫與建置]]、[[05-待辦清單]]

### 2026-08-25｜P0 報修至結案端到端測試

- 新增可重複執行的 `tools/e2e-repair-flow.mjs`（npm 指令：`npm run test:e2e:repair`）。
- 測試透過正式 `app-api` Edge Function 的 `workorder_workflow` 動作執行，不直接改寫流程狀態；以一般報修人員、單位主管、維修技師三個分離登入帳號驗證角色邊界。
- 完整驗證：報修 → 主管派工 → 工程師接單 → 開始維修 → 完工回報 → 報修人驗收 → 主管驗收結案。
- 每一步均確認 `repair_requests`、`maintenance_orders` 與 `case_status_log` 同步；歷程共 6 筆狀態轉移：`assigned`、`accepted`、`in_progress`、`pending_review`、`completed`、`closed`。
- 額外驗證越級防護：待報修人驗收時直接主管結案被拒絕，確認六階段順序由後端執行。
- 本次測試建立的 P0-E2E 資料標記為隱藏；測試帳號已依階層順序停用，報修／工單／歷程不刪除以保留稽核證據。
- 測試結果：`ok: true`、`roleSeparated: true`；代表性報修單 `78b39f04-bb29-47b6-a7f5-083af0b8bc8b`、工單 `ccab8940-54d5-46dd-82c8-99c5a0ceadd6`。
- 角色權限修正：一般報修人員、維修技師、值班／派工角色恢復維修系統入口及必要讀寫／派工權限，未放大主管驗收權限。
- 權限 Migration：[Apply database migration #32846158816](https://github.com/jnfakimo/Inspection/actions/runs/32846158816)
- 功能 Commit：`cd7c4d117fbf1b17167c43735dc44cfbe765e5ea`

### 2026-08-25｜P0 帳號申請與派車核可階層端到端驗收

#### 補強內容

- 新增 `supabase/migrations/20260825210000_vehicle_dispatch_role_access.sql`，恢復一般使用者（`reporter`）、單位主管（`unit_supervisor`）、派車管理員（`dispatcher`）、駕駛（`duty`）及可被指派為駕駛的技術角色（`technician`）進入 `sys_vehicle` 的最小系統權限；核可、派車與接單仍由 RPC、名單表及資料庫 trigger 分別限制。
- 修正 `supabase/functions/admin-api/index.ts` 帳號申請核准的臨時密碼長度；原本兩組完整 UUID 串接為 73 字元，會觸發 Auth／bcrypt 72 字元上限而無法建立帳號，現在改為含複雜度且低於上限的隨機密碼。
- 新增可重複執行的 `tools/e2e-vehicle-dispatch-flow.mjs`（npm 指令：`npm run test:e2e:vehicle`），實際走公開帳號申請、`admin-api` 核准、V2 `app-api` 建立派車申請及 `vehicle_request_action` 四種分離登入角色。

#### 驗收結果

- 帳號申請已建立並由系統管理員核准為一般使用者，核准時明確綁定直屬單位主管。
- 派車狀態完整通過：`pending_approval`（一般使用者送出）→ `approved`（單位主管核可）→ `assigned`（派車管理員指派車輛與駕駛）→ 駕駛接單（狀態維持 `assigned`、寫入接單人與時間）。
- 越權防護通過：派車管理員嘗試代替單位主管核可被拒絕；流程歷程包含「單位主管核可」「派車管理員完成派車」「司機接單」三筆。
- 測試結果：`ok: true`、`roleSeparated: true`、`negativeGuard: true`；代表性帳號申請 `1d4d1151-af15-45de-a7d5-bae28e5dbd94`、派車申請 `96c1f8ff-0d7d-4dfc-8858-446a860977d9`。
- 測試結束後測試派車單改為「已取消／測試資料」，測試帳號、派車管理員／駕駛名單及測試車均停用；申請與三階段歷程保留供稽核，未實體刪除資料。

#### 部署證據

- 功能 Commit：`db2f34214492c964fb8e0b507a96365080ebc147`、`a4f5528dff63b8601bfab1b187f94018508fe381`、`e3e21422f597f2dfa9c3ae852af30df4e7d8c7d1`
- [Apply database migration #32847047510](https://github.com/jnfakimo/Inspection/actions/runs/32847047510)
- [Edge functions deployment #32847241050](https://github.com/jnfakimo/Inspection/actions/runs/32847241050)
- [CI #32847375092](https://github.com/jnfakimo/Inspection/actions/runs/32847375092)
- [Hardened Pages deployment #32847375039](https://github.com/jnfakimo/Inspection/actions/runs/32847375039)
- [Commercial readiness checks #32847375086](https://github.com/jnfakimo/Inspection/actions/runs/32847375086)
- [CodeQL Analysis #32847375043](https://github.com/jnfakimo/Inspection/actions/runs/32847375043)

### 2026-08-25｜P1 統一共用登入與使用者資料處理

#### 補強內容

- V1 `system/supabase-config.js` 新增 `SystemAuth`：登入憑證只由 `username-login` 核發，登入後與既有 session 均透過受保護的 `app-api` `profile` 動作取得正式人員資料、角色、權限與可用系統；不新增瀏覽器 token 儲存。
- V2 登入頁、`AuthGate`、`AppShell` 登出流程改用同一份 `inspectionSystemUserProfile` sessionStorage 契約；新增 `web/lib/profile-cache.ts`，與 V1 `SystemUserProfile` 欄位及更新事件對齊。
- 後台、報修／派工、儀表板、通知、電子交接簿、會議室、派車、報修、巡檢 3D、工作單與巡檢簽到等主要 V1 模組，移除目前登入者的頁面專屬 `auth_id` 查詢；仍需顯示其他人員的清單查詢則保留。
- 電子交接簿登入改走相同的 CAPTCHA、限流、登入稽核與 `username-login`，不再直接呼叫 `signInWithPassword` 或 `login_lookup_email`。
- `app-api/profile` 回傳既有 `permissions` 欄位，維持 V1 舊角色頁面行為並以 `allowed_systems` 作為 V2 可用系統來源；所有共用腳本版本參數同步更新以避開 CDN 舊快取。

#### 驗證結果

- `npm test`：安全稽核遮蔽測試 4／4、登入一致性檢查通過。
- V1 主要修改頁面的 inline JavaScript、`theme.js`、`supabase-config.js` 語法檢查通過；`npm run security:audit` 錯誤 0（既有警告 4），`npm run security:secrets` 通過。
- GitHub Actions 乾淨環境：CI、Hardened Pages、Commercial readiness、CodeQL 均成功；Edge Function 變更部署成功。

#### 部署證據

- 功能 Commit：`5afdbb015c7b105cf288313ed0e72d190b642e42`；驗證修正 Commit：`ee07459f138f81a07d983069fbb745c1a910fe5f`
- [Edge functions deployment #32849840784](https://github.com/jnfakimo/Inspection/actions/runs/32849840784)
- [CI #32850009787](https://github.com/jnfakimo/Inspection/actions/runs/32850009787)
- [Hardened Pages deployment #32850009760](https://github.com/jnfakimo/Inspection/actions/runs/32850009760)
- [Commercial readiness checks #32850009763](https://github.com/jnfakimo/Inspection/actions/runs/32850009763)
- [CodeQL Analysis #32850009634](https://github.com/jnfakimo/Inspection/actions/runs/32850009634)
- 正式驗證入口：[V1 登入](https://jnfakimo.github.io/Inspection/system/login.html?v=20260825-auth1)、[電子交接簿登入](https://jnfakimo.github.io/Inspection/system/handover-login.html?v=20260825-auth1)、[V2 登入](https://jnfakimo.github.io/Inspection/v2/login/?v=20260825-auth1)

### 2026-08-25｜P1 統一樓層代碼轉換規則

#### 補強內容

- 新增 `supabase/functions/_shared/floor.ts`，定義全站唯一的 `canonicalFloor()` 與 `floorOrder()`；內部比對鍵值固定為 `B1`、`1F`、`RF`，兼容 `B1F`、`地下1樓`、`1`、`頂樓`、`RF頂` 等歷史／輸入寫法。
- V2 `web/lib/floor.ts`、V1 `system/theme.js`／`floor-utils.js` 與 Edge／Admin／巡檢 Function 對齊同一規則；既有資料不刪除，讀取與新寫入於邊界正規化。
- V2 圖臺、2D／3D 樓層模型、設備、巡邏點、巡檢打卡、交接設備、會議室、位置分析與模型建置頁改用正規化鍵值比較，避免 `B1` 與 `B1F` 分裂成兩層。
- 共用腳本與巡檢 Service Worker 更新至 `20260825-floor1`，避免瀏覽器／CDN 使用舊樓層規則。

#### 驗證結果

- `npm test`：安全稽核測試 4／4、登入一致性、樓層代碼一致性全部通過。
- `npm run security:audit`：錯誤 0，既有警告 4；`npm run security:secrets` 通過。
- `deno check`：共用樓層規則、`app-api`、`admin-api`、QR 巡檢與逾時通知 Function 通過。
- 樓層一致性測試涵蓋：`B1／B1F／地下1樓`→`B1`、`1／1F／1樓`→`1F`、`RF／頂樓`→`RF`，並確認排序為 B2 → B1 → 1F → RF。

#### 部署狀態

- 已部署至 `main`（commit `dd71c27`）；Edge Function、Pages、CI、Commercial readiness 與 CodeQL 均成功。
- [Edge functions deployment #32853332588](https://github.com/jnfakimo/Inspection/actions/runs/32853332588)
- [CI #32853332646](https://github.com/jnfakimo/Inspection/actions/runs/32853332646)
- [CodeQL Analysis #32853332728](https://github.com/jnfakimo/Inspection/actions/runs/32853332728)
- [Commercial readiness checks #32853333014](https://github.com/jnfakimo/Inspection/actions/runs/32853333014)
- [Hardened Pages deployment #32853333290](https://github.com/jnfakimo/Inspection/actions/runs/32853333290)

### 2026-08-25｜P1 盤點資料表與前端欄位一致性

#### 盤點與修正

- 新增 `tools/schema-contract-check.mjs`，掃描 `system/sql`、Supabase migrations、V1／V2 前端、Edge Function 直接與巢狀 Supabase `select` 欄位，以及 `MODULE_SOURCES`／設備欄位設定，確認欄位均存在於 SQL schema。
- 發現並修正 `app-api` 設備圖臺查詢：`plan_markers` 實際欄位為 `floor_id`，原查詢誤用不存在的 `floor`；`locations` 實際以 `area`／`detail` 組成位置名稱，原查詢誤用不存在的 `name`。
- API 回傳同時保留正規化後的 `floor_id`／相容 `floor`，位置資料回傳 `area`、`detail` 與顯示用 `name`；V2 `EquipmentMapData`、`Profile.dept_id` 型別同步補齊，避免資料表與前端契約漂移。
- 新增 `npm run test:schema-contract` 並納入 `npm test`，後續欄位變更會在 CI 先被攔截；本次未刪除、清空或重建任何正式資料。

#### 驗證結果

- `npm test` 通過：安全稽核遮蔽 4／4、登入一致性、樓層一致性，以及 291 組資料表／前端／Edge Function 欄位契約，涵蓋 69 張資料表。
- `deno check supabase/functions/app-api/index.ts` 通過；`npm run security:audit` 錯誤 0（既有警告 4），`npm run security:secrets` 通過。
- 本機 `typecheck:v2` 因工作區未安裝 `tsc` 未執行；GitHub Actions 乾淨環境 CI、Pages、Edge Functions、Commercial readiness、CodeQL 全部成功。

#### 部署證據

- 功能 Commit：`39f2c69b0c912c2c4359f96ad613954444d8856`
- [CI #32854411899](https://github.com/jnfakimo/Inspection/actions/runs/32854411899)
- [Edge functions deployment #32854411895](https://github.com/jnfakimo/Inspection/actions/runs/32854411895)
- [Hardened Pages deployment #32854411894](https://github.com/jnfakimo/Inspection/actions/runs/32854411894)
- [Commercial readiness checks #32854411893](https://github.com/jnfakimo/Inspection/actions/runs/32854411893)
- [CodeQL Analysis #32854411905](https://github.com/jnfakimo/Inspection/actions/runs/32854411905)
- 正式設備圖臺入口：[V2 設備樓層分布](https://jnfakimo.github.io/Inspection/v2/equipment-map/?v=39f2c69)

### 2026-08-25｜P1 行動巡檢離線同步衝突處理

#### 盤點結論與補強

- 現行 QR 巡檢採「線上＋登入工作階段＋MFA＋`patrol-checkin` Edge Function」；離線時直接提示需要網路，不在瀏覽器建立可稍後重放的簽到紀錄，避免過期工作階段、MFA 重放與巡檢點狀態過期造成錯誤資料。
- `patrol-checkin` Edge Function 以 `checkin_id` 做冪等去重，再以同人／同目標五分鐘規則防止重複簽到；資料庫 `uq_checkin_dedup` 提供併發重送的最後防線。
- 舊版 `patrol-offline.js` 不再直接寫入 `checkin_logs`；遺留佇列改以 `patrolCheckinQuarantineV1` 隔離保存，保留原事件與隔離原因，不再靜默清除。相同 `checkin_id` 內容相同時去重，內容不同時雙方隔離為衝突事件。
- 巡檢簽到頁開啟時會執行遺留佇列隔離，並解除舊版 Service Worker／清理舊離線快取；Service Worker 不代送資料。
- 新增 `tools/patrol-offline-consistency-check.mjs` 與 `npm run test:patrol-offline`，將離線拒寫、舊佇列隔離、重複去重、Edge Function 與資料庫防重複列為自動檢查。

#### 驗證結果

- `npm test` 通過：安全稽核 4／4、登入一致性、樓層一致性、291 組資料表欄位契約，以及行動巡檢離線／重送衝突檢查。
- `node --check system/patrol-offline.js`、`deno check supabase/functions/patrol-checkin/index.ts` 通過。
- `npm run security:audit` 錯誤 0（既有警告 4），`npm run security:secrets` 通過；本次未修改資料庫與正式業務資料。

#### 部署證據

- 功能 Commit：`e6f55b2fbbd61f6533d4efe5a4ff3d02f4978044`
- [CI #32855588354](https://github.com/jnfakimo/Inspection/actions/runs/32855588354)
- [Hardened Pages deployment #32855588323](https://github.com/jnfakimo/Inspection/actions/runs/32855588323)
- [Commercial readiness checks #32855588482](https://github.com/jnfakimo/Inspection/actions/runs/32855588482)
- [CodeQL Analysis #32855588360](https://github.com/jnfakimo/Inspection/actions/runs/32855588360)
- 正式驗證重點：巡檢 QR 頁離線顯示「需要網路連線」，不產生本地待送簽到；遺留佇列開啟頁面後會進入隔離區而非遭清除。

### 2026-08-25｜CDN／WAF／Supabase 平台層攻擊偵測評估

#### 評估結論

- **應用層：部分完成。** V1／V2 頁面、登入與 Edge Function 已有伺服器端限流、資安告警、稽核與 LINE 投遞；RLS 仍是資料存取的最後邊界。
- **直接 REST：尚未納入應用層告警。** `system/api.html` 公開 `/rest/v1` 使用方式，直接呼叫 PostgREST 不會經過 `app-api`／`admin-api` 的應用層限流與事件寫入；必須改由 Supabase API Gateway 日誌與外部 WAF／限流規則觀測。
- **直接 Storage：分為兩種風險。** `repair-files`、`handover-attachments`、`vehicle-dispatch-files` 已是私有 bucket，並有系統權限、附件索引路徑、10 MB／MIME 防線；`floorplans` 已改為私有 bucket，V1／V2 圖臺先取得 300 秒 signed URL，匿名下載會被拒絕。
- **未載入 JavaScript 的機器流量：目前只能在平台側看見。** 前端稽核事件不會產生，應以 API Gateway／Storage／Auth／Edge Function logs 的 IP、User-Agent、路徑、狀態碼、延遲與請求量建立偵測；Supabase Logs Explorer 可查 API 網路請求與 Storage 進出物件紀錄。
- **DDoS：本 repository 無法單獨完成。** 必須由 Supabase／上游 CDN 或自有 API 網域前的 WAF 吸收與封鎖；GitHub Pages 的靜態網站保護不會自動保護 `qztffronusdhgxhjjub.supabase.co` 的 REST／Storage 端點。

#### 建議處理順序（P0 已執行；其餘平台設定待補）

1. **P0｜可見性與基線（已執行）**：已在 Supabase Logs Explorer 保存 `API 流量五分鐘基線`、`API Gateway 4xx/5xx 異常`、`Auth 登入與授權異常`、`Edge Function 執行異常`、`Edge Function 網路與執行異常`、`Storage 4xx/5xx 異常` 六組查詢；另以 GitHub Actions 每 10 分鐘查詢最近 20 分鐘，達門檻即寫入資安告警並依既有設定送 LINE。Supabase 官方說明：[Logging](https://supabase.com/docs/guides/monitoring-and-debugging/logs)、[Storage Logs](https://supabase.com/docs/guides/storage/debugging/logs)、[Logs API](https://supabase.com/docs/reference/api/v1-get-project-logs)。
2. **P0｜公開圖臺資產決策（已執行）**：完整樓層配置判定為敏感圖資，已改私有 bucket＋300 秒 signed URL；匿名 B1／1F／2F／3F／RF 與手機縮圖請求均回應 HTTP 400。Supabase 私有 bucket 的存取模型：[Storage Buckets](https://supabase.com/docs/guides/storage/buckets/fundamentals)。
3. **P1｜WAF／限流**：以自有 API 網域（或受控反向代理）承接 `/rest/v1/*`、`/storage/v1/*`、`/functions/v1/*`、`/auth/v1/*`，再套用 WAF managed rules、按 IP／API key／路徑的 rate limiting，以及對疑似機器流量的 challenge；規則需先以 7 日基線校準，避免誤傷正常巡檢與圖臺。Cloudflare 官方：[Rate limiting rules](https://developers.cloudflare.com/waf/rate-limiting-rules/)、[Security features interoperability](https://developers.cloudflare.com/waf/feature-interoperability/)。
4. **P1｜無 JavaScript 監測**：建立低頻、固定來源的 synthetic probe，只做 HEAD／小型 GET／未授權 REST 檢查，不模擬大量攻擊；把 401／403、429、5xx、P95 延遲及流量突增送到既有告警通道。
5. **P2｜DDoS 與外部留存**：確認 Supabase 方案的日誌保留、Log Drain、費用上限與事件通報窗口；必要時以自有 WAF／CDN 供應商承接流量並做受控封鎖演練。Supabase Log Drains：[Log Drains](https://supabase.com/docs/guides/monitoring-and-debugging/log-drains)。

#### 邊界與驗證狀態

- 文件部署驗證：平台監測功能 commit `beab8f8b39e3aabc90e0200db904ea43f584316b`；[Edge functions deployment #32859199749](https://github.com/jnfakimo/Inspection/actions/runs/32859199749)、[CI #32859199793](https://github.com/jnfakimo/Inspection/actions/runs/32859199793)、[Hardened Pages deployment #32859199802](https://github.com/jnfakimo/Inspection/actions/runs/32859199802)、[Commercial readiness checks #32859199759](https://github.com/jnfakimo/Inspection/actions/runs/32859199759)、[CodeQL Analysis #32859199845](https://github.com/jnfakimo/Inspection/actions/runs/32859199845) 均成功；新增 `function_edge_logs` 後的 Edge Function 部署 [#32860188925](https://github.com/jnfakimo/Inspection/actions/runs/32860188925) 成功。
- 實際監測執行 [#32859310358](https://github.com/jnfakimo/Inspection/actions/runs/32859310358) 先回報 `state: healthy`；納入 Edge Function 網路層後，[#32860295661](https://github.com/jnfakimo/Inspection/actions/runs/32860295661) 偵測到 `function_edge_logs` 20 分鐘內 26 次請求、6 次 HTTP 錯誤、10 次錯誤訊號，超過門檻並回報 `state: alerted`，確認告警寫入／通知流程確實被觸發。
- 本次已變更 Supabase Dashboard 的 Logs Explorer 儲存查詢，並部署監測 Edge Function；未變更 DNS、WAF 或方案。正式平台日誌監測使用 GitHub secret `SUPABASE_ACCESS_TOKEN`，權杖未寫入 repository、日誌或 Obsidian。
- `floorplans` 私有化已於 P0 完成；三個業務附件的私有政策仍維持不變。登入後實測 V2 2D／3D 均能以授權網址載入，圖面與標記顯示正常；V1 不列入本階段驗收。
- 本次未執行高流量壓力測試或 DDoS 模擬，避免對正式服務造成流量風險；P2 另以低流量、唯讀的封鎖邊界演練驗證拒絕行為，結果記錄於下方。

### 2026-08-25｜P1 自有 API 網域／CDN／WAF 前置盤點

- GitHub Pages 設定查核結果：`custom_domain` 為空，現行網站仍使用 `jnfakimo.github.io`；V2 `web/lib/config.ts` 及 V1 共用設定直接指向 `https://qztffronusdhgxhjjubt.supabase.co`。
- GitHub Actions 已有 `NEXT_PUBLIC_APP_API_URL=https://beinong-app-api.onrender.com`，但這是 Render 平台網址，不是可由本專案管理 DNS／WAF 的自有 API 網域；目前沒有 Cloudflare Zone、DNS、WAF 或 Worker 路由設定可安全套用。
- 7 日基線不能以目前 Free 方案的 Supabase Logs 直接回溯（平台日誌僅保留 1 天）；無 JavaScript synthetic probe 已開始每 15 分鐘收集低頻狀態／延遲／流量資料，需累積滿 7 日後才適合校準正式限流門檻。
- **尚未執行的外部變更**：未切換前端 API origin、未變更 DNS、未建立 Cloudflare 代理／WAF 規則，避免把第三方 Render 或 Supabase 端點誤當成自有網域而造成服務中斷。
- **完成本 P1 所需資料**：自有 API 網域（例如 `api.<公司網域>`）、DNS 供應商／Cloudflare Zone 管理授權，以及確認反向代理目標（Render Node API、Supabase REST／Storage／Functions／Auth 是否全部納管）。取得授權後，才可依 7 日基線建立 `/rest/v1/*`、`/storage/v1/*`、`/functions/v1/*`、`/auth/v1/*` 的 rate limit、Bot challenge、允許清單與回源 TLS 規則。

### 2026-08-25｜P0 Supabase 平台日誌門檻與通知

#### 監測範圍與門檻

| 來源 | 20 分鐘視窗門檻 | 告警類型／處理 |
| --- | --- | --- |
| API Gateway（`edge_logs`） | HTTP 4xx／5xx 或錯誤訊號 ≥ 20；請求量 ≥ 1,000 | 系統錯誤爆量，寫入 `security_alerts` 並依設定送 LINE |
| Storage（`storage_logs`） | HTTP 4xx／5xx 或錯誤訊號 ≥ 10；請求量 ≥ 500 | 系統錯誤爆量，寫入 `security_alerts` 並依設定送 LINE |
| Auth（`auth_logs`） | 登入／授權／拒絕／限流等訊號 ≥ 10 | 疑似暴力登入，寫入 `security_alerts` 並依設定送 LINE |
| Edge Function 內部（`function_logs`） | 例外／逾時／失敗等訊號 ≥ 5 | 系統錯誤爆量，寫入 `security_alerts` 並依設定送 LINE |
| Edge Function 網路（`function_edge_logs`） | HTTP 4xx／5xx 或網路錯誤訊號 ≥ 5 | 系統錯誤爆量，寫入 `security_alerts` 並依設定送 LINE |

- 監測程式：`tools/platform-log-monitor.mjs`；通知與永久告警：`supabase/functions/platform-log-alert/index.ts`；排程：`.github/workflows/platform-log-monitor.yml`，每 10 分鐘執行一次。
- 查詢使用目前 Supabase Management API 的統一 Logs endpoint（ClickHouse SQL，查詢區間上限 24 小時）；監測視窗固定 20 分鐘以涵蓋排程延遲。門檻可透過 GitHub Actions env 調整，應依後續基線調校，程式不輸出管理權杖。
- 告警由既有 `recordAndNotifySecurityIncident` 去重並留下站內／LINE 投遞歷程；同一來源通知冷卻 10 分鐘，Management API 查詢失敗則另記錄嚴重平台告警，冷卻 30 分鐘。

#### 日誌保留期限與實際驗證

- Supabase Dashboard 顯示本專案為 **Free** 方案；依官方方案說明，Free 的日誌保留期限為 **1 天**，且 Free 不提供 Log Drains：[Supabase Pricing](https://supabase.com/pricing)。因此本監測不依賴超過 24 小時的回溯資料；若需 7／30／90 日稽核或外部 SIEM，必須升級方案或接入可用的 Log Drain。
- 2026-08-25 基線執行回報：API Gateway 27 次／HTTP 錯誤 4、Storage 12 次／HTTP 錯誤 0、Auth 4 次／訊號 4、Edge Function 內部 25 次／訊號 0；低於門檻，`state: healthy`。
- 同日納入網路層後的驗證回報：API Gateway 31 次／HTTP 錯誤 4、Storage 11 次／HTTP 錯誤 0、Auth 4 次／訊號 4、Edge Function 內部 45 次／訊號 0、Edge Function 網路 26 次／HTTP 錯誤 6／訊號 10；已超過網路層門檻，產生 `error_threshold` 告警並回報 `state: alerted`。這是監測驗證事件，不代表已判定為入侵；後續應在告警頁確認來源路徑及必要封鎖策略。
- Logs Explorer 六組已保存查詢 ID：API 基線 `5d3b4e49-5fa4-4b52-88c2-4f324b249d17`、API 異常 `b0ece693-0825-423d-821c-f21712c2ffb9`、Auth `e8b3dfb2-acfb-4dbb-838c-337abdbd20e9`、Edge Function 內部 `93ccb0fe-ca81-4b52-bb9a-44a23949d48d`、Edge Function 網路與執行 `4371058b-66b0-4300-93e6-13aaf286d041`、Storage `dba12860-68f4-4af1-a6d6-6906a3419807`。

### 2026-08-25｜P1 無 JavaScript synthetic probe

- 新增 `tools/synthetic-probe.mjs` 與 `.github/workflows/synthetic-probe.yml`，每 15 分鐘由固定 GitHub Actions runner 執行一次；不開啟瀏覽器、不載入頁面 JavaScript、不寫入業務資料。
- 探針只做三個小型唯讀檢查：GitHub Pages V2 入口 `HEAD`、Supabase REST `locations?select=location_id&limit=1`（anon key；目前依 RLS 預期回 HTTP 401／錯誤碼 `42501`）、私有 `floorplans/B1.png` 公開路徑 `HEAD`（預期 HTTP 400，持續驗證匿名封鎖）。每次記錄 HTTP 狀態、網路錯誤與延遲；不符合預期的 4xx／5xx 或延遲超過 3 秒告警，超過 10 秒為嚴重告警，Storage 意外回 2xx 另以可疑檔案存取告警。
- 同一 15 分鐘視窗以 Supabase Management Logs API 讀取 `edge_logs`／`storage_logs` 請求量；依既有平台監測的 20 分鐘 1,000／500 門檻換算，API Gateway 達 750 次或 Storage 達 375 次即視為流量突增。這是低頻門檻，不是壓力測試；真實流量仍由既有平台日誌監測補充。
- 異常透過既有 `platform-log-alert` 寫入 `security_alerts` 並依設定送 LINE；V2 告警頁已補上 GitHub Pages／REST／Storage／流量探針的繁體中文資源名稱。探針不輸出 `SUPABASE_ACCESS_TOKEN`、anon key 或 `CRON_SECRET`。
- 驗證：`node --check tools/synthetic-probe.mjs` 通過；手動執行 [No-JavaScript synthetic probe #32864995595](https://github.com/jnfakimo/Inspection/actions/runs/32864995595) 成功回報 `state: healthy`：GitHub Pages HTTP 200／202 ms、REST HTTP 401／預期拒絕／638 ms、Storage HTTP 400／預期封鎖／544 ms；同視窗 API 173 次、Storage 9 次，均未達流量突增門檻。

### 2026-08-25｜P2 方案、Log Drain、DDoS 通報與費用上限驗證

- **目前方案與費用邊界**：Supabase Dashboard 顯示組織為 **Free Plan**，計費週期 `2026-08-17 — 2026-09-17`；頁面顯示 Spend cap 已啟用、沒有超額計費。當時用量為 DB 0.061／0.5 GB（12%）、快取流量 0.154／5 GB（3%）、一般流量 0.083／5 GB（2%）、Edge invocations 8,136／500,000（2%）、Storage 0.015／1 GB（2%）、Realtime 峰值 3／200（2%）、MAU 19／50,000（<1%），均未超過 Free 配額。方案及費用規則以 [Supabase Pricing](https://supabase.com/pricing) 與 [Cost Control](https://supabase.com/docs/guides/platform/cost-control) 為準。
- **Log Drain**：專案 Log Drains 頁面顯示目前方案不可用，須升級 Pro／Team／Enterprise；Audit log drains 亦須 Team／Enterprise。本次未升級方案、未建立外部端點，避免在缺少目的地與預算核准時產生付費項目。官方限制與計費說明見 [Log Drains](https://supabase.com/docs/guides/monitoring-and-debugging/log-drains)。
- **DDoS 與事件通報**：Supabase 官方說明由邊緣 Cloudflare 提供 DDoS 防護，專案本身沒有可由 repository 開啟的 DDoS 開關；應用層事件由既有平台 Logs monitor／`security_alerts`／LINE 處理。Supabase Status 可訂閱 Email、Webhook、Slack、Teams 或 RSS，但本次未填入外部收件端點；通報入口為 [Supabase Status](https://status.supabase.com/)／[通知設定](https://status.supabase.com/uptime)。驗證當下 Status 顯示 API Gateway `degraded_performance`，屬 Supabase 公開平台狀態，不是本次演練造成。
- **受控封鎖邊界演練（2026-08-25 23:08 Asia/Taipei）**：對私有 `floorplans` 公開路徑連續送出 12 次 `HEAD`（無登入、無 API key、無寫入），12／12 均回應 HTTP 400，耗時 143–618 ms；再對 `/rest/v1/floor_models?select=floor_id&limit=1` 發送 1 次未帶 API key 的唯讀請求，回應 HTTP 401（`No API key found in request`）。這確認匿名 Storage 與未授權 REST 的拒絕邊界；未執行高併發、持續流量或 DDoS／壓力攻擊。
- **後續條件**：若要外部長期留存、跨平台事件通報或更長日誌保留，需先核准升級方案、Log Drain 月費／用量費與通知目的地，再由平台管理員設定；目前 V2 專案以既有 GitHub Actions 平台日誌監測與 LINE 告警維持可追溯性。

### 2026-08-26｜資安告警持續推播原因與修正

- 2026-08-25 21:39／22:00 的 `function_edge_logs` 分別為 6／9 次請求、各 4 次 HTTP 4xx；原監測器又把 `error／failed／denied／invalid／rate` 文字訊號合併計數，達到 5 次門檻，因此在每 10 分鐘排程持續推播。這是系統性誤報風險，不足以判定人為入侵。
- `tools/platform-log-monitor.mjs` 已改為 Edge Function 網路層只對 HTTP 5xx、HTTP 429、明確上游／連線／逾時錯誤告警；正常 401／403／404 仍保留於證據明細。`platform-log-alert` 同一事件 LINE 冷卻改為 30 分鐘。
- 修正 Commit `b8cead0`；Edge deployment [#32904998748](https://github.com/jnfakimo/Inspection/actions/runs/32904998748)、平台監測 [#32905053097](https://github.com/jnfakimo/Inspection/actions/runs/32905053097)、synthetic probe [#32905105993](https://github.com/jnfakimo/Inspection/actions/runs/32905105993) 均成功，修正後平台監測與 synthetic probe 回報 `state: healthy`。完整研判見 [[08-資安告警原因與修正報告]]。

### 2026-08-25｜V2 資料庫備份與復原流程建立

- 新增 [[07-資料庫備份與復原流程]]，把 PostgreSQL 邏輯備份、四個 private Storage bucket、Auth／Realtime／Webhook／extensions、Edge Function 原始碼與平台 Secret 管理分開列管；明確要求 DB dump 不得進 Git，Secret 只留在受控 Secret Store。
- 目前 Supabase 為 Free 方案，未假設平台每日備份、未升級方案、未啟用 PITR，亦未連線正式資料庫執行備份或復原。流程採人工核准、加密異地保存、SHA-256 第二人覆核，首次演練必須在新／暫存專案完成。
- Obsidian／待辦清單已將「建立流程」標記完成，另保留「首次加密備份與暫存復原演練」未完成項，避免將文件建立誤當成資料可恢復性已驗證。

### 2026-08-26｜V2 圖資專案來源統一與整合標記改版驗證

#### 變更內容

- 確認整合標記系統已使用 V2 路由 `/v2/systems/structuremap/markers/`；V1 `b1_integrated_marker_system.html` 僅保留歷史相容，不列入目前 V2 驗收。
- 新增共用圖資路由定義，圖資專案設定、3D 建模、區域位置表、整合標記、平面樓層圖、3D 模型圖與巡邏點清單全部改為 V2 入口，移除 V2 畫面殘留的 V1 導覽。
- 3D 建模系統是唯一圖資來源：DXF 產出寫入共用樓層模型與私有樓層圖檔；V2 整合標記、平面圖、3D 模型與立體巡檢雲臺均直接讀取同一份專案圖資，登入後以短效授權網址載入。
- 「圖資專案設定」新增平面樓層圖入口與共用來源說明，避免人員誤以為各圖臺各自維護圖檔。

#### 驗證與部署證據

- 自動一致性檢查通過：樓層正規化、四個圖臺讀取共用樓層模型／私有圖資，以及 V2 導覽不得回到 V1。
- `npm test` 通過；資安設計稽核錯誤 0；GitHub CI、商用檢查與 Pages build／deploy／verify 全部成功。
- 正式登入驗證：V2 平面圖成功載入 B1 圖檔；整合標記成功載入 26 個標記；3D 模型成功產生 Canvas 並顯示 7 層；三者皆觀測到同一 Supabase `floor_models` 與私有 `floorplans` 來源。
- 功能 Commit：`979bab5`；Hardened Pages deployment [#32931190066](https://github.com/jnfakimo/Inspection/actions/runs/32931190066)；CI [#32931190067](https://github.com/jnfakimo/Inspection/actions/runs/32931190067)；Commercial readiness checks [#32931190062](https://github.com/jnfakimo/Inspection/actions/runs/32931190062)。

### 2026-08-26｜CodeQL 告警清理與 Actions 版本統一

#### 起點：一條過時的待辦

待辦「將 CodeQL Action 由已淘汰的 v2 升級至 v3」查證後確認**早已完成，且直接升到 v4**（08-21 `8febc3a` 升 v3、`d54c9ee` 再升 v4）。實際執行 CodeQL CLI 2.26.3、87 條規則，每次 push 都上傳分析，執行紀錄零淘汰警告。

真正的缺口不在版本，而在**沒有人看結果**：掃描一路正常，卻累積了 27 筆從未研判的告警（18 high、9 medium）。這與「CI 紅了 21 次沒人發現」是同一種失效——機制在跑，沒有人在收。

#### 修掉的 9 個真缺陷

| 缺陷 | 位置 | 說明 |
| --- | --- | --- |
| 臨時密碼可預測 | `system/admin.html` | `genTempPassword()` 用 `Math.random()` 產生實際發給同仁的密碼。V8 的 xorshift128+ 可由少量輸出還原內部狀態，批次匯入時任何人拿到自己那組就能推算同批其他人的。改用 `crypto.getRandomValues` |
| 取模偏差（自行引入後修正） | `system/admin.html` | 上一項改完後 CodeQL 報 `js/biased-cryptographic-random`。32 確實整除 256、本無偏差，但改用 `b & 31` 把「字集長度必須是 2 的冪」這個前提寫進程式碼，換字集也不會悄悄出現偏差 |
| onclick 屬性注入 | `system/b1_integrated_marker_system.html` | `it.id` 內插進 `onclick`。**即使用 `esc()` 也擋不住**——逸出的 `&#39;` 會先被 HTML 解析器還原成單引號，才交給 JS 解析器。改為 data 屬性＋事件委派 |
| DOM 文字重新解析為 HTML | `system/theme.js` | `document.title` 的文字拼進 `innerHTML`。改用 `textContent`。此函式掛在全站每一頁 |
| 正則指數回溯 | `supabase/functions/cwa-weather/index.ts` | `[^']` 也吃得下反斜線，與逸出分支重疊。實測每多兩組重複耗時乘四（20 次 5.9ms、26 次 373.7ms），改後恆定 0.003ms |
| 不完整消毒（4 處） | `system/structure_map.html` | `replace('\n', ' ')` 只換第一個換行，多行標籤在資訊面板與搬移提示仍會斷行。改為 `/\n/g` |
| 底層錯誤外流 | `supabase/functions/google-calendar/index.ts` | catch 一律回傳 `error.message`。三支被標的函式裡唯一一般登入使用者可觸發的（另兩支只接受 cron secret 或管理員）。改用 `UserError` 標記型別分流，未預期錯誤回通用訊息、細節走 `console.error` 進 `function_logs` |

#### 研判為誤報或不在安全邊界上的 14 筆

已在 GitHub 逐筆附理由標記 dismissed，隨時可 reopen：

- **登入頁 4 筆**（`js/xss`、open redirect）：`nextPath()` 有白名單，只回傳以 `/Inspection/v2/` 或 `/word-cloud/v2/` 開頭且不指向 login 的值。CodeQL 不認得自訂 sanitizer
- **`system/equipment.html` 1 筆**：`document.write` 的每個插值都經過 `esc()`，`src` 是 `canvas.toDataURL` 的 data URL
- **`system/supabase-config.js` 1 筆**：token 存 localStorage 是 supabase-js 的預設行為，V1 是純靜態站沒有伺服器可簽發 httpOnly cookie。已知並接受的設計取捨
- **兩支 cron 函式 2 筆**：只接受 CRON_SECRET 或 active 的 admin/sysadmin，錯誤訊息不會流到一般使用者，且是運維排查排程失敗的主要線索
- **`tools/` 6 筆**：建置與稽核腳本，處理自家 HTML、不接受不受信任輸入、不進正式環境

#### 一併處理

- `.github/workflows/apply-database-migration.yml` 的 `actions/checkout` 補到 `@v7`。08-21 統一升級時這支還不存在，是唯一的漏網；`.github/workflows/` 現已全數一致。該流程只由 `workflow_dispatch` 觸發，改版本不會誤觸資料庫 migration。

#### 結果與證據

- **告警 27 → 4，high 由 18 筆歸零。** 當下剩餘 4 筆 medium：`tools/build-hardened-pages.mjs` 兩筆（`JSON.stringify` 產生要嵌進 `<script>` 的字串，遇 `</script>` 或 U+2028/9 會截斷；目前輸入是自家路徑）、`docs/removed-edge-functions/` 兩筆（已移除、只留作文件的程式碼）。這 4 筆隨後由另一個工作階段收尾——歷史目錄加入 CodeQL `paths-ignore`、建置工具兩筆標記 `won't fix`，**現行開放告警 0 筆**（[CodeQL Analysis #32866250808](https://github.com/jnfakimo/Inspection/actions/runs/32866250808)）
- 每筆修正單獨 commit，可獨立回退：`998193c`、`14e909c`、`8d7f086`、`490f5d1`、`83d7b83`、`6bbb8a9`、`c837d98`、`42614df`
- `npm run scan:pages` 通過（38 頁、73 段內嵌程式、18 個自有 JavaScript）；`security:audit` 維持錯誤 0／警告 4 的既有基準；`security:secrets` 通過；`deno check` 通過
- `c837d98` 全綠：CI [#32865903814](https://github.com/jnfakimo/Inspection/actions/runs/32865903814)、CodeQL [#32865903747](https://github.com/jnfakimo/Inspection/actions/runs/32865903747)、**Edge functions deployment [#32865903745](https://github.com/jnfakimo/Inspection/actions/runs/32865903745)**（`google-calendar` 修正已實際部署）、Hardened Pages [#32865903802](https://github.com/jnfakimo/Inspection/actions/runs/32865903802)、Commercial readiness [#32865903774](https://github.com/jnfakimo/Inspection/actions/runs/32865903774)

### 2026-08-26｜資料庫備份自動化（執行層）

> 治理層——核准流程、責任分工、RPO／RTO 目標與驗收證據表——見 [[07-資料庫備份與復原流程]]。本節只記實作。

#### 關鍵發現：Free 方案不必升級也能自動備份

`07` 原本假設 Free 只能靠人工 `pg_dump`，需要資料庫密碼與 Docker（本機三樣都沒有，`supabase db dump` 因缺 Docker 直接失敗）。實際上 **Management API 的 `/database/query` 用既有的 `SUPABASE_ACCESS_TOKEN` 就能唯讀匯出**，不需資料庫密碼、不需 Docker，且可自動化。RPO 因此從「每週人工」變成「每日自動」。

盤點現況：Free 方案、無自動備份、無 PITR；`public` schema 67 張表約 11,450 筆／20 MB；Storage 五個 bucket 約 13 MB。Schema 本身有版控可重建，**真正沒有第二份的是資料**。`trg_prevent_removal` 只擋得住 DELETE 與 TRUNCATE，擋不住寫錯條件的 UPDATE。

#### 實作

- `tools/database-backup.mjs`：逐表分頁以 `row_to_json` 匯出成 NDJSON，型別轉換交給 PostgreSQL；一併下載 Storage 附件。service_role key 只在記憶體停留，不寫檔也不列印
- `tools/database-backup-verify.mjs`：比對 sha256 與行數，對不上就讓流程失敗
- `tools/database-restore.mjs`：**不接任何 workflow，只能人工執行**。預設演練，要 `--execute` 且 `--confirm` 對得上專案 ref 才寫入；一律 upsert 或 insert-missing，**永不刪除**，符合正式資料只能追加／更新／停用
- `tools/database-backup-consistency-check.mjs`：23 項離線測試，已接進 `npm test`
- `.github/workflows/database-backup.yml`：每天台北時間凌晨 2 點，AES256 加密後存 artifact（保留 90 天）

#### 為什麼加密是強制的

**本 repo 是 public，artifact 任何人都下載得到**，而備份含全公司人員個資與巡檢／報修紀錄。缺少 `BACKUP_PASSPHRASE` 時流程直接中止，不會退而求其次產生未加密的檔案；上傳前還會確認產物確實解不開成 tar。這與 `07` 第 3 節「不要使用 repository 儲存」有張力，取捨已寫在該處。

#### 驗證

- 對正式庫實測（全部唯讀）：`row_to_json` 分頁查詢、主鍵查詢，以及 `jsonb_populate_recordset` 的型別 round-trip——3 筆進 3 筆出，UUID 與 timestamp 均完整還原
- 離線測試釘住分頁邊界（200 筆／201 筆／空表），**201 那組正是最容易靜默截斷的情況**；另驗證 checksum 抓得到竄改、行數抓得到截斷、還原語句不含 delete/truncate/drop、演練模式 0 次寫入、表名含 SQL 片段會被拒絕
- `npm test` 通過（含新增的 `test:backup`）
- 功能 Commit：`0e780f7`；CI [#32868339490](https://github.com/jnfakimo/Inspection/actions/runs/32868339490)、CodeQL [#32868338943](https://github.com/jnfakimo/Inspection/actions/runs/32868338943)、Hardened Pages [#32868338674](https://github.com/jnfakimo/Inspection/actions/runs/32868338674)、Commercial readiness [#32868338378](https://github.com/jnfakimo/Inspection/actions/runs/32868338378)

#### 首次實際執行（當日稍晚，由保管人完成）

- 保管人設定 `BACKUP_PASSPHRASE` 後手動觸發，**流程一次跑通**：Database backup [#32928929277](https://github.com/jnfakimo/Inspection/actions/runs/32928929277)，2026-08-26 12:08:50（UTC+8），commit `0bd3d0c`
- 產出 **67 張表、12,272 筆資料、33 個 Storage 物件**，加密 artifact `inspection-backup-20260826T040850Z.tar.gz.gpg`（ID `9592559330`，保留 90 天）
- 筆數與盤點時的估計相符（估 11,450 筆為 `reltuples` 統計值，非精確計數）

#### 尚未完成

- **暫存專案復原演練仍未執行。** 備份跑通不等於資料可恢復性已驗證——沒有還原過的備份不算數。需先核准演練時間與暫存專案，不得在正式資料庫上演練
- `07` 第 8 節證據表的 SHA-256 第二人覆核、Storage 抽樣、RPO／RTO 實測仍待填
- Auth 帳號刻意不在備份範圍：完整復原後所有人都無法登入，需管理員重建帳號並重新綁定 `auth_id`
- artifact 只保留 90 天，長期保存需定期下載歸檔；要真正異地備援仍需另接外部物件儲存

#### 順帶發現

`inspection-photos` 是 **public bucket**（目前為空）。先前的 Storage 資安盤點只列了四個 bucket，漏了它——一旦有人上傳巡檢照片就會匿名可讀，與剛處理完的 `floorplans` 是同類問題。已寫進 `AGENTS.md`，**尚未變更該 bucket 的設定**。

### 2026-08-26｜V2 使用者操作手冊補齊

- 新增 `docs/V2-使用者操作手冊.md`，以 V2 八大系統與目前正式路由為基準，整理登入／帳號申請、共用導覽、個人資料、維修派工、派車核可、駐衛警巡檢、電子交接、設備建置、圖臺、會議室與後台管理流程。
- 手冊補上一般使用者、單位主管、派車管理員、駕駛、維修人員與系統管理員的操作責任，並明確記載權限不足、資安告警、離線補傳、附件與異常回報注意事項。
- 尚未驗收的離線衝突、部分通知／會議室情境與暫存資料庫復原演練，標示為待辦，不在手冊中宣稱已完成。

### 2026-08-26｜V2 駐衛警巡檢打卡與排班通報時段勾稽

- 巡邏打卡頁改由 `web/lib/patrol-status.ts` 統一解析每日班別：`patrol_shifts.start_time/end_time` 是班別時段，`patrol_shift_staff.workTimes` 是排班頁設定的通報時段；指定日期設定優先於班別範本。
- 打卡矩陣的逾期起點改用通報時段結束時間。以早班班別 08:00–16:00、通報 09:00–10:00 為例，10:00 後未打卡顯示 `✕`，不再因班別尚未到 16:00 而顯示 `…`；遲到但仍在班別時段內完成的打卡仍可轉為 `✓`。
- Edge Function `patrol-timeout-check` 同步讀取同一份每日／範本 `workTimes` 與每日班表，LINE／FCM 逾時通知的查詢區間、通知文字與打卡矩陣一致；停用的班別範本不再產生通知。
- 打卡頁矩陣欄標補上「班別」與「通報」兩行時間，頁首標題與內容間距收窄。驗證：`node node_modules/typescript/bin/tsc -p web/tsconfig.json --noEmit`、`deno check supabase/functions/patrol-timeout-check/index.ts`、`node tools/patrol-offline-consistency-check.mjs` 與 `git diff --check` 均通過。正式頁面仍需登入後以 `?v=<commit>` 清快取驗收。

### 2026-08-26｜V2 夜班隔夜值班日修正

- 夜班的顯示值班日與實際資料日期分離：值班日 `D` 的夜班顯示在 `D`，但 00:00–08:00 的實際班別資料、通報時段與打卡時間軸使用 `D+1`，不再把凌晨紀錄算成當日夜班。
- `web/lib/patrol-status.ts` 成為唯一日期規則來源；巡檢打卡矩陣、首頁儀表板、巡檢排班頁與 `patrol-timeout-check` 共用同一套隔夜判定，畫面明確標示「（隔夜）」。
- 排班頁的範本套用 RPC 新增 `supabase/migrations/20260826120000_patrol_night_shift_overnight.sql`；套用到日期區間時，夜班資料列與每日人員／通報設定寫入下一個曆日。既有誤存於值班日的夜班先相容讀取，管理員下次編輯或套用即可轉入新規則。
- 逾時通知仍以原值班日寫入通知紀錄與訊息，實際查詢打卡則用隔日時間區間，避免 LINE／FCM 與打卡矩陣分歧。
- 驗證：`npm test`、`node node_modules/typescript/bin/tsc -p web/tsconfig.json --noEmit`、`deno check --no-lock supabase/functions/patrol-timeout-check/index.ts`、`git diff --check` 通過；本機缺少完整 Next CLI，`npm run build:v2` 未能執行，需由 GitHub Pages CI 完成建置驗證。

### 2026-08-26｜V2 公文傳送流程子系統（程式建置）

- 新增 `SYS-09 公文傳送系統`／`routing` 子系統：原申請人建立公文後，由公文管理人員依序選擇會辦部室，再送陳核部室；會辦收文／完成、陳核簽收／核決、退回補正重送與原申請人收訖均有獨立動作。
- 新增 `supabase/migrations/20260826150000_official_document_routing.sql`：`official_documents`、`official_document_steps`、不可修改／不可移除的 `official_document_events` 時間軸、`official_document_notifications` 及站內通知關聯欄位；事件以 `idempotency_key` 防止重複收文、簽收與掃描操作。
- 新增 `supabase/functions/official-document-timeout-check` 與 `.github/workflows/official-document-timeout.yml`：依收文／核決門檻建立承辦人與部室主管的站內逾期通知，預設 60／480 分鐘，可由 `system_settings` 調整。
- V2 頁面支援條碼掃描（瀏覽器 `BarcodeDetector`）、公文編號／主旨查詢、QR 條碼產生／下載／列印；掃描與查詢不會直接改變流程狀態。
- 權限沿用 `role_permissions` 的 `sys_officialdocs`，新增後台權限標籤與系統關係圖節點；migration 會為既有角色建立預設可用權限，後台可再依最小權限收緊。
- 驗證：`deno check --no-lock`（`app-api`、逾期通知函式）通過；V2 TypeScript 專案在隔離的完整依賴目錄 typecheck 通過；`security-design-audit` 錯誤 0／既有警告 4；schema contract 311 組通過；事件／migration 語法與 `git diff --check` 通過。
- 程式提交：`2aa215e`（`feat(v2): add official document routing subsystem`）；推送後由 Pages 與 Edge Functions workflow 執行部署，正式 migration／跨角色驗收仍待完成。
- 2026-08-26 修正建立公文時的舊 Node API 相容性：若 Render 尚未完成滾動部署而回傳「不支援的 API 動作」，前端只針對此訊息回退 Supabase Edge Function，其他驗證錯誤不重送；`Edge functions deployment #32`（run `32969771547`）與 `Hardened Pages deployment #491`（run `32969771526`）均已成功，提交 `c3b8076`。
- 追加修正公文 API 限流範圍：改沿用已存在的 `app-api`／`admin-api:write` scope，避免正式資料庫尚未新增專用 scope 時被限流服務拒絕；待正式 migration 套用後再評估是否拆分專用門檻。
- 上線檢查：正式 REST 回應 `PGRST205 Could not find the table public.official_documents`，確認建立失敗主因為 `20260826150000_official_document_routing.sql` 尚未套用；未取得正式資料庫核准前不直接執行。
- 2026-08-26 驗證 migration 已套用：`official_documents` REST 查詢回應 HTTP 200、回傳空集合；登入後公文頁面重新整理可正常載入，錯誤訊息消失，尚未代建測試公文。
- 尚待：將 migration 套用正式 Supabase、部署兩支 Edge Function、GitHub Pages 上線後，以一般使用者→公文管理人員→會辦收發→陳核／核決→原申請人完成跨角色驗收；未完成前不宣稱正式流程已驗收。

### 2026-08-26｜V2 公文取號、條碼與單位階層權限補強

- 建立公文改由系統依臺北日期自動產生 `YYYYMMDD`＋三碼流水號；同日重號會查詢後取下一號，若併發撞到唯一索引則自動重試，查詢條碼固定與公文編號相同。
- 目標部室前端下拉與後端動作驗證均只接受第一階單位；單位主管／公文管理人員的人員與公文可見範圍依本單位及下屬單位遞迴計算，一般使用者只回傳本人送出的公文；建立與送出公文限定公文管理人員。
- 新增 `supabase/migrations/20260826170000_official_document_barcode_number_sync.sql`：先將既有條碼校正為公文編號，再以資料庫 trigger 維持一致；只更新欄位，不刪除資料。已由 `Apply database migration` workflow run `32974213919` 成功套用；正式 REST 查詢 `official_documents` 回應 HTTP 200。
- 操作手冊與 RDQ 需求規格已同步更新；本次驗證：`deno check --no-lock` 通過、前端 TSX 轉譯檢查通過、資安設計檢查錯誤 0／既有警告 4、schema contract 312 組通過、migration guard 與 `git diff --check` 通過。
- 程式提交：`68e71b3`（`feat(v2): enforce official document numbering and unit scopes`）；Edge Functions deployment run `32973417601`、CI run `32973417641`、Commercial readiness run `32973417681`、Pages run `32973417583`、CodeQL run `32973417610` 均已成功。

### 2026-08-26｜V2 公文傳送頁版型修正

- 修正 `web/app/official-docs.css` 被覆蓋造成的版型異常：恢復區段標題、搜尋列、清單／明細雙欄、流程卡片、時間軸、建立公文與響應式窄版規則，保留單位人員資料表樣式。
- 驗證：登入後桌面版標題／搜尋／清單／明細排列正常；390px 窄版改為單欄，按鈕與輸入框不水平溢出。
- 程式提交：`f4b4e24`（`fix(v2): restore official document workspace layout styles`）；Hardened Pages deployment run `32974782805`、CI run `32974782617`、Commercial readiness run `32974782644`、CodeQL run `32974782637` 均已成功。

### 2026-08-26｜V2 公文傳送頁查詢、分頁與建立流程補強

- 可查詢公文區在標題左側顯示目前帳號可見的第一階單位；公文清單固定每頁 5 筆，換頁不改變右側流程明細。
- 建立公文保留主旨輸入列，寫入動作固定走已部署的 Supabase Edge Function，避開尚未同步自動取號欄位的舊 Node API；編號仍由後端依臺北日期產生 `YYYYMMDD`＋三碼流水號，查詢條碼與公文編號一致。
- 單位人員資料新增姓名／單位／角色／第一階或第二階篩選，使用可輸入的下拉提示；表格分開顯示第一階單位與第二階單位，每頁 10 筆。後端回傳單位根節點、完整路徑與階層，並維持單位主管／公文管理人員的下屬單位可見範圍。
- 驗證：`deno check --no-lock supabase/functions/app-api/index.ts`、前端 TSX 轉譯、`security-design-audit`（錯誤 0／既有警告 4）、schema contract（312 組）、`git diff --check` 均通過。完整 `tsc -p web/tsconfig.json` 受目前工作區缺少可解析的 Next／React 依賴阻斷，已由 CI 建置驗證。
- 程式提交：`132596a`（`fix(v2): improve official document workspace`）；CI run `32976480759`、Edge functions deployment run `32976480677`、Commercial readiness run `32976480667`、Hardened Pages deployment run `32976480679`、CodeQL run `32976480609` 均成功。

### 2026-08-26｜V2 公文傳送頁視覺與單位標示微調

- 「02 公文清單」標題左側新增目前公文管理人員所屬的第一階單位（本次登入驗證顯示「管理部」），由後端依部門階層根節點回傳，避免只顯示全部可見單位而無法辨識目前管理範圍。
- 建立公文列改為主旨輸入佔主要寬度、自動取號固定窄欄，桌面版比例一致；手機版仍自動堆疊為單欄。
- 流程指示列 1 建立、2 依序會辦、3 陳核簽收、4 核決、5 原申請人收訖加入青／綠／琥珀／紅／青色階，已完成節點另加完成底色，未完成節點仍保留辨識色。
- 驗證：正式頁 `?v=1929e44` 登入後 DOM 顯示「第一階單位：管理部」、建立列主旨寬／按鈕窄；流程節點 computed color 分別為青、綠、琥珀、紅、青；桌面版版型與人員資料區正常。`deno check`、TSX 轉譯、資安檢查錯誤 0、schema contract 312 組、`git diff --check` 通過。
- 程式提交：`1929e44`（`fix(v2): refine official document layout`）；CI run `32978292414`、Edge functions deployment run `32978292284`、Commercial readiness run `32978292766`、Hardened Pages deployment run `32978292463`、CodeQL run `32978292416` 均成功。

### 2026-08-26｜V2 公文傳送頁單位摘要、申請人階層與條碼尺寸補強

- 第 03「建立公文」區塊新增統計卡：目前登入者的第一階單位（本次驗證為「管理部」）、目前會辦公文數量、本單位送出公文數量；數字由已載入的可見公文資料計算，不新增或修改公文。
- 公文明細的原申請人改顯示完整階層路徑，例如 `黃建發 管理部／總務課`，保留第一階與第二階單位辨識。
- 公文查詢 QR 條碼預覽與列印圖固定為寬 4 公分、高 1.5 公分（瀏覽器 96dpi 實測約 151.177px × 56.688px）。
- 單位人員資料篩選改為姓名、第一階單位、第二階單位、角色四欄，與表格欄位對齊；第一／第二階選項分開提供，每頁仍為 10 筆。
- 線上登入驗證：`20260826002` 明細顯示 `黃建發 管理部／總務課`；第 03 區塊顯示「管理部／目前會辦公文 1／本單位送出公文 3」；人員篩選列顯示「第一階單位」「第二階單位」；公文清單維持每頁 5 筆。
- 驗證：TSX 轉譯、`security-design-audit`（錯誤 0／既有警告 4）、schema contract（312 組）、`git diff --check` 通過；CI run `32980856669`、Hardened Pages deployment run `32980856626`（build／deploy／verify 均成功）、Commercial readiness run `32980856551`、CodeQL run `32980856572` 均成功。
- 程式提交：`5349b04`（`fix(v2): add official document unit summary`）；本次僅前端與樣式變更，未新增 Edge Function 或資料庫 migration。

### 2026-08-26｜V2 公文清單摘要位置、Code 39 一維條碼與完成階段視覺修正

- 統計卡由第 03「建立公文」移至第 02「公文清單」標題下：左側顯示登入者第一階單位，右側顯示目前會辦公文數量與本單位送出公文數量；本次登入驗證為「管理部／1／3」。
- 查詢條碼由 QR 二維圖改為 Code 39 一維條碼，條碼圖固定寬 3.5 公分、高 1.5 公分；保留公文編號文字供人工核對，瀏覽器既有掃描器格式清單包含 `code_39`。
- 流程列已完成階段改為淺螢光綠膠囊樣式，階段之間以白色線段區隔；`1150826001` 驗證顯示「建立」與「依序會辦」已完成，其餘階段維持待辦色彩。
- `1150826001` 時間軸已確認保留 2026/08/26 21:03:44 建立、21:03:44 產生查詢條碼、21:54:54 送出會辦三筆事件，申請人顯示 `黃建發 管理部／總務課`。
- 驗證：正式頁登入後 DOM、流程色彩 computed style、條碼 SVG 輸出與尺寸（132.281px × 56.688px，即 3.5cm × 1.5cm）確認；TSX 轉譯、`security-design-audit`（錯誤 0／既有警告 4）、schema contract（312 組）、`git diff --check` 通過。CI run `32982411405`、Hardened Pages deployment run `32982411481`（build／deploy／verify 均成功）、Commercial readiness run `32982411404`、CodeQL run `32982411394` 均成功。
- 程式提交：`8691a0a`（`fix(v2): move official document summary and use barcode`）。

### 2026-08-26｜V2 公文摘要移至頁首與完成時間軸高亮

- 依畫面指示將三張摘要卡移至頁面上方右側，與「可查詢公文」數字卡同列；第 02 公文清單恢復為清單標題與重新載入，不再重複顯示摘要卡。
- `1150826001` 的完整時間軸事件區改為淺螢光綠填色，事件之間使用白色分隔線；已確認建立、產生查詢條碼、送出會辦三筆事件均保留。
- 條碼尺寸依最新指示調整為寬 3 公分、高 1.8 公分；正式頁 computed style 約 113.381px × 68.024px（96dpi），仍為 Code 39 一維 SVG 條碼。
- 登入後驗證：摘要卡位於頁首右側；`1150826001` 申請人顯示 `黃建發 管理部／總務課`，時間軸三筆事件背景均為綠色且第二筆起有白色分隔線。
- 驗證：TSX 轉譯、`security-design-audit`（錯誤 0／既有警告 4）、schema contract（312 組）、`git diff --check` 通過；CI run `32983784523`、Hardened Pages deployment run `32983785143`（build／deploy／verify 均成功）、Commercial readiness run `32983784880`、CodeQL run `32983784448` 均成功。
- 程式提交：`cdf1334`（`fix(v2): relocate official document summary`）。

### 2026-08-26｜V2 公文查詢條碼尺寸調整

- 依最新版面需求，公文查詢條碼（Code 39 一維條碼）一般顯示、彈窗及列印樣式統一為寬 2 公分、高 0.8 公分。
- 驗證：前端 TSX 轉譯、`security-design-audit`（錯誤 0／既有警告 4）、schema contract（312 組）、`git diff --check` 通過；GitHub Actions 待本次提交完成排程後確認。
- 程式提交：`8e34c8c`（`fix(v2): resize official document barcode`）。

### 2026-08-26｜V2 公文頁摘要圖卡與建立按鈕配色美化

- 頁首三張摘要圖卡改為青／琥珀／綠三種主題色，增加柔和漸層、上緣色帶、邊框與陰影層次，保持一般版與科技版皆由主題 token 控制。
- 「建立並自動取號」按鈕指定為淺藍色 80% 混合色，加入 hover／focus 狀態；僅套用建立公文按鈕，不影響其他流程操作按鈕。
- 驗證：TSX 轉譯、`security-design-audit`（錯誤 0／既有警告 4）、schema contract（312 組）、`git diff --check` 通過。
- 程式提交：`3f9a9f9`（`fix(v2): polish official document card colors`）；Pages 部署待 GitHub runner 排程完成後確認。

### 2026-08-26｜GitHub Actions 啟用與部署佇列修正

- 盤點確認 repository Actions 總開關已啟用，17 個 workflow 均為 active；補強 CI、CodeQL、Commercial readiness 的分支併發控制，僅保留最新驗證，避免舊 run 堆積佔用 hosted runner。
- CI 與 CodeQL 補上 `workflow_dispatch`，可由 GitHub Actions 介面手動重跑；主要流程已重新啟用。
- `error-threshold-check` Edge Function 已依資安政策停用，workflow 改將 `state=disabled` 視為預期成功並顯示 notice，只有 degraded／missing 或其他 HTTP 錯誤才失敗。
- 驗證：CI `32986223312`、CodeQL `32986225426`、Commercial readiness `32986201635`、Error threshold `32986362584`、Hardened Pages `32986404680` 均成功；Pages build／deploy／verify 全部成功。正式公文頁 HTTP 200，CSS 已確認包含條碼 2×0.8 公分、三色圖卡與建立按鈕淺藍 80% 樣式。
- 程式提交：`e33d781`（併發控制）、`57720fb`（CI／CodeQL 手動啟動）、`14a9e04`（停用門檻狀態正確處理）。

### 2026-08-27｜V2 公文頁圖卡、掃描查詢、條碼與建立表單調整

- 頁首改為四張同尺寸摘要圖卡：登入者第一階單位、目前會辦公文、本單位送出公文、可查詢公文；統一標題／動態資訊字級，並以青、琥珀、綠、紅配色區分。
- 01／02／03 區段分別使用青、綠、琥珀色識別；掃描按鈕明確標示「開啟相機／掃描器」，掃描結果用於查詢單一文件的最新動態與完整時間軸，不執行收文或簽收。04 單位人員資料圖塊已取消顯示。
- Code 39 條碼改為 6 公分 × 2 公分，版面比照紙本參考圖，包含上方民國日期、中間一維條碼與下方完整編號；下載時輸出 709 × 236 PNG，列印維持實體尺寸。
- 新建文件編號統一為民國年三碼＋月日＋四碼流水號（`YYYMMDDNNNN`）；建立區新增公文／採購單／其他類別、第一階單位、第二階單位與該二階單位人員連動選單，建立按鈕桌面寬度縮小約三分之一。
- 新增 `20260827090000_official_document_type_and_responsibility.sql`，保存文件類別、承辦單位與承辦人員；既有資料以原申請人／單位補值，不刪除資料。正式 migration 已由 Apply database migration run `33033334194` 成功套用；跨角色建立驗收仍列待辦。
- 驗證：前端 TSX 與 Edge Function 均通過 esbuild 轉譯；資安設計檢查錯誤 0／既有警告 4；schema contract 314 組與 `git diff --check` 通過。CI run `33033331519`、Commercial readiness run `33033331484`、Hardened Pages run `33033331525` 均成功；首次 Edge deployment 因 GitHub API rate limit 無法解析 CLI 版本而失敗，改以 workflow dispatch 重跑 app-api，run `33033424868` 的 Deno type-check、部署與端點冒煙測試均成功。
- 程式提交：`af445b3`（`feat(v2): refine official document creation and barcode`）。

### 2026-08-27｜Obsidian 開發紀錄流程與 MCP 設定檢查

- 查核結果：`antigravity-obsidian` 技能已安裝，且 `mcpvault v0.12.1` 已存在；但原本 Codex 使用者設定沒有 `[mcp_servers.obsidian]`，因此一般開發任務不會自動產生 Obsidian 操作，也不會因「開工」一詞自動寫入紀錄。
- 已補上本機 MCP 設定，指向本專案內含 `.obsidian` 設定的 `Obsidian/` vault；此設定只存在本機，不寫入公開 repository，需重新啟動 Codex 後載入。
- 已確認專案規範：開發紀錄的公開同步來源是 repository 內 `Obsidian/` Markdown；即使 MCP 暫時不可用，完成程式／資料庫／資安／部署工作後仍須直接追加 `04-開發與部署.md`，不得跳過。
- 回填近期漏記的公開工作：台灣氣象圖示位置修正（`0c77285`）、報修清單 100% 縮放不出現水平捲軸（`058a34a`）、報修單位顯示一階／二階完整路徑（`fe1c756`、`81db18d`）。未記錄任何密鑰、Session、Cookie 或私有網址。
- 驗證：`mcpvault --help` 可執行、vault 路徑存在且含 `.obsidian`、`git diff --check` 通過；MCP 工具需重啟 Codex 後才能在工作階段載入。

### 2026-08-27｜V2 公文頁第二輪操作與視覺修正

- 四張頁首圖卡高度改為約原版 70%，保留四卡同高與響應式排列；02 公文清單改為間隔淺藍跳色，建立表單的一階單位鎖定登入者目前根單位，不再顯示其他一階單位。
- 掃描器先取得並顯示裝置鏡頭，再依瀏覽器能力啟用 BarcodeDetector；不支援原生掃描 API 時也會開啟鏡頭並提供掃描器／手動查詢提示，不再直接呈現空白灰框。
- 狀態標籤 `draft` 改為「待送出」；公文明細加入目前流程節點非登入者部室時的原因提示。資料查核確認 `1150826001` 目前節點為「第一市場／待收文」，登入者為「總務課」，因此收文按鈕未顯示屬權限範圍正確行為。
- 驗證：`1150826001` Supabase 唯讀查核、前端／Edge Function esbuild 轉譯、資安設計檢查錯誤 0／既有警告 4、schema contract 314 組、`git diff --check` 通過；CI run `33035052095`、Commercial readiness run `33035052080`、CodeQL run `33035052065`、Hardened Pages run `33035052029`（build／deploy／verify）均成功。
- 程式提交：`e41b7ddc`（`fix(v2): refine official document scanner and routing cues`）。

### 2026-08-27｜V2 公文頁摘要卡固定高度

- 修正四張摘要卡因父層 `stretch` 與百分比高度互相作用而越變越高的問題；桌面版固定每張 160px、手機版 126px，標題 14px、數字 22px，移除 `height: 100%` 拉伸來源。
- 驗證：前端 TSX esbuild 轉譯、資安設計檢查錯誤 0／既有警告 4、schema contract 314 組與 `git diff --check` 通過；部署提交 `4f00ce39`。

### 2026-08-27｜V2 公文一維條碼相機掃描與清單視覺修正

- 新增鎖定版本 `@zxing/browser@0.2.1`（含 `@zxing/library`），以 `BrowserMultiFormatOneDReader` 搭配相機 constraints 掃描 Code 39、Code 128、Code 93、EAN、ITF、Codabar、UPC 等一維條碼；保留相機權限、找不到裝置與相機被占用的繁中提示，掃描成功後帶入公文查詢。
- 桌面四張摘要卡固定為 80px（手機維持 126px）；02 公文清單隱藏目前會辦／陳核節點已簽收或完成的文件，並顯示隱藏筆數；清單跳色與不可刪除完整時間軸的色彩強度降低約 80%。
- 驗證：V2 `typecheck` 通過、前端 esbuild 產出含 ZXing 掃描器、資安設計檢查錯誤 0／既有警告 4、schema contract 314 組、`git diff --check` 通過；CI `33036733734`、Commercial readiness `33036733700`、CodeQL `33036733707`、Hardened Pages `33036733762` 均成功。

### 2026-08-27｜V2 公文頁手機版與相機預覽修正

- 掃描器改為頁面先取得單一後鏡頭串流，明確設定 `autoplay`／`muted`／`playsInline` 並交由 ZXing `decodeFromStream` 解碼，修正 iPhone 出現相機綠點但預覽灰框的問題；相機軌道在關閉或失敗時會完整停止。
- 手機四張摘要卡固定 60px；頁首流程說明保持單列並可水平捲動；第一階單位清單以單一文字列呈現，空間不足時自然換行。
- 驗證：V2 `typecheck`、官方文件頁 esbuild、資安設計檢查錯誤 0／既有警告 4、schema contract 314 組與 `git diff --check` 通過；CI `33038022141`、Commercial readiness `33038022119`、CodeQL `33038022131`、Hardened Pages `33038022127`（build／deploy／verify）均成功。
- 程式提交：`384ad016`（`fix(v2): restore mobile barcode camera preview`）。

### 2026-08-27｜V2 派車人員名單移出功能

- 駕駛人員／派車管理員名單新增管理者「刪除」操作；依正式資料保護規則採軟停用（`active=false`），保留既有派車歷程，不再列入後續可指派名單，並可由管理者重新啟用。
- 後端 `vehicle_roster_update` 增加移出旗標、目標不存在時的錯誤回應與狀態變更稽核；不執行資料庫實體刪除。
- 驗證：`git diff --check`、資安設計檢查（錯誤 0／既有警告 4）、schema contract（314 組）通過；CI `33038362524`、Commercial readiness `33038362503`、CodeQL `33038362504`、Hardened Pages deployment `33038362495`（build／deploy／verify）與 Edge functions deployment `33038362521` 均成功。
- 程式提交：`6a5b5776`（`feat(v2): add vehicle roster removal`）。

### 2026-08-27｜V2 公文頁 100% 響應式版面與相機重試修正

- 100% 瀏覽器寬度下，頁首在中等寬度自動堆疊，摘要卡不再把整頁撐出水平捲軸；手機四張摘要卡縮減為 92% 版心寬度並置中，維持每張固定 60px 高度。
- 第一階單位藍色資訊列改為全寬，單位名稱改為可換行顯示，不再產生內嵌水平捲軸。
- 相機預覽補上 iOS `webkit-playsinline`、點擊預覽重新播放、重新啟動相機按鈕，以及裝置不接受解析度限制時的 `getUserMedia` 低限制備援；關閉與重試均會清理舊串流。
- 驗證：V2 `typecheck`、官方文件頁 esbuild、資安設計檢查錯誤 0／既有警告 4、schema contract 314 組與 `git diff --check` 通過；CI `33039783871`、Commercial readiness `33039783887`、CodeQL `33039783846`、Hardened Pages `33039783873`（build／deploy／verify）均成功。
- 程式提交：`5fd4d593`（`fix(v2): prevent official docs horizontal overflow`）。

### 2026-08-27｜V2 派車名單批次移出與停用項目篩選

- 修正單筆移出後仍顯示在名單造成的誤解：名單預設只顯示啟用人員，提供「顯示已停用」切換查看歷史項目並可重新啟用。
- 駕駛人員／派車管理員頁面新增管理者「全部刪除」批次操作；後端以一次更新將所有啟用項目設為停用並逐筆寫入狀態稽核，保留既有派車紀錄，不執行資料庫實體刪除。
- 驗證：資安設計檢查錯誤 0／既有警告 4、schema contract 314 組、`git diff --check` 通過；GitHub Actions 待本次提交後確認。

### 2026-08-27｜V2 派車申請工具列單列排列

- 將日期篩選與「清除篩選／派車報表／公務車主檔／新增派車申請」維持在同一個桌面工具列；四個按鈕包成固定不拆行的操作群組，不再因篩選欄位換行而被拆成兩列。
- 保留小螢幕響應式換行，避免窄版面產生水平溢出。
- 驗證：`git diff --check` 通過；待本次提交後確認 GitHub Actions 與 GitHub Pages 部署。

### 2026-08-27｜V2 公文頁第一階單位資訊列內距調整

- 桌面版「目前可查詢的第一階單位」資訊圖塊左右內距固定為 `34px`，保留全寬、可換行與手機版響應式內距。
- 驗證：`git diff --check` 通過；待本次提交後確認 GitHub Actions 與 GitHub Pages 部署。

### 2026-08-27｜V2 公文頁第一階單位單列顯示

- 桌面版第一階單位清單改為單列顯示，移除內容區水平捲軸；以 `clamp(8px, 1.2vw, 12px)` 響應式字級確保窄桌面仍不撐出整頁橫向捲動。
- 手機版維持原有可換行排版，避免小螢幕文字過度縮小。

### 2026-08-27｜V2 派車申請工具列控制項精簡

- 移除派車申請頁面不需要的文字搜尋欄位與日期旁日曆圖示，保留狀態／日期篩選本體，讓工具列更清楚且不再出現空白窄控制項。
- 「派車報表」與「公務車主檔」維持僅對派車管理員／系統管理員顯示，一般使用者不會看到管理控制項。
- 驗證：`git diff --check` 通過；待本次提交後確認 GitHub Actions 與 GitHub Pages 部署。

### 2026-08-27｜V2 公文頁區塊色線與手機標題排版

- 公文查詢、清單與建立區塊的上緣色線統一加入對應色彩的柔和發光效果，藍色、綠色、棕色與紅色層級一致。
- 手機版查詢與建立區塊標題改為單列，說明文字移至下一列並保留自然換行。
- 驗證：`git diff --check` 通過；待本次提交後確認 GitHub Actions 與 GitHub Pages 部署。

### 2026-08-27｜V2 派車逾期回報與里程卡控

- 派車申請頁加入逾期未出車／已接單未回報／已出車未補里程的紅黃警示；司機與申請人可在明細填寫取消原因後取消，避免用車日過後留下無紀錄案件。
- 車輛管理人看到需追蹤的行程會以顏色標示，並由受控的資料庫函式建立去重站內通知給司機、申請人與車輛管理人，通知內容要求補填取消原因或行車回報。
- 新增資料庫里程 trigger：完成行車回報必須有起始／回程里程；同一車輛若前一趟已出車或逾期接單但未補里程，禁止再次派車。正式資料仍不實體刪除。
- 驗證：`git diff --check`、資安設計檢查（錯誤 0／既有警告 4）、schema contract（314 組）與備份一致性檢查通過；migration run `33042946901`、CI run `33042936216`、Commercial readiness run `33042936188`、CodeQL run `33042936225`、Hardened Pages run `33042936197`（build／deploy／verify）均成功。
- 程式提交：`fc4ec77`（`feat(v2): add vehicle dispatch follow-up controls`）。

### 2026-08-27｜V2 公文頁文案與桌面敘述行

- 登入者摘要卡改以「第一階部門」呈現；流程節點、目前狀態與完整時間軸文案改為「流程」、「目前狀態」及「不可刪除的時間軸」。
- 桌面版頁首說明文字固定單列，並以響應式字級避免窄版面產生橫向捲動。
- 驗證：`git diff --check` 通過；待本次提交後確認 GitHub Actions 與 GitHub Pages 部署。

### 2026-08-27｜V2 公文頁上緣線與流程按鈕配色

- 01、02、03 區塊統一沿用 02 清單的實體色線，維持對應藍／綠／棕色上緣識別。
- 登入者摘要卡顯示文字簡化為「部門」；送出會辦採 80% 淺藍底白字，送出陳核採 80% 螢光綠底白字。
- 驗證：`git diff --check` 通過；待本次提交後確認 GitHub Actions 與 GitHub Pages 部署。

### 2026-08-27｜V2 派車申請手機版卡片排版

- 手機版派車申請頁的四步驟流程卡改為 2 欄 2 列，四張 KPI 統計卡改為單列四欄，縮減卡片內距與字級並允許文字換行，避免卡片超出內容版面。
- 桌面版維持原有四欄排列；樣式集中於 `vehicle-flow-grid` 與 `vehicle-kpi-grid`，不影響其他系統頁面。
- 驗證：`git diff --check`、資安設計檢查（錯誤 0／既有警告 4）與 schema contract（314 組）通過；CI run `33044489804`、Commercial readiness run `33044489872`、CodeQL run `33044489782`、Hardened Pages run `33044489779`（build／deploy／verify）均成功。
- 程式提交：`9850b3a`（`fix(v2): refine vehicle request mobile cards`）。

### 2026-08-27｜V2 公文頁上緣色線穩定修正

- 01 查詢、02 公文清單與 03 建立公文區塊改為直接綁定藍、綠、棕色 6px 實線上緣，避免父層選擇器或 CSS 載入順序使色線變成留白。
- 驗證：Stylelint 與 `git diff --check` 通過；本機瀏覽器計算樣式確認三區上緣分別為 `rgb(2, 132, 199)`、`rgb(5, 150, 105)`、`rgb(180, 83, 9)`。程式提交 `23ccbf3`；CI run `33044813951`、Commercial readiness run `33044813866`、CodeQL run `33044814012`與 Hardened Pages run `33044813941`（build／deploy／verify）均成功。

### 2026-08-27｜V2 公文陳核單位、手機後鏡頭與條碼按鈕修正

- 流程目標改為前後端雙重限制：董事長室、總經理室與副總經理室僅能陳核，秘書室可會辦也可陳核，其他第一階單位僅能會辦；後端會拒絕繞過畫面的無效動作。
- 手機掃描先強制請求後鏡頭；若瀏覽器不接受約束，授權後會枚舉鏡頭並優先選後置裝置，多鏡頭手機另顯示「切換鏡頭」。
- 新建文件原本已自動寫入條碼，不再因此將按鈕永久停用；有條碼時按鈕改為可直接查看／列印，舊資料缺條碼時才補產生後自動開啟。
- 驗證：`git diff --check`、前端 esbuild 語法編譯、Edge Function `deno check`、資安設計稽核（錯誤 0／既有警告 4）、祅密掃描與完整 `npm test` 均通過；手機鏡頭授權需部署後由實機確認。程式提交 `cf55c0b`，並以 `42a8246` 保留公文三色上緣；Edge functions run `33045410950`、CI run `33045442808`、Commercial readiness run `33045442813`、Hardened Pages run `33045442836`（build／deploy／verify）均成功，後續文件提交的 CodeQL run `33045555401` 亦成功。

### 2026-08-27｜V2 派車申請手機／平板篩選版型

- 「我的申請／今日派車／待我處理／派車總覽」改為窄版單列等寬排列，文字與計數自動縮放並截斷，避免手機及平板水平溢出。
- 手機／平板的用車日期篩選恢復日曆下拉按鈕，並保留 `LocalizedDateInput` 的繁中日期顯示。
- 驗證：`git diff --check`、資安設計檢查（錯誤 0／既有警告 4）與 schema contract（314 組）通過；CI run `33045555356`、Commercial readiness run `33045555313`、CodeQL run `33045555401`、Hardened Pages run `33045555326`（build／deploy／verify）均成功。
- 程式提交：`cf55c0b`（`fix(v2): harden official docs routing and scanning`，包含本次派車篩選版型修正）。

### 2026-08-27｜V2 公文三色上緣覆蓋修正與選填文號

- 確認共用 `.panel` 的邊框縮寫規則載入較晚，會覆蓋同層級的 01、03 色線；三區改以較明確的頁面／版面選擇器固定為 01 藍、02 綠、03 棕色 6px 上緣。
- 「建立公文／採購單／其他」最前方新增「文號（選填）」；有填時後端使用輸入文號並同步作為查詢條碼，留白才沿用民國年日期與四碼流水號自動取號。
- 後端拒絕超過 100 字元、控制字元及重複文號，避免資料與條碼不一致；成功訊息與建立按鈕會依手動文號／自動取號模式切換。
- 驗證：`git diff --check`、前端 esbuild 語法編譯、Edge Function `deno check`、資安設計稽核（錯誤 0／既有警告 4）、祕密掃描與完整 `npm test` 均通過；程式提交 `029b8a1`。CI run `33046567637`、Edge functions run `33046567625`、Commercial readiness run `33046567624`、CodeQL run `33046567639` 與 Hardened Pages run `33046567622`（build／deploy／verify）均成功；線上 CSS 資產已確認包含藍、綠、棕三色高層級上緣規則。

### 2026-08-27｜V2 公文頁「條碼」文案統一為「文號」

- 公文頁的查詢標題、輸入提示、相機掃描引導、彈窗、圖片替代文字、下載檔名、列印按鈕、狀態提示、詳細欄位及時間軸等所有可見「條碼」用語統一改為「文號」。
- 底層掃描、圖片產生、查詢值與既有資料欄位維持不變；Edge Function 的權限錯誤與建立事件文字同步更新，避免前後端文案混用。
- 驗證：`git diff --check`、前端 esbuild 語法編譯、Edge Function `deno check`、資安設計稽核（錯誤 0／既有警告 4）、祕密掃描與完整 `npm test` 均通過；程式提交 `d34ceda`。CI run `33060050416`、Edge functions run `33060050428`、Commercial readiness run `33060050424`、CodeQL run `33060050420` 與 Hardened Pages run `33060050440` 均成功。

### 2026-08-27｜V2 派車申請 100% 縮放單頁適配

- 以瀏覽器 100% 縮放、1617×678 CSS 可視區實測，原主頁高度 712px、明細彈窗內容高度 890px；主頁流程卡、KPI 卡、頁籤、工具列及表格間距改為緊湊尺寸，資料筆數較多時改由固定表頭的表格區域依視窗高度內部捲動，不再撐高整頁。
- 派車明細彈窗新增專用樣式，桌面版放寬至 860px、資料圖塊改三欄，並縮減標題、歷程、意見欄與頁尾高度；窄版維持兩欄並保留彈窗內部捲動保險。
- 「取消原因」改為必選選單，選項依序為空白、未出車、臨時取消、其他；未選原因前停用「取消申請」。
- 驗證：`git diff --check`、前端 esbuild 語法編譯、資安設計稽核（錯誤 0／既有警告 4）、祕密掃描與完整 `npm test` 均通過；部署後以同一個 100% 縮放、1617×677 視窗實測，明細彈窗由 890px 降為 494px、內部溢出 0，取消原因選項順序正確；主頁表格高度依視窗收斂為 243px，外頁 `scrollHeight`／可視高度同為 677px、溢出 0。程式提交 `6ef4eca`、`573cdc6`、`7460e3e`；CI run `33061630956`、Commercial readiness run `33061630963`、CodeQL run `33061630987` 與 Hardened Pages run `33061630968` 均成功。

### 2026-08-27｜V2 派車明細圖塊分色美編

- 派車明細視窗依待核可、已派車、已完成、已退回與已取消等案件狀態套用對應主色，並將主色延伸至標題列、頂緣、頁尾與操作表單，讓案件狀態更容易辨識。
- 每個資料圖塊加入青、紫、綠、橘的低彩度淡色底、左側識別色條與細緻邊框；流程歷程同步分色，保留一般版與科技版的文字對比及原有緊湊單頁尺寸。
- 驗證：`git diff --check`、前端 esbuild 語法編譯、資安設計稽核（錯誤 0／既有警告 4）、祕密掃描與完整 `npm test` 均通過；部署後以瀏覽器 100% 縮放、1455×657 視窗檢查已取消明細，13 個資料圖塊呈現 5 種識別色、5 筆流程呈現 4 種識別色，彈窗與外頁溢出均為 0。程式提交：`1399ae3`、`ab6458c`、`807557b`；Hardened Pages run `33062993905`（build／deploy／verify）成功。

### 2026-08-27｜V2 駐衛警巡檢打卡頁標題上移

- 巡檢打卡頁原本同時套用 AppShell 外層與頁面內層各 20px 上方留白；新增頁面專用樣式，只移除內層重複留白，不影響共用頂列與其他駐衛警模組。
- 瀏覽器 100% 縮放量測：修改前頂列底部 54px、標題頂端 96px，間距 42px；修改後頂列底部維持 54px、標題頂端 76px，實際間距縮短為 22px。
- 驗證：`git diff --check`、前端 esbuild 語法編譯、資安設計稽核（錯誤 0／既有警告 4）與完整 `npm test` 均通過；程式提交 `057f7a2`、`e1150ee`，Hardened Pages run `33064202847`（build／deploy／verify）成功。

### 2026-08-27｜V2 九大系統／49 子系統標題統一

- 新增共用 `SystemPageHeader`，一般內容頁由 `AppShell` 依路由與 `web/lib/modules.ts` 自動產生系統標題，統一涵蓋 9 大系統、49 個子系統；46 個一般頁使用標準頁首，3 個全螢幕圖資頁使用不遮擋圖面的緊湊頁首。
- 標準頁首固定為：標題文字頂端距共用頂列 22px、標題 26px、顏色 `var(--cyan)`、正式系統 Logo 42×42px；頁內重複的系統級標題已改為次級標題，避免同頁出現兩個競爭的 `<h1>`。
- 規格已寫入 `AGENTS.md` 與 `PROJECT_CONTEXT.md`，並新增 `npm run test:page-headings`，持續檢查 9／49 數量、Logo 資產、共用元件、CSS 數值及三個全螢幕例外。
- 驗證：`git diff --check`、前端 esbuild 語法編譯、標題覆蓋稽核、資安設計稽核（錯誤 0／既有警告 4）、祕密掃描與完整 `npm test` 均通過。部署後以瀏覽器 100% 縮放抽查 9 大系統代表頁，全部實測為頂列距離 22px、標題 26px、一般版標題色 `rgb(2, 132, 199)`、Logo 42×42px，且每頁只有一個系統級 `<h1>`；3 個全螢幕圖資頁均呈現對應 26×26px 緊湊 Logo。程式提交 `d1d99ac`、`66c1fa1`；CI run `33065315456`、Commercial readiness run `33065315452`、CodeQL run `33065315502` 與 Hardened Pages run `33065315508`（build／deploy／verify）均成功。
### 2026-08-27｜V2 駐衛警巡檢打卡工具列單列化

- 日期前後切換、今天、樓層、班別、打卡狀態、匯出起／迄日與兩個 XLSX 匯出按鈕在寬版面整合為同一列，方便一眼完成查詢與匯出操作。
- 窄版面於 1100px 以下自動改為可換行排列，並保留日期元件與匯出日期的繁中日期顯示及日曆操作，避免水平溢出。
- 驗證：`git diff --check`、資安設計稽核（錯誤 0／既有警告 4）與 schema contract（314 組）通過；CI run `33065869827`、Commercial readiness run `33065869808`、CodeQL run `33065869912` 與 Hardened Pages run `33065869871` 均成功。程式提交：`8bc6a95`；打卡頁可用 `?v=8bc6a95` 清除快取確認實機版面。

### 2026-08-27｜V2 交接紀錄首頁標題

- `/v2/systems/handover/` 直接呈現交接紀錄，因此套用共用 `SystemPageHeader` 並將大標題指定為「交接紀錄」，識別行顯示「SYS-04 · 電子交接簿」，沿用電子交接簿正式 Logo 與模組說明。
- 此首頁例外已寫入 `AGENTS.md` 與 `PROJECT_CONTEXT.md`；標題元件新增可選文字覆寫，但距離、字級、配色與 Logo 尺寸仍由同一份共用樣式控制。
- 驗證：前端 esbuild 語法編譯、完整 `npm test`、資安設計稽核（錯誤 0／既有警告 4）與祕密掃描通過；部署後以瀏覽器 100% 縮放實測，標題距頂列 22px、字級 26px、一般版顏色 `rgb(2, 132, 199)`、Logo 42×42px，且頁面只有一個系統級 `<h1>`。程式提交 `a3e384c`、`1a69359`；Hardened Pages run `33066371318`（build／deploy／verify）成功。

### 2026-08-27｜V2 Edge Function 限流暫時失敗修正

- 追查交接簿偶發顯示「安全限流服務暫時無法使用」的路徑，確認限流 RPC 遇到短暫網路／PostgREST／資料庫鎖定錯誤時原本只嘗試一次，便回傳 503；一般超額仍維持 429，不是放行限流。
- 共用 `security-monitor` 對可判定為暫時性基礎設施錯誤加入 100ms、300ms 的有限退避重試，仍失敗時保留 503 拒絕；`AuthGate` 對相同暫時錯誤自動重試最多兩次，並保留手動「重試」按鈕。
- 驗證：`npm test`（全部通過）、`git diff --check`、資安設計稽核（錯誤 0／既有警告 4）與 schema contract（314 組）通過；本機未安裝 Deno，Edge Function 型別檢查待 GitHub Actions 的部署工作流程執行。

### 2026-08-27｜V2 駐衛警入口圖卡與標題規格

- 駐衛警系統入口改用共用 `SystemPageHeader`，標題「駐衛警巡檢系統」與說明「巡邏點、打卡、排班、逾時通知與立體巡檢。」直接取系統定義，避免入口頁另寫一套標題尺寸與間距。
- 四張功能圖卡在桌面縮為原設計的 80% 並置中；瀏覽器縮放或視窗變窄造成可用 CSS 寬度下降時，版面依 1100px／600px 斷點自動由 4 欄切成 2 欄／1 欄並恢復滿寬，避免水平溢出。
- 標題水平規格補入專案設計：桌面內容左右內距 24px、Logo 42px、Logo 與文字間距 14px，因此一般滿寬桌面的標題文字左緣為 80px；手機則為 70px。
- 驗證：前端 esbuild 語法編譯、完整 `npm test`、資安設計稽核（錯誤 0／既有警告 4）與祕密掃描通過；部署後以瀏覽器 100% 縮放、1455×657 CSS 視窗實測，標題距頂列 22px、26px、`rgb(2, 132, 199)`、Logo 左緣 24px、標題左緣 80px、圖卡區寬度為內容區 80%、四張圖卡各約 269×239px，頁面水平溢出 0。程式提交 `b7cb211`；Hardened Pages run `33067431892`（build／deploy／verify）成功。

### 2026-08-27｜V2 駐衛警入口圖卡固定尺寸

- 駐衛警入口桌面四張功能圖卡改為瀏覽器 100% 時固定 269×200px，四欄置中排列；1100px 以下自動切為兩欄、600px 以下切為單欄並恢復彈性寬度。
- 專案版型檢查同步驗證桌面欄寬、卡片寬高與窄版斷點，避免後續修改退回相對尺寸或造成水平溢出。
- 驗證：`npm run test:page-headings`、完整 `npm test` 與 `git diff --check` 通過；部署後以瀏覽器 100%、1455×657 CSS 視窗實測，四張圖卡皆為 269×200px，四欄水平排列，頁面水平／垂直溢出皆為 0。程式提交 `b439c84`；Hardened Pages run `33069213245`（build／deploy／verify）、CI run `33069213233`、Commercial readiness run `33069213306` 與 CodeQL run `33069213194` 均成功。

### 2026-08-27｜V2 49 個子系統圖卡尺寸統一

- 一般 `.module-grid` 以及交接／駐衛警入口的子系統圖卡，桌面瀏覽器 100% 統一為 269×200px；1100px 以下改兩欄、600px 以下改單欄並恢復彈性寬度。
- 同步固定共用標題設計：距頂列 22px、字級 26px、淺藍 `rgb(2, 132, 199)`、Logo 左側 24px、標題文字左側 80px、Logo 42×42px。
- 驗證：`npm run test:page-headings`、完整 `npm test` 與 `git diff --check` 通過；49 個子系統數量檢查通過，部署後由 CI／Hardened Pages 驗證。

### 2026-08-27｜V2 維修／派工入口圖卡比例

- 7 張統計小卡改為桌面單列的 80% 比例版型；3 張流程主圖卡固定為 269×200px，900px 以下兩欄、600px 以下單欄。
- 驗證：`npm run test:page-headings`、完整 `npm test` 與 `git diff --check` 通過；固定尺寸與響應式斷點已加入防回歸檢查。

### 2026-08-27｜V2 登入白色圖卡 80% 與單頁適配

- 桌面登入頁將共用 430px 白色圖卡完整縮放為 80%，Logo、文字、欄位、驗證碼、按鈕與內距同步縮放，帳號申請表不套用此規則。
- 登入頁桌面版固定使用 `100svh` 並隱藏外頁溢出，確保瀏覽器 100% 時不出現水平或垂直捲軸。
- 驗證：完整 `npm test`、資安設計稽核（錯誤 0／既有警告 4）及祕密掃描均通過；部署後以瀏覽器 100%、1280×720 CSS 視窗實測，圖卡為 344×519px（430px × 80%），文件與 `body` 的水平／垂直溢出皆為 0。程式提交 `f2f7c66`；Hardened Pages run `33068414436`（build／deploy／verify）成功。
### 2026-08-27｜V2 九大系統／49 子系統頂列精簡與 ICON 統一

- 一般內容頁共用頂列統一只保留「首頁／個人資料／登出」；移除戰情儀表板、維修／派完工、駐衛警巡檢、電子交接簿與後台等跨系統按鈕，系統切換改由入口頁、系統頁與後台側欄承擔。
- 使用生圖工具製作同風格透明 ICON：`assets/system-icons/home-nav-icon.png` 與 `assets/system-icons/profile-nav-icon.png`，並縮為 128×128 供頂列 18px 顯示；三個全螢幕圖資工具同步移除舊跨系統導覽，只保留必要的首頁與圖資操作連結。
- 驗證：`npm test`（全部通過）、資安設計稽核（錯誤 0／既有警告 4）、schema contract（314 組）與 `git diff --check` 通過；程式提交 `d59454d` 並推送 `origin/main`。CI run `33072473468`、Commercial readiness run `33072473364`、CodeQL run `33072473302` 與 Hardened Pages deployment run `33072473477` 均成功。

### 2026-08-27｜V2 頂列帳號控制靠右排序

- 共用 `AppShell` 將「首頁／個人資料／登出」收斂為同一組靠右控制，固定順序為首頁、個人資料、登出；首頁與個人資料保留同風格 ICON，登出維持純文字按鈕。
- 驗證：`npm test`、資安設計稽核（錯誤 0／既有警告 4）、schema contract（314 組）與 `git diff-tree --check` 通過；程式提交 `fe0d982`。CI run `33073760194`、Commercial readiness run `33073760192`、CodeQL run `33073760220` 與 Hardened Pages deployment run `33073760203` 均成功；部署後正式網址回應 HTTP 200。

### 2026-08-27｜V2 登出 ICON 統一

- 依首頁與個人資料的共用頂列風格，以生圖工具製作透明背景的藍色立體開門／右箭頭 ICON，加入 `assets/system-icons/logout-nav-icon.png`（128×128 RGBA）。
- 共用 `AppShell` 的登出按鈕改為顯示新 ICON 與「登出」文字；首頁／個人資料／登出仍維持靠右及固定順序，套用至所有一般內容子頁。
- 驗證：`npm test`、資安設計稽核（錯誤 0／既有警告 4）、schema contract（314 組）、`git diff --check` 與頂列順序／ICON 資產檢查均通過；GitHub Actions 的 CI run `33074568453`、Commercial readiness run `33074568481`、CodeQL run `33074568450` 與 Hardened Pages deployment run `33074568446` 均成功；部署後頁面回應 HTTP 200，登出 ICON 資產回應 HTTP 200（14,900 bytes）。程式提交 `4eb6451` 並推送 `origin/main`。

### 2026-08-27｜V2 駐衛警巡檢打卡工具列真正單列化

- 修正前一版僅靠兩個 inline-flex 列容器排列、寬度不足時仍會換行的問題；在 `operations-workspace.tsx` 新增共用 `operations-tool-row`，將日期、篩選與 XLSX 匯出控制項納入同一個 Flex 工具列。
- 寬版面固定同列；901–1100px 縮小控制項，900px 以下才換行，避免桌面／平板橫向視窗仍出現兩列，同時保留手機可用性。
- 驗證：完整 `npm test`、資安設計稽核（錯誤 0／既有警告 4）、schema contract（314 組）與 `git diff --check` 通過；程式提交 `a41da087` 並推送 `origin/main`。

### 2026-08-27｜V2 駐衛警巡檢打卡工具列單列修正

- 調整 `/v2/systems/guardpatrol/checkins/` 的日期、樓層、班別、打卡狀態、匯出起／迄日與兩個 XLSX 匯出控制項：901px 以上維持同一列，901–1100px 自動縮小控制項以避免拆列。
- 900px 以下才進入響應式換行，保留手機與窄版操作空間並避免水平溢出。
- 驗證：完整 `npm test`、資安設計稽核（錯誤 0／既有警告 4）與 `git diff --check` 通過；GitHub Actions 的 CI run `33075675334`、Commercial readiness run `33075675224`、CodeQL run `33075675249` 與 Hardened Pages deployment run `33075675210` 均成功；版本網址回應 HTTP 200，部署 CSS 已包含 `901px` 單列與 `900px` 以下響應式斷點。程式提交 `34a17c59` 並推送 `origin/main`。

### 2026-08-27｜V2 整合管理入口圖卡縮小 88%

- `/v2/systems/` 九大系統入口圖卡以原桌面 300px 寬、250px 高基準縮為 264px × 220px（88%），圖示、字級、內距與間距同步收斂，維持四欄置中排列。
- 1200px 以下改為兩欄、761px 以下仍保留兩欄但使用固定 264px 欄寬；760px 以下沿用手機滿寬自動版型，避免任何水平溢出。
- 驗證：完整 `npm test`、`git diff --check`、資安設計稽核（錯誤 0／既有警告 4）與 88% CSS 靜態防回歸檢查通過；程式提交 `4b88f202` 並推送 `origin/main`。最新主分支 `a90bfee1` 的 CI run `33081448317`、Commercial readiness run `33081448310`、CodeQL run `33081448293` 與 Hardened Pages deployment run `33081448303` 均成功；版本網址回應 HTTP 200，部署 CSS 已確認含 264px × 220px 圖卡與四欄規則。

### 2026-08-27｜V2 駐衛警逾時推播白色圖卡 85% 寬度

- `/v2/systems/guardpatrol/notifications/` 的白色逾時推播內容卡套用既有 `.patrol-notifications-panel` 響應式樣式：桌面瀏覽器 100% 時使用內容區 85% 寬度並水平置中，900px 以下恢復滿寬，避免手機／窄版水平溢出。
- 驗證：完整 `npm test`、`git diff --check`、資安設計稽核（錯誤 0／既有警告 4）與圖卡寬度靜態檢查通過；程式提交 `17257785`、紀錄提交 `88a93f67` 並推送 `origin/main`。Hardened Pages deployment run `33079504871` 的 build／deploy／verify-deployment 均成功；版本網址回應 HTTP 200，部署 CSS 已確認包含 `.patrol-notifications-panel{width:85%}`。

### 2026-08-27｜V2 個人資料 01／02／03 圖塊美編

- 共用個人資料彈窗的「01 基本資料」、「02 Google 個人行事曆」與「03 登入安全」加入專屬識別色、左側色帶、編號徽章、標題底線與柔和陰影，欄位聚焦、連結狀態及操作按鈕同步呼應區塊色彩。
- 維持既有欄位、儲存、Google 連結、密碼更新與手機版單欄功能不變；樣式全部使用主題變數與 `color-mix`，兼容一般版與科技版。
- 驗證：完整 `npm test`、`npm run security:audit`、`git diff --check` 與 01／02／03 CSS 靜態檢查通過；本機型別檢查因工作區未安裝 `tsc` 無法執行，將由 GitHub Pages 建置流程驗證。程式提交 `0f2c22b3` 並推送 `origin/main`。

### 2026-08-27｜V2 九大系統／49 個子系統按鈕規格統一

- 新增 `web/app/button-standard.css` 並由 `globals.css` 全站載入，將主要、一般、危險與緊湊按鈕統一為同一套幾何規格：36px 基準高度、8px 圓角、8×13px 內距；`.compact` 為 32px，手機主要按鈕提升至 40px。
- 主要操作採巡檢「完成打卡」按鈕的青色線框／淡色底，危險操作採紅色語意色；hover、停用、鍵盤焦點與一般版／科技版主題均由共用規格控制。既有 `.btn`、`.btn-primary`、`.btn-danger` 及圖臺／表格／匯出控制沿用一致幾何，保留必要功能色與特殊布局。
- `PROJECT_CONTEXT.md` 與 `AGENTS.md` 已補上後續新增相同按鈕的使用規則；新增 `tools/v2-button-standard-check.mjs` 並接入 `npm test`，固定檢查 9 大系統／49 個子系統與按鈕 token，避免未來分叉。
- 驗證：`npm run test:button-standard`、完整 `npm test`、`git diff --check` 通過；本機因未安裝 `tsc`／Next CLI，型別檢查與 Next 建置由 CI 執行。GitHub Actions 的 CI run `33083627806`、Commercial readiness run `33083627772`、CodeQL run `33083628231` 與 Hardened Pages deployment run `33083627915`（build／deploy／verify-deployment）均成功。程式提交 `8d02c3e7` 已推送 `origin/main`。

### 2026-08-27｜V2 按鈕規格載入順序收斂

- 共用按鈕 CSS 改由根版面 `layout.tsx` 在既有全域／交接樣式之後載入，並移除後台與天氣控制對 `.primary-btn`／`.secondary-btn` 的局部尺寸覆寫，確保 49 個子系統實際取得同一套規格。
- 驗證：完整 `npm test`、`git diff --check` 與按鈕規格檢查通過；GitHub Actions 的 CI run `33084207970`、Commercial readiness run `33084207972`、CodeQL run `33084207969` 與 Hardened Pages deployment run `33084207991`（build／deploy／verify-deployment）均成功。程式提交 `b461d384` 已推送 `origin/main`。

### 2026-08-27｜V2 標準頁首代碼列與說明列合併為一列

- `web/components/SystemPageHeader.tsx` 將「SYS-xx · 子系統名」與子系統說明包進新的 `.system-page-heading-meta`，`web/app/v1-layout.css` 以 `display:flex;align-items:baseline;gap:2px 10px` 讓兩段文字在同一列以基線對齊；`span` 加 `white-space:nowrap` 避免代碼被拆行，容器保留 `flex-wrap`，窄螢幕（760px 以下）自動回復為兩列，不產生水平溢出。
- 標準頁首為單一共用元件，本次調整同時涵蓋 46 個標準頁首（含 SYS-02 報修案件／派工作業／維修工單、SYS-03 巡邏打卡等）；3 個全螢幕緊湊頁首（`floor2d`／`floor3d`／`map3d`）使用 `f3-topbar`，不受影響。
- 驗證：完整 `npm test`（含 `test:page-headings` 46 個標準頁首、`test:button-standard`）、`npm run security:audit`（錯誤 0／既有警告 4）、`git diff --check` 通過；以相同 CSS 規則的靜態重現頁在 1280px 與 375px 實測，桌面為單列、375px 自動換行且 `document.scrollWidth` 等於 375（無水平溢出）。本機未安裝 `tsc`／Next CLI，型別檢查與 Next 建置由 CI 執行。程式提交 `54ab1fef` 並推送 `origin/main`。

### 2026-08-27｜V2 個人資料彈窗 100% 縮放內容可見

- 修正共用個人資料彈窗在瀏覽器 100% 或可視高度較矮時，CSS Grid 會將三個資料區塊壓縮成只有 01／02／03 標題的問題；內容區改為可收縮的 flex 子區塊、以 `max-content` 保留卡片高度，背景與內容層皆可在彈窗內捲動。
- 套用至公文傳送系統及所有使用共用 `AppShell` 的 V2 子頁；新增 `test:profile-modal` 防回歸檢查，確認根版面載入響應式樣式與區塊內容高度規則。
- 驗證：完整 `npm test`（包含個人資料彈窗檢查）、`git diff --check` 通過；本機 Next CLI 未安裝，正式建置由 GitHub Actions 驗證。程式提交 `0f488c80` 並推送 `origin/main`；CI run `33085709942`、Commercial readiness run `33085709359`、CodeQL run `33085709954` 與 Hardened Pages deployment run `33085709389`（build／deploy／verify-deployment）均成功。

### 2026-08-27｜V2 公文掃描器：沒有影像時不再謊稱「鏡頭已開啟」

- 現場回報 `/v2/systems/officialdocs/routing/` 在 iPhone 上點「開啟相機掃描」後，提示顯示「鏡頭已開啟」但預覽框全灰、完全沒有影像。查證截圖版本確認為最新版（含「切換鏡頭／重新啟動相機」），非舊快取。
- 根因分類：`getUserMedia` 成功、`decodeFromStream` 回傳成功，程式就直接顯示「鏡頭已開啟」，**從未確認真的有影格**。iOS 主畫面捷徑（standalone）與 App 內建瀏覽器（LINE／Facebook）會給出串流但不送影格，於是畫面把故障偽裝成正常——與 2026-08-21 記錄的「靜默失效」同一類。
- 修正：新增 `waitForFirstFrame()`，等到 `videoWidth`／`videoHeight` > 0 且 `readyState >= 2` 且未暫停才顯示成功；4 秒逾時則顯示「相機已授權但沒有影像」與改用 Safari／Chrome 的指引，並把 `readyState`、`videoWidth`、track 狀態、`display-mode: standalone`、UA 記入 console。
- 同時把 `decodeFromStream` 改為 `decodeFromVideoElement`：串流已由元件自行掛上並 `play()`，`decodeFromStream` 會透過 `attachStreamToVideo` 再指派一次同一個 `srcObject`，iOS 上有讓畫面停在載入中的風險，改用後 ZXing 只負責讀影格、不再碰 `srcObject`。
- 驗證：完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）、`git diff --check` 通過；本機未安裝 `tsc`／Next CLI，型別檢查與建置由 CI 執行。**尚待使用者實機複測**（此次修正的重點是讓失敗時說實話，真正的根因需要現場回報的 console 內容才能確認）。程式提交 `0b1bf220` 並推送 `origin/main`。

### 2026-08-28｜V2 公文條碼：影像已修復，補強辨識率並新增拍照辨識

- 前一筆的影格驗證修正上線後，現場回報**預覽畫面已經正常**（`decodeFromVideoElement` 不再重複指派 `srcObject`），但對著實體公文的 Code 39 條碼**讀不出來**。
- 判斷：紙本一維條碼的窄條寬度只有幾個像素，即時影像同時受對焦與手震影響，是辨識失敗的主因，不是格式支援問題（`CODE_39` 本來就在 `POSSIBLE_FORMATS` 內，系統自己產生的條碼也是 Code 39）。
- 三項補強：
  1. 相機解析度由 `1280x720` 提高到 `1920x1080`，讓每根窄條多幾個像素。
  2. 取得串流後嘗試 `applyConstraints({ advanced: [{ focusMode: 'continuous' }] })`；Safari 不支援時只記 `console.info`，不影響掃描。
  3. 新增「拍照辨識」按鈕（`<input type="file" accept="image/*" capture="environment">`），交給系統相機拍一張對焦完成的高解析靜態影像，再用同一組設定 `decodeFromImageUrl()` 解碼。這條路避開對焦與手震，是紙本條碼最可靠的方式。
- 即時掃描與拍照辨識改用共用的 `createOneDReader()`，格式設定只保留一份。
- 驗證：**工作區已搬到 `C:\claude-code\Inspection`，本機終於能跑完整工具鏈**——`npm run typecheck:v2`（初次執行抓到兩個型別錯誤：`ChangeEvent` 未匯入、`focusMode` 不在 `MediaTrackConstraintSet`，均已修正）、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）、`git diff --check` 全數通過。程式提交 `64c3fee` 並推送 `origin/main`。尚待實機複測辨識率。

### 2026-08-28｜V2 共用按鈕互動色統一

- 個人資料設定的「連結 Google 帳號」及 49 個子系統共用按鈕，未選取時統一白底深色字；滑過、聚焦、按下或選取時改為淺藍底綠字。危險操作仍保留紅色語意色。
- Google 行事曆已連結狀態新增 `is-connected` 視覺標記，並同步調整儲存個人資料／更新密碼按鈕，避免文字被底色遮蓋。
- 驗證：`npm run test:button-standard`、`npm run test:page-headings`、`git diff --check` 通過。

### 2026-08-28｜V2 完工回報表單：欄位改選單、手機版一列兩欄

- 現場回報完工回報表單不好填。三項修正：
  1. **更換零件**由自由輸入改為下拉選單，第一列空白，選項為「是／否」。
  2. **工時（小時）**改為下拉選單，以 1 小時為單位、上限 24 小時（跨日案件請分次回報）。原本是 `type="number"` 且 step 為 0.5。
  3. **手機版（≤640px）**把「更換零件、使用材料、工時、零件費用、工資費用」五個短欄位改成一列兩欄；故障原因、處理方式、完工備註維持整列。作法是給短欄位加 `.half`，手機版斷點改為兩欄網格並讓一般 label 預設 `grid-column:1/-1`。**桌面版完全不變**。
- 注意：`parts_used` 欄位語意由「零件清單」改為「是／否」，維修結果摘要會顯示「更換：是」。需要記錄實際零件品項時請填在「使用材料」。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）、`git diff --check` 通過；並以實際 CSS 建立靜態重現頁量測版面——375px 下「更換零件＋使用材料」同列（y=289，各 153px）、「工時＋零件費用」同列（y=369）、三個長欄位維持 318px 整列、`document.scrollWidth` 等於 375 無水平溢出；1280px 下八個欄位維持原本兩欄各 605px，與修改前一致。程式提交 `22197c9`（rebase 到 `4e6a990` 之後）並推送 `origin/main`。
### 2026-08-28｜公文建立按鈕套用共用互動色

- 修正公文傳送 routing 頁「建立並自動取號／依輸入文號建立」被頁面專用 CSS 蓋過共用規格的問題，恢復白底深色字與互動時淺藍底綠字。
- 驗證：`npm run test:button-standard`、`npm run test:page-headings`、`git diff --check` 通過。

### 2026-08-28｜派車申請列表每頁 10 筆免內層捲軸

- 派車申請列表維持每頁 10 筆，移除管理版型造成的內部高度限制；桌面表格直接展開並固定欄寬，窄版仍自動卡片化，避免水平溢出。
- 驗證：`npm run test:button-standard`、`npm run test:page-headings`、`git diff --check` 通過。

### 2026-08-28｜維修／派工／完工統計卡手機版四欄

- 僅在 600px 以下手機版將 7 張統計圖卡改為每列 4 張（4＋3），縮小內距與文字以維持可讀性；桌面版與平板版維持原配置。
- 驗證：`npm run test:button-standard`、`npm run test:page-headings`、`git diff --check` 通過。

### 2026-08-28｜手機統計圖卡高度調整為 48px

- 依最新規格將手機版 7 張統計圖卡高度由 63px 調整為 48px；四欄排列、6px 間距與桌面版 133×63px 維持不變。

### 2026-08-28｜報修子網頁手機表頭單列化

- 僅在手機版將「報修案件」標題與「新增報修」按鈕固定同列，搜尋欄移至下一列並縮小按鈕寬度，避免標題換行與版面溢出。

### 2026-08-28｜報修紀錄手機卡片完整顯示

- 修正報修子網頁手機卡片沿用桌面 `nowrap/ellipsis` 導致欄位值被截斷或不顯示；手機版改為完整顯示、可換行並隱藏水平溢出，桌面版維持原表格格式。

### 2026-08-28｜SYS-02 系統名稱統一為報修／派工／完工

- 將 9 大系統／49 子系統共用定義、工作台標題、後台側欄與權限標籤的「維修／派工／完工」統一修正為「報修／派工／完工」。
- 驗證：`npm run test:button-standard`、`npm run test:page-headings`、`git diff --check` 通過。

### 2026-08-28｜V2 巡檢排班手機版：頁首按鈕靠右、日期切換排成一列

- 現場回報 `/v2/systems/guardpatrol/shifts/` 手機版兩處版面問題，**只調整手機版（≤800px），桌面版維持原樣**：
  1. 頁首的「＋新增當日班別／重新載入」原本靠左，改為靠右對齊（`.admin-page-actions:has(+ .patrol-shift-shell)>div:last-child{width:100%;justify-content:flex-end}`，以相鄰選擇器限定只影響這一頁）。
  2. 「◀前一天／日期／後一天▶／今天」原本會斷成兩三列，改為同一列，「值班日 …」說明另起一行。
- 關鍵細節：日期欄位的 `flex-basis` 必須收到 **72px**。原本設 `flex:1 1 auto` 時，basis 取內容寬度（190px）會先佔滿整行，把「今天」擠到第二列；改成 72px 後四個控制項先排進同一行，再由 `flex-grow` 把日期撐開。
- 原本 `patrol-workspace.tsx` 寫死的 inline style（`maxWidth:'85%'`）改為 `.patrol-shift-shell`，桌面仍是 85% 置中，手機版放寬到滿版，否則四個控制項排不下。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；並用 **Next 建置產出的實際 CSS chunk** 建立靜態重現頁量測——375px：四個控制項同列（y=68～71，日期 138px），「值班日」另起一行，`scrollWidth` 375 無溢出；320px：仍同列，右緣 306 未超出；1280px：外框 1088px（=85%）、日期 210px、五個元素同列，與修改前一致。程式提交 `71e7911` 並推送 `origin/main`。

### 2026-08-28｜V2 巡邏打卡手機版：工具列收斂、匯出功能隱藏

- 現場回報 `/v2/systems/guardpatrol/checkins/` 手機版工具列被拆成五列。**只調整手機版（≤640px），桌面版維持原樣**：
  1. 「當班即時統計」字樣隱藏，只留「早班 08:00–16:00」。作法是把文字包進 `<span className='stat-caption'>` 再於手機版 `display:none`——直接對外層 span 動手會連數值一起消失。
  2. 樓層／班別／打卡狀態三個篩選改為等寬（`flex:1 1 0`）排成同一列，日期切換（◀／日期／▶／今天）自成一列。
  3. 匯出起日、匯出迄日與「匯出值班日 XLSX」「匯出期間 XLSX」在手機上隱藏，匯出改用電腦版。
- 關鍵細節：`.operations-tool-row` 的兩個子容器在寬版面是 `display:contents`（刻意把日期列與篩選列攤平成同一列，避免總寬不足時被瀏覽器拆開）。手機版要分成上下兩列，**必須先把 `display:contents` 改回 `display:flex`**，否則所有控制項都是同一個 flex 容器的直接子項，無法分列控制。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；並用 Next 建置產出的實際 CSS chunk 建立靜態重現頁量測——375px：日期列 y=131 四個控制項同列、篩選列 y=169 三個各 97px 同列、四個匯出控制項 `display:none`、`.stat-caption` `display:none`、`scrollWidth` 375 無溢出；1280px：11 個控制項全部同列、`.stat-caption` 為 `inline`、匯出按鈕正常顯示，與修改前一致。程式提交 `0b72787` 並推送 `origin/main`。

### 2026-08-28｜V2 巡邏打卡：「當班即時統計」字樣桌面版一併移除

- 前一筆只在手機版隱藏「當班即時統計」，使用者確認桌面版也不要，只保留班別與時段（例：早班 08:00–16:00）。
- 既然兩邊都不顯示，直接把文字從 `operations-workspace.tsx` 移除，並刪掉上一筆新增的 `.stat-caption` 手機版規則與那層 `<span>`，不留「只為隱藏而存在」的標記與樣式。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；並確認建置產物的 JS chunk 已不含「當班即時統計」字串。程式提交 `2b1ef2f` 並推送 `origin/main`。

### 2026-08-28｜V2 逾時推播手機版：頁首按鈕靠右、篩選排成一列

- 現場回報 `/v2/systems/guardpatrol/notifications/` 手機版三個篩選各佔一列。**只調整手機版（≤800px），桌面版維持原樣**：
  1. 頁首「重新載入」靠右對齊（`.admin-page-actions:has(+ .patrol-notifications-panel)`，以相鄰選擇器限定只影響這一頁）。
  2. 最近 30 天／全部班別／全部狀態三個 `select` 改為等寬（`flex:1 1 0`）排成一列。
  3. 「共 N 筆｜未打卡 N 點次」以 `flex:1 0 100%` 另起一行。
- 本次不需要改 TSX，`.patrol-notifications-panel` 這個 class 早就存在（桌面 85% 寬那條規則），直接往下掛即可。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；靜態重現頁量測（375px）：重新載入右緣 361 與內容區切齊、三個 select 同列各 98px、統計文字獨立一行 y=186、`scrollWidth` 375 無溢出；1280px：四個元素同列、面板 1047px（85%），與修改前一致。
- **驗證方法的教訓**：第一次量測時「重新載入」沒靠右、統計文字也沒換行，一度以為規則沒生效。實際原因是重現頁只載入了含新規則的那一個 CSS chunk，缺少定義 `.admin-toolbar{display:flex}`／`.admin-page-actions>div:last-child{display:flex}` 的基礎 chunk——**沒有 flex 容器，`justify-content` 與 `flex-basis` 全部無效**。之後建重現頁務必把基礎樣式 chunk 一起載入。
- 程式提交 `062a5e1` 並推送 `origin/main`。

### 2026-08-28｜V2 巡檢排班手機版：班別範本三顆操作鈕排成一列

- 現場回報 `/v2/systems/guardpatrol/shifts/` 手機版班別範本的「套用到期間／編輯範本／刪除」被拆成兩行。**只調整手機版（≤800px），桌面版不受影響**（桌面是一般表格，卡片版規則不生效）。
- 原因：手機版 `responsive-table` 會把每列轉成卡片，`td` 是 `grid-template-columns:minmax(72px,32%) 1fr`，操作欄實測只有約 189px，三顆按鈕（含內距）約 233px 排不下。
- 作法：`.admin-row-actions{flex-wrap:nowrap}`、按鈕內距由 `6px 9px` 收到 `5px 6px`，並把欄位名那一欄由 32% 收到 26%，操作欄拿回約 22px 餘裕。字級沿用既有的 11px，沒有再縮。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；靜態重現頁量測——375px：三顆同列（y=182），合計 174px、容器 196px（餘裕 22px），`scrollWidth` 375 無溢出；320px：仍同列、剛好排滿；1280px：`td` 為 `table-cell`、按鈕內距維持 `6px 9px`，與修改前一致。程式提交 `d315916` 並推送 `origin/main`。

### 2026-08-28｜維修入口統計卡：190×90 孤兒 commit 已被後續決定取代（未套用）

- 準備 cherry-pick `backup/workorder-190x90-20260827`（`601ac30d`，08-27 20:36，統計卡 190×90）時查證發現：**同日 20:53 的 `76e8e39` 已把同一組卡片改為 133×63 並推上線**，也就是 190×90 是被後來的決定取代的中間狀態，而非「還沒套用的最新想法」。先前交接說明寫的「線上是 108×52」也是錯的。
- 另外 `repeat(7,190px)` 為固定寬度，七欄合計 1390px；`.content` 桌面可用寬約 1232px（1280 筆電），套用後在 901–1390px 之間會產生水平捲軸，既有的 900px 斷點接不住。
- 依上述兩點，暫不套用，等使用者確認是否仍要從 133×63 改回 190×90（若要，欄寬應改為 `minmax(0,190px)` 讓空間不足時等比收窄，並同步更新 `tools/system-page-heading-check.mjs` 內釘住尺寸的斷言）。備份分支保留。
### 2026-08-28｜3D／平面圖資頁共用頂列導覽

- 3D 模型圖與平面樓層圖全螢幕頁統一補上右側「上頁、首頁、個人資料、登出」動作，並提供平面圖／3D 圖切換入口；手機版收斂按鈕間距避免溢出。
- 個人資料入口改以共用 AppShell 開啟個人設定視窗，登出沿用 Supabase 登出與快取清除流程。
- 驗證：`npm run test:page-headings`、`git diff --check` 通過。

### 2026-08-28｜V2 派車申請手機版：工具列分成兩列

- 現場回報 `/v2/systems/vehicle/requests/` 手機版工具列排列凌亂。**只調整手機版（≤800px），桌面版仍是同一列**：
  - 第一列：全部狀態／年月日／清除篩選
  - 第二列：派車報表／公務車主檔／新增派車申請（三顆等寬）
- 結構調整：「清除篩選」原本在 `.vehicle-request-actions` 內，與下面三顆綁在一起無法拆列，改為 `.admin-toolbar` 的直接子項並加上 `.vehicle-request-clear`。桌面順序與外觀不變，但**必須補回 `white-space:nowrap`**——它離開那層之後就沒有該容器的 nowrap，桌面窄寬度下「清除篩選」會被拆成兩行、按鈕高度變成 53px 與其他 40px 不齊（實測抓到，已修）。
- 六個控制項統一 `36px` 高、`12px` 字，兩列才會對齊；選擇器要寫到兩層（`.vehicle-request-toolbar .vehicle-request-actions>button`）才蓋得過 `button-standard.css` 的 `.primary-btn.compact`（0,2,0）。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；靜態重現頁量測——375px：第一列 y=35（121/104/65px）、第二列 y=79（98/98/97px 等寬），六者高度皆 36px，右緣 341 切齊，`scrollWidth` 375 無溢出；320px：仍是兩列、右緣 285 無溢出；1280px：六者同一列、清除篩選回到 40px 高與其他按鈕一致。程式提交 `2583c61` 並推送 `origin/main`。
- **踩坑**：用 Python 字串切片改 CSS 時 `old_end` 算錯，把下一個 `@media (max-width:600px){` 的開頭吃掉，變成 `}-width:600px){`，Next 建置直接失敗（`Invalid token in pseudo element`）。改大段 CSS 時要用唯一錨點字串取代，不要用索引切片。
### 2026-08-28｜圖資頂列切換按鈕順序

- 將「平面圖切換」移至「上頁」前方，3D 模型圖與平面樓層圖共用相同順序。
- 平面樓層圖頁顯示「切換3D圖」、3D 頁顯示「切換平面圖」，兩個按鈕互相導向。

### 2026-08-28｜V2 公務車主檔手機版：頁首按鈕與台數統計靠右

- 現場回報 `/v2/systems/vehicle/vehicles/` 手機版。**只調整手機版（≤800px），桌面版不變**：
  1. 頁首「＋新增車輛／重新載入」靠右對齊（`.admin-page-actions:has(+ .vehicle-master-panel)`）。
  2. 搜尋框與「可派用 N／共 N 台」排成一列，統計靠右。
- 關鍵細節：`.admin-toolbar>input` 的基礎規則是 `min-width:min(420px,100%)`，手機上等同 100%，搜尋框會獨佔整列把統計擠到下一行。手機版改成 `flex:1 1 0` 加 `min-width:0` 才排得進同一列。
- 新增 `.vehicle-master-panel`／`.vehicle-master-toolbar` 兩個標記 class，讓規則只作用於這一頁。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；靜態重現頁量測——375px：頁首兩鈕同列右緣 361 切齊、搜尋框 205px 與統計同列（統計右緣 341）、`scrollWidth` 375 無溢出；320px：同樣兩列、無溢出；1280px：新增車輛仍在右上（x=1077）、搜尋框 1071px、統計靠右，與修改前一致。程式提交 `1b3d36e` 並推送 `origin/main`。
- 整合標記系統同步套用相同頂列與「切換3D圖」入口。
- 立體巡檢雲臺同步使用互相切換的「切換平面圖」入口。

### 2026-08-28｜駐衛警平面圖巡檢點篩選

- `floor2d?kind=patrol` 延續 3D 建模系統的 `floor_models` 平面圖來源，並將標記面板限制為只顯示巡檢點。
- 驗證：`npm run test:page-headings`、`git diff --check` 通過。

### 2026-08-28｜巡檢點顯示控制按鈕尺寸統一

- 將立體巡檢雲臺的「巡檢點顯示」與「立體控制」統一為 110×34px，左側控制入口整齊對齊。

### 2026-08-28｜駐衛警平面圖入口調整

- 平面圖頁移除「標記定位」入口按鈕；頂列「切換3D圖」連結維持巡檢篩選參數。

### 2026-08-28｜派車申請桌面版 85% 縮放

- 派車申請內容區在桌面 100% 瀏覽器以 85% 視覺比例呈現，透過外層寬度補償避免水平溢出；手機版維持 100% 滿寬。

### 2026-08-28｜派車申請編號清單取消內層捲軸

- 移除派車申請表格的內部高度限制與捲動，申請編號清單完整展開，由頁面自然捲動。

### 2026-08-28｜公務車主檔內容寬度調整

- 公務車主檔白色內容卡桌面版縮減為 85% 並置中，頁面標題維持原寬度；手機版內容卡恢復 100% 滿寬。
- 公務車主檔頁內的說明文字與右側操作按鈕同步對齊 85% 白色內容卡左右邊界。

### 2026-08-28｜駕駛人員內容寬度調整

- 駕駛人員白色內容卡比照公務車主檔，桌面版 85% 置中、標題維持原寬度，手機版恢復 100% 滿寬。

### 2026-08-28｜派車管理員與派車紀錄內容寬度調整

- 派車管理員、派車紀錄白色內容卡同步採桌面 85% 置中，標題區維持原寬度，手機版恢復 100% 滿寬。
- 三頁的頁內說明與操作列同步對齊白色內容卡左右邊界。

### 2026-08-28｜駐衛警 2D／3D 圖資與巡檢點同步

- 駐衛警平面圖與 3D 雲臺共用 3D 建模產出的 `floor_models`，巡檢點共用 `plan_markers` 的 `kind=patrol`；平面圖的切換按鈕回到駐衛警 3D 雲臺並保留巡檢篩選。
- 平面圖「切換3D圖」改為直接連至駐衛警巡檢 3D 雲臺 `/systems/guardpatrol/map3d/?kind=patrol`。

### 2026-08-28｜V2 日期欄位：觸控裝置改為直接叫出系統日曆

- 現場回報 `/v2/systems/vehicle/requests/` 手機版「年/月/日」點下去沒有萬年曆。
- 根因：`LocalizedDateInput` 用一個 `1x1`、`opacity:0`、`pointer-events:none` 的原生 `<input type="date">` 當載體，點欄位時呼叫它的 `showPicker()`。**iOS Safari 對不可見元素呼叫 `showPicker()` 會丟例外**，元件 catch 之後切到手動輸入模式（`manual`），使用者看到的就是「可以打字但沒有日曆」。這不是版面問題，桌面 Chrome 因為 `showPicker()` 正常所以一直沒被發現。
- 修正：原生 date 欄位的行內樣式改成 `.localized-date-native` class；`@media (pointer:coarse)` 時變成覆蓋整個欄位的透明層（`inset:0`、`pointer-events:auto`、`z-index:2`、`color:transparent`），點欄位直接觸發系統日曆，不再依賴 `showPicker()`。桌面（`pointer:fine`）維持 `1x1` 隱藏與 `pointer-events:none`，行為完全不變。
- 順帶修掉元件自己註解過的坑：那段行內樣式正是讓 media query 蓋不過去的原因（行內樣式優先序最高），已改為 class。
- **影響範圍是全站所有 `LocalizedDateInput`**（約 20 處），觸控裝置上都會變成可直接點開日曆；桌面不受影響。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；靜態重現頁以 Chrome 觸控模擬量測——`pointer:coarse` 時 `document.elementFromPoint()` 打在欄位中央回傳的是 `localized-date-native`（`type=date`），代表點擊會落到原生日期控制項；`pointer:fine` 時原生欄位 `pointer-events:none`、中央回傳的仍是原本的文字欄位容器。**尚待 iPhone 實機確認**。程式提交 `e98f7ac` 並推送 `origin/main`。

### 2026-08-28｜V2 派車申請手機版：頁首按鈕靠右

- `/v2/systems/vehicle/requests/` 手機版「＋新增派車申請／重新載入」改為靠右對齊，比照同日其他頁面的處理。以相鄰選擇器 `.admin-page-actions:has(+ .vehicle-flow-grid)` 限定只影響這一頁，規則位於 `@media (max-width:800px)`，桌面版不變。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；靜態重現頁量測 375px：兩顆按鈕同列且靠右（右緣與容器切齊），`scrollWidth` 375 無溢出。程式提交 `34a6001` 並推送 `origin/main`。

### 2026-08-28｜V2 駕駛人員名單手機版：頁首按鈕靠右

- `/v2/systems/vehicle/drivers/` 手機版「＋新增駕駛／全部刪除／重新載入」改為靠右對齊。新增 `.vehicle-people-panel` 標記 class，以 `.admin-page-actions:has(+ .vehicle-people-panel)` 限定範圍；同一個元件也負責派車管理員名單，兩頁一起生效。規則位於 `@media (max-width:800px)`，桌面版不變。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；靜態重現頁量測 375px：三顆按鈕同列（y=29）且靠右切齊，`scrollWidth` 375 無溢出。程式提交 `0f5ab2f` 並推送 `origin/main`。

### 2026-08-28｜V2 派車紀錄手機版：頁首按鈕靠右

- `/v2/systems/vehicle/logs/` 手機版「重新載入」改為靠右對齊。新增 `.vehicle-logs-panel` 標記 class，與 `.vehicle-people-panel` 合併為同一條規則，避免同樣的宣告一再複製。規則位於 `@media (max-width:800px)`，桌面版不變。
- 未一併調整的部分：這頁的「搜尋框＋共 N 筆」目前仍是上下兩列，結構與公務車主檔相同（該頁已改為同列靠右）。使用者本次只指定按鈕靠右，故維持現狀，待確認是否要比照處理。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；靜態重現頁量測 375px：按鈕右緣與容器切齊、無水平溢出。程式提交 `ca5327d` 並推送 `origin/main`。

### 2026-08-28｜V2 會議室預約系統手機版：按鈕靠右（四個子系統一次涵蓋）

- 現場回報 `/v2/systems/meetingroom/` 底下 bookings／rooms／changes／notifications 四頁的按鈕要靠右。**只調整手機版（≤800px），桌面版不變**。
- 作法改進：不再逐頁加標記 class，改用**系統代碼定位**——`.content:has(.system-page-heading[data-system-key="meetingroom"]) .admin-page-actions>div:last-child`。`SystemPageHeader` 一律帶 `data-system-key`，因此同系統所有子系統一次涵蓋；`:has()` 掛在 `.content` 上，連巢狀在 `.mr-page` 裡的頁首也命中（實測兩種位置都正確靠右）。之後同類需求可沿用這個寫法。
- 「＋新增會議室」原本包在 `style={{display:'flex',gap:8}}` 的容器裡，**行內樣式優先序高於 media query，無法只在手機版改方向**，改為 `.mr-form-actions` class；桌面版計算後的樣式與原本完全相同（`display:flex; gap:8px; justify-content:normal`，已實測比對）。這是本日第二次被行內樣式擋住（前一次是 `LocalizedDateInput`）。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；靜態重現頁量測 375px：直接同層的頁首按鈕、巢狀在 `.mr-page` 內的頁首按鈕、彈窗送出按鈕三者右緣皆為 375（切齊容器），`scrollWidth` 375 無溢出；1280px：`.mr-form-actions` 計算樣式與原行內樣式一致。程式提交 `367ba16` 並推送 `origin/main`。

### 2026-08-28｜V2 手機版頁首按鈕靠右改為全站規則＋寫入專案規範

- 使用者要求把先前逐頁處理的「手機版頁首按鈕靠右」套用到全部 49 個子系統，並寫進專案設定。
- **收斂成一條全站規則**：`@media (max-width:800px){.admin-page-actions>div:last-child{width:100%;justify-content:flex-end}}`，放在 `web/app/admin-workspace.css` 既有的 800px 斷點內。`AdminHeader` 全站共 18 個檔案、27 處使用，一條規則全部涵蓋。
- **同時刪掉六條分頁專用宣告**（`vehicle-master-panel`／`vehicle-flow-grid`／`vehicle-people-panel`／`vehicle-logs-panel`／`patrol-shift-shell`／`patrol-notifications-panel`，以及會議室那條用系統代碼定位的規則），避免相同宣告散落在三個 CSS 檔。
- 另一種頁首 `.operations-panel-title` 本身就是 `justify-content:space-between`，按鈕已在右側，不需處理——V2 只有這兩種頁首樣式。
- **寫入專案規範**：`AGENTS.md` 新增「V2 手機版版型規範（2026-08-28 訂）」一節，除頁首靠右外，一併記下本日累積的四條教訓——不要用行內樣式排版（一天內被擋三次）、手機版排版靠 `flex-basis` 而非 `flex-wrap`、`display:contents` 的容器要先解掉才能分列、以及手機版驗證要用建置產出的實際 CSS chunk 建重現頁（且必須載入基礎樣式 chunk，否則量到錯誤結果）。
- **加上防迴歸斷言**：`tools/system-page-heading-check.mjs` 新增兩條——全站規則必須存在、不得再出現 `.admin-page-actions:has(...)` 的分頁重複宣告。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；靜態重現頁以四種頁首形態（1 顆／2 顆／3 顆按鈕、巢狀於 `.mr-page`）量測——375px 四者的最後一顆按鈕右緣皆為 361，與內容區右緣切齊，`scrollWidth` 375 無溢出；1280px 四者右緣皆為 1256（沿用 `.page-actions` 的 `space-between`），與修改前一致。程式提交 `0103f06` 並推送 `origin/main`。

### 2026-08-28｜V2 效能：系統圖示過大導致入口頁很慢（13.6MB → 2.18MB）

- 現場回報 `/v2/systems/` 很慢、並詢問是否為記憶體未釋放。實測後**兩個問題分開**：
- **不是記憶體洩漏**。V2 全部 4 處 `setInterval`（AppShell 時鐘、TV 儀表板、資安告警輪詢、會議室週視圖）都有對應的 `clearInterval`；Supabase realtime 兩處都有 `removeChannel`。`web/lib/` 底下 `access-audit`／`error-tracker`／`security-audit` 的 `addEventListener` 沒有對應 remove，但那是模組載入時註冊一次的全域監聽，屬於應用程式生命週期，不是每頁累積。
- **真正原因是圖示**。入口頁 JS/CSS 壓縮後只有 **278KB**，但八個系統圖示要載 **2.63MB**（PNG 無法再壓縮），等於整頁約九成流量都是圖示。
- 根因：圖示以 `draw` 產生的原尺寸 1024～1254px 直接進版控（單檔 1～1.5MB），但最大顯示尺寸只有入口圖卡的 **88px**。全站 24 個被引用圖示合計 **13.6MB**。
- 修正：LANCZOS 重取樣縮到 320px；兩張 Sprite 依格數換算（`equipment-structure-icons-ai.png` 4×4 → 512px、`topbar-nav-icons-ai.png` 2×2 → 256px，`background-size` 用百分比所以**不必改 CSS**）。結果 **13.60MB → 2.18MB（-84%）**，入口頁圖示 **2.63MB → 769KB（-71%）**，88px 顯示下無畫質損失。
- **不採用 256 色量化**：實測可再壓到 0.46MB，但這批 3D 光澤圖示量化後在 88px 顯示的色差 RMS 達 **5～17**（可見色帶），與先前背景圖優化的 0.114 標準差太多，省下的 1.7MB 不值得。
- 防迴歸：`AGENTS.md` 補上「圖示一律收在 320px 以內」與量化的禁用理由；`tools/system-page-heading-check.mjs` 新增斷言——系統 Logo 超過 200KB 直接擋下。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；Sprite 縮圖後以肉眼確認 4×4 格線完整。程式提交 `4cf377d` 並推送 `origin/main`。

### 2026-08-28｜兩件待確認事項結案

- **日期欄位的系統日曆：實機確認可用**。08-28 的修正（觸控裝置把原生 `<input type="date">` 改為覆蓋整個欄位的透明層，取代在 iOS 上會丟例外的 `showPicker()`）經使用者於 iPhone 實測，點欄位會正常跳出系統日曆。先前紀錄中「尚待 iPhone 實機確認」的部分到此結案；全站約 20 個 `LocalizedDateInput` 一併受益。
- **維修入口七張統計卡：維持 133×63**。使用者確認不改回 190×90。備份分支 `backup/workorder-190x90-20260827` 已刪除（commit `601ac30d`，若日後想找回可由 reflog 或此紀錄的 SHA 取回）。相關脈絡：190×90 是 08-27 20:36 的中間狀態，20:53 的 `76e8e39` 已改為 133×63 並上線，等於被後續決定取代。

### 2026-08-28｜V2 平面樓層圖：慢的原因與面板對齊

- 現場回報 `/v2/systems/structuremap/floor2d/` 顯示圖很慢，並要求面板對齊。
- **量測結果（真實圖檔 4096×4015、16.4M 像素，桌機 Chrome）**：下載＋解碼（本機）9～25ms、`drawImage` 1ms、`getImageData` **90～241ms**、逐像素迴圈 36～40ms、`putImageData` 3ms、`toBlob` PNG **116～168ms**。**客戶端前處理合計約 250～450ms**，之後 OpenSeadragon 還要再解碼一次 1.26MB 的 blob；手機依經驗約 3～6 倍。而且**每次切換樓層都重跑一次，沒有快取**。
- 兩個被否定的假設（都實測過，不採用）：`toBlob` 改 WebP **更慢**（PNG 116ms vs WebP 無損 293ms、q0.92 572ms）；canvas 開 `willReadFrequently:true` 的 `getImageData` **沒有比較快**（106ms vs 90ms）。
- **主因之一：V2 一直用原圖**。Storage 的 `floorplans` 原圖 B1 977KB ～ RF 2265KB，另有 `mobile/` 縮圖（1024px、240～281KB，**RF 沒有**）。V1 的 `b1plan.html` 早就用 `matchMedia('(max-width:768px),(pointer:coarse)')` 換圖，V2 沒做，等於手機也在下載 4096px 原圖並對 16.4M 像素做前處理。已修正：手機／觸控裝置改讀 `mobile/`，取不到（RF）時退回原圖。像素量少 16 倍、下載少約 75%。
- **面板對齊**：`.f3-floors` 是 `top:62px`、`.f3-mkpanel` 是 `top:104px`（104px 是為了讓出 3D 的「控制」按鈕位置）。平面圖沒有控制面板，左欄 62px 是空的。新增 `.f3-no-ctrl` 標記於平面圖根節點，把標記面板與收合鈕上移到 62px；3D 與雲台維持 104px。實測兩面板 `top` 皆為 62。
- 順帶查證：樓層清單的圓點**本來就對齊**（實測皆 x=1095、9×9），選取中的那顆只是多了 `box-shadow: 0 0 6px var(--cyan)` 的光暈，看起來比較大。若要完全一致可以拿掉光暈，待使用者決定。
- **尚未做、需決定的兩項**：(1) 把處理後的 blob 依「樓層＋主題」快取（AGENTS.md 早已列為待辦），可省下切換樓層時整段 250～450ms；(2) 治本做法是在 3D建模系統上傳時就產生 light／tech 兩種成品圖，客戶端零處理，但需要產生並上傳新資產。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過。程式提交 `66ee336` 並推送 `origin/main`。

### 2026-08-28｜V2 平面圖：切換樓層快取、圓點光暈一致、衍生圖產生器

- **切換樓層快取**：重畫後的 blob 依「樓層圖路徑＋主題」以 LRU 快取（上限 4 張），切回看過的樓層不必再跑一次 下載→`getImageData`→逐像素→`toBlob`（實測 250～450ms）。快取放在元件 `ref` 而非模組層級，離開頁面時一併 `revokeObjectURL`；命中時重新插入 Map 更新使用順序，避免把正在顯示的那張淘汰掉。
- **圓點光暈一致**：未選取的樓層圓點補上同半徑的中性灰 `box-shadow`。原本只有選取中的那顆有光暈，視覺占位大一圈，整排看起來像沒對齊（方塊本身一直是同一個 x、9×9，已實測）。
- **新增 `tools/build-floorplan-variants.py`**：從 `floorplans` 儲存桶下載原圖，產生 `desktop/`（2048px）、`mobile/`（1024px）與 `light/`、`tech/`（已重畫完成）四組衍生圖後上傳。`to_light()`／`to_tech()` 與 `floor-stack-3d.tsx` 的 `preparePlanCanvas` 演算法一致，並沿用同一條「底圖不透明就放棄重畫」的保險絲。以 `system/plans/tex/b1_tex.png`（4096×4015）實跑：原圖 2.40MB → desktop 936KB／light 693KB／mobile 311KB／light-mobile 221KB，產出的 light 圖確認為黑線＋透明底。
- **本機無法代為上傳**：Supabase CLI 2.116 的 `storage cp` 在此環境不支援遠端→本機複製（`LegacyStorageUnsupportedOperationError`），且環境未設 service_role key。上傳需由使用者自行執行腳本（先 `--dry-run` 檢查）。等衍生圖進到儲存桶之後，再改客戶端優先讀 `light/`／`tech/` 成品圖並跳過預處理——那一步要等資產存在才驗證得了，因此未先寫。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過。程式提交 `144e13b`、`3b52020` 並推送 `origin/main`。

### 2026-08-28｜V2 平面圖治本（步驟 1／4）：上傳時就產生衍生圖

- 決策轉向：原先規劃用 `tools/build-floorplan-variants.py` 加 service_role key 產生衍生圖，改為**做進 3D建模系統的上傳流程**。理由是查證後發現建模頁本來就以登入管理員身分寫入 `floorplans`（已經在傳 `<F>.png` 與 `mobile/<F>.png`），既有 `makeMobileBlob()` 也是同一套 canvas 縮圖手法——不需要 service_role key（那把鑰匙可繞過所有 RLS，為產圖而放進環境變數不划算），也不需要本機 Python。Python 腳本保留作為批次／CI 備援。
- 本次（步驟 1）：`saveModel()` 除了原圖與 `mobile/`，再產生並上傳 `desktop/`（2048px）、`light/`、`light/mobile/`、`tech/`、`tech/mobile/` 五個檔。
- 為了讓成品與畫面一致，把 `preparePlanCanvas`／`preparePlanObjectUrl` 從 `floor-stack-3d.tsx` 抽到 **`web/lib/floorplan-render.ts`**（本來就與 three.js 無關），`floor-stack-3d.tsx` 改為 re-export，既有三個呼叫端不用動；建模系統也不必從帶方括號的路由資料夾 import。函式簽章放寬為 `HTMLImageElement | HTMLCanvasElement`。
- 失敗處理比照既有 mobile 版：任一張衍生圖失敗只記 console 並在成功訊息裡回報張數，不影響原圖上傳；檢視器取不到成品時仍退回現行流程。
- 驗證：`npm run typecheck:v2`（過程中抓到與既有 `canvasBlob` 重複定義，已改為沿用既有 helper）、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過。程式提交 `e086a03` 並推送 `origin/main`。
- 下一步（步驟 2）：在建模頁加「補產生衍生圖」按鈕，讓現有 7 層不必重新上傳 DXF 就能補齊。

### 2026-08-28｜V2 平面圖治本（步驟 2／4）：建模頁「補產生衍生圖」按鈕

- 步驟 1 只讓「之後上傳的樓層」自動附帶衍生圖，既有 7 層仍然沒有。本步在建模頁「已建立的樓層模型」下方新增 **「⟳ 補產生衍生圖」**：逐層以短效 signed URL 讀回原圖 → 在瀏覽器產生五張衍生圖 → 上傳，**不必重新上傳 DXF**。
- 與 `saveModel()` 共用同一支 `uploadDerivedPlans()`，成品與上傳時產生的完全一致。逐層顯示進度（`補產生衍生圖…（3/7）3F.png`），單層失敗只記 console 並計入失敗張數後繼續下一層；檢視器取不到成品本來就會退回原圖，所以部分失敗不會壞畫面。
- 原圖以 `crossOrigin='anonymous'` 載入，否則 canvas 會被跨網域污染而讀不到像素。
- 按鈕只在有樓層模型且無 `floor_models` 錯誤時顯示，並註明「只需執行一次；之後新上傳的樓層會自動附帶」。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過。程式提交 `dc01987` 並推送 `origin/main`。
- 下一步（步驟 3 由使用者執行）：到 `/v2/systems/structuremap/modeler/` 按一次該按鈕；完成後（步驟 4）再把檢視器改為優先讀成品圖並跳過預處理。

### 2026-08-28｜交接紀錄內容區寬度與對齊

- 交接紀錄頁的說明／操作列與白色內容卡統一納入同一個內容容器，桌面版寬度為 85% 並置中；系統標題區維持原本寬度。
- 手機版內容容器恢復 100% 滿寬，避免內容卡與操作列水平溢出。
- 驗證：`git diff --check`、`npm run test:page-headings` 通過。

### 2026-08-28｜平面圖巡檢點顯示同步立體巡檢雲臺

- `floor2d/?kind=patrol` 的標記面板改與 `guardpatrol/map3d/?kind=patrol` 同步：顯示巡檢日期、顯示巡檢點、文字標籤，以及已打卡／未打卡圖例。
- 依選定日期讀取打卡紀錄，巡檢點顏色同步為已打卡綠色、未打卡紅色；2D 與 3D 共用同一套巡檢點語意。
- 驗證：`npm run test:page-headings`、`git diff --check` 通過；`npm run typecheck:v2` 因環境未安裝 `tsc` 未執行成功。

### 2026-08-28｜稽核紀錄表頭單列排版

- 稽核紀錄表加入專用樣式，固定「時間、操作人員、來源 IP、資源／操作、具體內容、裝置、紀錄編號、詳細內容」表頭不換行，並維持內容欄位省略顯示。
- 驗證：`git diff --check`、`npm run test:page-headings`。

### 2026-08-28｜資安告警篩選列防溢出

- 資安告警頁篩選列改用可收縮欄寬與完整寬度限制，較窄桌面會依既有斷點自動分欄，避免搜尋、類型、等級、狀態與日期控制項超出內容區。
- 驗證：`git diff --check`、`npm run test:page-headings`。

### 2026-08-28｜稽核紀錄表格防水平溢出

- 桌面稽核表改為固定比例欄寬，長裝置資訊與紀錄編號以省略號收斂，避免長字串撐出水平捲軸；窄版仍沿用原有卡片式響應式表格。
- 驗證：`git diff --check`、`npm run test:page-headings`。

### 2026-08-28｜稽核頁關閉水平捲軸

- 稽核紀錄表容器關閉水平捲軸，搭配固定欄寬與長字串省略，確保頁面不出現水平捲軸。
- 驗證：`git diff --check`、`npm run test:page-headings`。

### 2026-08-28｜平面圖巡檢模式排除非巡檢點

- `floor2d/?kind=patrol` 改為強制只渲染 `patrol` 類型標記，避免設備或其他類型的藍色圖釘混入巡檢圖面。
- 巡檢點仍依選定日期打卡狀態顯示綠色（已打卡）／紅色（未打卡）。
- 驗證：`git diff --check`、`npm run test:page-headings`。

### 2026-08-28｜V2 補產生衍生圖：回報成功但檔案沒進儲存桶（未解，調查紀錄）

- 使用者按下「補產生衍生圖」，畫面回報「共 35 張」（7 層 × 5 張），但直接查正式庫：`floorplans` 桶內**物件總數仍是 13**（7 原圖 + 6 mobile），`light/`、`tech/`、`desktop/` 各 **0**，全桶最後異動時間停在 2026-08-19。所有 bucket 在近 3 小時內**都沒有新物件**。
- 加上驗證後再跑一次，結果是「上傳回報成功 35 張、失敗 0 張，但儲存桶實際只有 0 張」——**`upload()` 連 `data.path` 都回傳了**（新版程式碼要求有 path 才算成功），但檔案不存在。
- 已排除的可能：
  - **專案接錯**：部署後的 JS chunk 內只出現 `qztffronusdhgxhjjubt.supabase.co`，與查詢的專案相同；以 `floor_models`（7 筆）與 B1 `plan_markers`（30 筆）交叉比對，CLI 查的確實是同一個庫。
  - **權限不足**：`floorplans_manager_insert`（INSERT，authenticated，`has_system_access('sys_equipment_manage')`）存在且不限定資料夾前綴；權限不足會回 403 而不是成功。
  - **桶設定限制**：`file_size_limit` 與 `allowed_mime_types` 皆為 `null`。
  - **觸發器攔截**：`storage.objects` 上只有 `protect_objects_delete`（BEFORE DELETE）與 `update_objects_updated_at`，沒有 INSERT 相關。
  - **Service Worker 假回應**：`system/patrol-service-worker.js` 只攔 `url.origin === location.origin`，Supabase 是跨網域，不會被攔。
  - **部署沒生效**：把線上 chunk 抓下來比對，上傳邏輯與原始碼一致。
- **下一步需要的資料**：瀏覽器 Network 分頁中，`POST .../storage/v1/object/floorplans/light/B1.png` 的**狀態碼與回應內容**。在拿到之前不動步驟 4（檢視器改讀成品圖），因為成品圖並不存在。
- 附帶成果：訊息已改為「以 `storage.list()` 實際數過的數量」為準，並要求 `upload()` 必須回傳 `data.path` 才算成功——這是今天第三次遇到「畫面回報成功、實際沒發生」，防線先補上。程式提交 `ca2b95a`。

### 2026-08-28｜會議室四個子頁內容區寬度與對齊

- 會議室預約、會議室管理、預約變更申請與預約提醒共用內容容器，桌面版說明／操作列與白色內容卡統一為 85% 並置中；系統標題區維持原寬度。
- 手機版恢復 100% 滿寬，避免內容卡與操作列水平溢出。
- 驗證：`git diff --check`、`npm run test:page-headings` 通過。

### 2026-08-28｜駐衛警通知頁內容區寬度與對齊

- 逾時推播通知頁的說明／操作列與白色內容卡統一納入 85% 置中內容容器；系統標題區維持原寬度。
- 手機版內容容器恢復 100% 滿寬，避免水平溢出。
- 驗證：`git diff --check`、`npm run test:page-headings` 通過。

### 2026-08-28｜49 個子系統共用內容寬度規範

- 在 V2 共用版面建立全站一般子系統內容規則：系統標題區維持原寬度，標題以下的直接內容桌面版統一 85% 置中；手機版自動恢復 100% 滿寬，避免各頁自行調整造成移動不一致。
- 此規則涵蓋 9 大系統的 49 個子系統，特殊全螢幕圖資頁仍沿用各自的圖面外殼，不受一般內容容器規則影響。
- 驗證：`git diff --check`、`npm run test:page-headings` 通過。

### 2026-08-28｜公文傳送入口圖卡連結

- 整合管理系統入口的「公文傳送系統」圖卡改為直接連至 `/v2/systems/officialdocs/routing/`，避免先進入空的系統入口頁。
- 驗證：`git diff --check`、`npm run test:page-headings` 通過。

### 2026-08-28｜V2 平面圖治本（步驟 3／4 完成）：成品圖已上傳並生效

- **步驟 3（使用者執行）**：建模頁按「⟳ 補產生衍生圖」，訊息為「✓ 補產生完成，儲存桶實際有 35 張衍生圖」。以 SQL 覆核：`floorplans` 物件由 13 增為 **48**（`light/` 14、`tech/` 14、`desktop/` 7），確認 35 張全部落地。訊息中沒有出現「改用直接呼叫 API 才成功」，代表這次 supabase-js 自己就成功。
- **前兩次失敗的根因未查明**。已排除專案接錯、RLS、桶限制、觸發器、Service Worker、部署未生效，並實測 Storage API 會正確拒絕無效身分（假 token → `Invalid Compact JWS`；anon key → `new row violates row-level security policy`），所以不是「對誰都回 200」。第三次成功前唯一的差異是硬重整與新版程式碼。**不編造解釋**；「上傳後立刻查證」的機制保留，日後再發生會立刻現形。
- **步驟 4**：平面樓層圖改為優先開 `light/` 或 `tech/` 成品圖，跳過 `getImageData → 逐像素 → toBlob`。兩種主題的 signed URL 在載入時一次簽好（桌機取 2048px、手機取 `mobile/`），切換介面風格不必重簽。取不到成品仍走原流程；連原圖都簽不出來但成品還在的樓層也開得起來（`anyUrl` 判斷）。
- 預期效果：桌機淺色由「下載 2.4MB + 250～450ms 處理」變為「下載約 690KB + 零處理」；手機由「1MB + 1～2.5 秒」變為「約 220KB + 零處理」。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過。程式提交 `d09a265` 並推送 `origin/main`。**尚待使用者實機確認進場速度**。
- 後續可做：3D 樓層圖與標記圖臺目前仍走客戶端重畫，成品圖已經備好，改法與本次相同。

### 2026-08-28｜V2 圖資：3D 樓層圖、立體雲台、標記圖臺一併改用成品圖

- 平面圖（步驟 4）之後，把同一套做法推到其餘三個檢視器：`structuremap-floor3d`、`patrol-map3d`（駐衛警立體雲台）、`structuremap-markerboard`（整合標記圖臺）。全部改為優先開 `light/`／`tech/` 成品圖，跳過「下載原圖 → `getImageData` → 逐像素重畫 → `toBlob`／重建貼圖」。**3D 是 7 層一起載入，這段成本原本要乘以 7。**
- **選圖規則抽成共用函式** `signFloorPlanVariants()`（`web/lib/floorplan-storage.ts`）：一次簽好兩種主題 × 對應尺寸（桌機 2048px、觸控 1024px）的連結，四個檢視器共用同一份，不再各自組 `light/`、`tech/`、`mobile/` 的路徑字串；切換介面風格也不必重簽。平面圖先前寫在頁面內的版本一併收斂進來。
- `floor-stack-3d.tsx` 的 `StackModel` 增加 `light_url`／`tech_url`，有成品圖時直接把 texture 貼上、不呼叫 `preparePlanTexture`。標記圖臺同理，且成品圖存在時**完全不產生 blob**，也就沒有 `revokeObjectURL` 的生命週期問題。
- **`tools/floor-consistency-check.mjs` 攔到了這次重構**：它原本硬性要求四個檢視器出現 `signFloorplanPaths` 字串（防止有人改用公開網址），重構後字串消失而測試失敗。改為接受它或上層包裝 `signFloorPlanVariants`（底層仍是短效 signed URL），並**新增斷言確保選圖邏輯留在共用檔案裡**，避免日後又被各頁複製一份。這條檢查發揮了預期作用，不是誤報。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過。程式提交 `22fc1ae` 並推送 `origin/main`。**四個檢視器均待實機確認**（平面圖、3D 模型圖、駐衛警立體雲台、整合標記圖臺）。

### 2026-08-28｜平面圖篩選提示與樓層選單對齊

- 平面樓層圖移除中央的篩選提示／關閉區塊，避免遮住圖面。
- 右側樓層選單項目改用與面板標題一致的水平內距，讓選單內容對齊面板基準線。
- 驗證：`git diff --check`、`npm run test:page-headings` 通過。

### 2026-08-28｜平面圖右側樓層選單對齊修正

- 修正共用按鈕樣式造成樓層選項內容水平置中的問題，改為靠左排列並對齊右側面板內容基準線。
- 驗證：`git diff --check`、`npm run test:page-headings`。

### 2026-08-28｜「上傳回報成功但未落地」追查進度：排除 SDK，根因仍未明

- 讀過安裝版本 `@supabase/storage-js 2.112.2` 的 `uploadOrUpdate()` 原始碼：`_removeEmptyFolders()` 只清頭尾與重複斜線，`light/B1.png` 這類路徑不受影響；`handleOperation()` 的錯誤處理正確（StorageError 會回 `{data:null, error}`）；回傳的 `fullPath` 取自伺服器回應的 `data.Key`。**SDK 可以排除**——那次請求確實被 Storage 服務接受並回了真實的 Key。
- **確認沒有資料損失**：7 張原圖的 `updated_at` 仍停在 2026-08-19／08-03，失敗的兩次沒有誤覆蓋原圖（曾懷疑 multipart 的 File name 會蓋掉 URL 路徑，已排除）。
- **修正先前的一個誤判**：`if (!result.data?.path)` 這道檢查其實沒有鑑別力——`path` 是 SDK 以本地變數 `cleanPath` 填的，只要請求沒拋錯就一定有值。真正有效的是其後以 `storage.list()` 實際列出來數的那道驗證，該道要保留。
- **結論**：伺服器接受請求並回了真實 Key，物件卻沒落地，且事後不可重現。無法從事後判定根因；剩餘可能為 Storage 服務當下的暫時性故障或用戶端網路的中間設備，兩者都符合「硬重整後第三次成功」。下次若再發生，應保留回應的 `x-request-id` 以便向官方查伺服器端紀錄。
- **四個圖資頁的檔案面已先行驗證**：35 張衍生圖全部存在、`mimetype` 皆為 `image/png`、大小合理（`desktop/` 718～1645KB、`light/` 488～966KB、`light/mobile/` 163～310KB、`tech/` 456～975KB、`tech/mobile/` 160～309KB），無 0 byte 或異常小檔，因此不會出現「某層空白」。仍待人工確認畫面與速度感受。

### 2026-08-28｜P0 公文傳送流程跨角色端到端驗收通過，並修好補正重送後送不出陳核的缺陷

- 新增 `tools/e2e-official-document-flow.mjs`，以五個彼此分離、分屬不同部室的測試帳號跑完整流程：公文管理人員（企劃部，`permissions.official_document_manager`）、承辦人員（企劃推廣課）、會辦收發（業務部）、陳核／核決（秘書室）、他部室對照組（財務部）。**31 項全部通過**。
- 涵蓋範圍：建立與取號（民國年三碼＋月日四碼＋流水四碼，條碼值與文號一致）、二階單位／人員連動驗證、送出會辦 → 收文 → 完成會辦 → 送出陳核 → 陳核簽收 → 核決 → 原申請人收訖、退回補正重送後再核決結案、條碼查詢唯一命中、時間軸不可刪除與不可竄改（服務角色的 DELETE／UPDATE 均被 `trg_official_document_events_immutable` 擋下）、冪等（重複 `idempotency_key` 回 `duplicate`），以及每一段的越權與順序防護共 11 條。
- **驗收抓到一個真缺陷並修正**：`official_document_action` 的 `send_approval` 前置條件寫成 `steps.length === 0`。退回補正後狀態雖回到 `draft`，既有節點仍留在時間軸上，於是**補正過的公文永遠送不出陳核**；而前端在 `draft` 仍會顯示可按的「送出陳核」，使用者會直接卡死。改為「沒有尚未完成的會辦節點」（`pendingCoSign`），維持原本「所有會辦完成後才能送出陳核」的意圖。提交 `5d4b425`，由 `deploy-edge-functions.yml` 自動部署後重跑 e2e，補正路徑時間軸為 `create → barcode_generated → send_approval → approval_receive → return → resubmit → send_approval → approval_receive → approve → originator_receive`。
- **限流是實作 e2e 時的關鍵細節**：`official_document_create`／`official_document_action` 屬 `admin-api:write` 範圍（`_shared/security-monitor.ts`：每位操作者 60 秒 10 次），且**限流在權限判斷之前計數，連被擋下的越權測試也算一次**。腳本因此在客戶端照同一條規則自我節流，429／5xx 退避只當保險；否則 429 會被誤讀成「已被擋下」而讓越權測試假通過。
- 測試資料依正式資料保護規則處理：帳號 `hidden=true` 建立、主旨標註「驗收測試（勿使用）」，結束後帳號改 `inactive`（**一併清掉 `supervisor_id`**，否則 `guard_user_supervisor_hierarchy` 會因「直屬主管必須是啟用中的帳號」讓其餘角色停用不掉），公文與事件一律保留不刪。
- 驗證：`npm run typecheck:v2`、完整 `npm test`、`npm run security:audit`（錯誤 0／既有警告 4）通過；`deno check supabase/functions/app-api/index.ts` 通過。

### 2026-08-28｜圖資成品圖檔案面全量驗證、補齊 mobile/RF.png，並修好批次腳本的金鑰標頭

- **四個檢視器的程式路徑已確認**：`structuremap-viewers`、`structuremap-floor3d`、`patrol-map3d`、`structuremap-markerboard` 全部先取 `light_url`／`tech_url` 成品圖直接開，`preparePlanCanvas` 只留在取不到成品時的退路；選圖規則統一在 `signFloorPlanVariants()`。
- **全量下載 7 樓層 × 7 個路徑共 49 個檔案逐一檢查**：全部 HTTP 200、尺寸正確（原圖 2480×2433、desktop／light／tech 2048×2009、mobile 系列 1024×1005），**沒有任何空白或異常小檔**（不透明像素比例 3.96%～14.77%）。`light` 的比例與原圖一致（非背景像素全部塗黑），`tech` 明顯較低（光暈已濾掉），兩個主題確實產出不同且符合演算法。**「3D 某層空白」的失敗模式可排除。**
- **補上唯一缺漏的 `mobile/RF.png`**（1024×1005、385KB），衍生圖 7 樓層 × 6 變體全部到齊，手機看 RF 不必再退回 2.3MB 原圖。
- **`tools/build-floorplan-variants.py` 其實是壞的**：新版 service_role 金鑰是 `sb_secret_` 格式（非 JWT），只送 `Authorization` 時 Storage 解析不到租戶，回一個**會誤導人的 400 `{"statusCode":"404","error":"Bucket not found"}`**——看起來像儲存桶不存在，其實是沒帶 `apikey`。兩個 header 都送即正常。這支是 CI／批次重產的備援路徑，先前沒人再跑過所以沒發現。
- 載入量對照（7 層 3D 全載）：原圖合計約 8.6MB；桌機淺色成品合計約 4.2MB、手機淺色成品合計約 1.4MB（**−84%**），且客戶端零像素處理。
- 提交 `31a7cc2`。

### 2026-08-28｜「上傳回報成功但未落地」：改為當下就留住 request id

- 追查本身停在「事後不可重現、手上沒有任何可以拿去向 Supabase 查伺服器紀錄的識別碼」。**supabase-js 的 `upload()` 不會把回應 header 交出來**，所以 SDK 路徑一旦「回報成功卻沒落地」，什麼線索都留不下來——這是上次查不下去的真正原因。
- 作法：`uploadViaRest` 收下 `x-request-id`／`sb-request-id`／`cf-ray`；SDK 回報成功但 `storage.list()` 查不到時，補打一次 `object/info` 取同一個儲存節點的 request id；**識別碼寫進畫面訊息，不只寫主控台**（現場人員關掉分頁就沒了）。建模頁的「補產生衍生圖」與 `saveModel()` 兩條路徑都會顯示。
- 根因仍未明，維持追蹤；但下次再發生時已具備可向官方查詢的條件。

### 2026-08-28｜報修3D平面圖子系統

- 報修／派工／完工系統新增「報修3D平面圖」模組，3D 場景沿用立體巡檢雲台的 `FloorStack3D` 與 `floor_models` 圖資，平面圖切換至共用 `floor2d/?kind=repair` 檢視器。
- 3D 與平面圖都即時讀取 `plan_markers`，僅顯示 `repair` 報修點與 `space` 空間標記；圖資或標記更新後重新載入即可同步。

### 2026-08-28｜公文傳送手機版表單排版

- 手機寬度下，文號查詢的「查詢／開啟相機／清除」改為同一列；建立公文的文件類別、第一階單位、第二階單位與承辦人員改為同一列，並以 `minmax(0,1fr)` 防止水平溢出。

### 2026-08-28｜會議室預約手機版基本資訊

- 新增預約的「會議室／日期／會議名稱」在手機版同列呈現；會議名稱改為含空白首項的選單（主管會議、擴大主管會議、面試、管控會議、教育訓練、開／決標）。
- 01／02／03 分段卡分別沿用青、綠、琥珀色識別，桌面版欄位配置不變。
- 第 03 段在網頁版將「每週重複預約」與「同步到我的 Google 行事曆」並排呈現；窄版自動恢復單欄。
- 會議名稱改為可輸入的下拉式欄位（datalist），保留空白首項並提供主管會議、擴大主管會議、面試、管控會議、教育訓練、開／決標選項，網頁版與手機版共用。
- 手機版預約彈窗取消日期原生欄位的焦點框；第 03 段電話、重複預約與 Google 行事曆選項並排。手機版快速預約工具與週排程導覽亦各自收斂為單列，保留完整點擊區。
- 會議室管理彈窗手機版的名稱／容納人數與樓層／狀態各自並排；預約提醒卡將房間、日期與時段整理為單列，避免手機資訊斷裂。
- 手機預約日期欄位隱藏多餘的日曆方形圖示與文字選取高亮，保留點擊日期欄位的原生選擇／手動輸入能力。
- 手機預約第 03 段的兩個選項圖塊改為上下各一列並各自滿寬，電話欄位仍維持同列，方便勾選與操作。
- 公文傳送的使用者可見單位標籤統一改為「部／室」與「課／組／隊」，涵蓋建立公文、單位人員篩選、摘要與驗證訊息；桌面與手機共用同一套文案。
- 組織管理與公文流程的錯誤提示、通知也沿用「部／室」與「課／組／隊」稱呼；資料庫層級與權限判斷仍維持既有兩層架構。
- 會議預約、變更申請與預約提醒頁的手機版頁首，說明文字與「重新載入」按鈕同列對齊並收緊下方間距；桌面版維持原版型。

### 2026-08-28｜地端 IIS／SQL Server 移轉準備（進行中）

- 地端 Windows 已確認 IIS（W3SVC）執行中；完成 SQL Server 2022 Developer 預設執行個體安裝、Windows 驗證系統管理員設定、TCP 1433 監聽與 SSMS 安裝，未在命令列或紀錄寫入 SQL 密碼。
- 建立隔離的本機資料庫 `InspectionLocal`（ONLINE），並以 IIS `InspectionLocal` 站台部署 `_site` 靜態輸出至 `http://localhost:8080/v2/`；入口、系統頁、派車與巡檢頁 HTTP 200 驗證通過。此階段前端仍依原 Supabase 服務運作，尚未切換雲端資料來源。
- 從最新成功的 database-backup workflow 下載加密 artifact 至 `C:\InspectionMigration\backup-20260828`；備份尚未解密，`BACKUP_PASSPHRASE` 僅由管理者在地端輸入，不保存於 repository、Obsidian 或聊天紀錄。
- 因原密語遺失，保管人已在 GitHub Secret 更新 `BACKUP_PASSPHRASE`，並成功完成含 Storage 的完整備份 workflow `33179706593`；新 artifact 已下載至 `C:\InspectionMigration\backup-20260828-new`，等待地端以新密語解密驗證。
- 2026-08-28 已以同一個本機密語成功解密 artifact `inspection-backup-20260828T144954Z.tar.gz.gpg`；manifest 與地端核對為 71 張表／20,706 列／69 個 Storage 物件／32,298,518 bytes。資料列已在 `InspectionLocal` 以交易方式寫入 `dbo.MigrationBackupManifest`／`dbo.MigrationBackupRows` 暫存表，Storage 檔案保留於 `C:\InspectionMigration\backup-20260828-cli\extracted\storage`。
- 2026-08-29 新增 `tools/sqlserver-deploy.ps1`，先驗證每個 NDJSON 的 SHA-256／行數，再將欄位型別推導後部署至 SQL Server `InspectionLocal` 的 `app_final` schema；以明確 BackupId `20260828T144954Z` 完成 71 張表、20,706 列匯入，並將 69 個 Storage 物件不覆蓋同步至 `C:\InspectionMigration\local-storage`。
- **待辦／風險**：現有 V2 直接使用 Supabase PostgreSQL／Auth／Storage／Edge Functions，備份不能直接還原至 SQL Server。下一階段須先以離線密碼解密並盤點 NDJSON，再決定 PostgreSQL→SQL Server 型別／查詢轉換、地端 API、登入與檔案儲存方案；本次未修改雲端資料。

### 2026-08-29｜會議室管理新增按鈕對齊

- 會議室管理彈窗的「新增會議室」操作列改為靠右對齊，與其他表單操作按鈕及手機版頁首操作方向一致；不改變新增、編輯或儲存流程。
- 變更檔案：`web/app/meetingroom-v1.css`。

### 2026-08-29｜派車申請明細彈窗配色收斂

- 派車申請明細彈窗由青／紫／綠／橙四色循環改為中性表面搭配一致藍色資訊線，僅保留目前狀態色作為狀態辨識。
- 移除明細卡片與流程歷程的彩色漸層及多餘陰影，保留狀態、流程、操作按鈕的層級與行動版兩欄版型。
- 驗證：`npm run typecheck:v2`、`npm run test:page-headings`、`npm run build:v2` 通過；完整 `npm test` 在既有的 50／49 子系統數量斷言不一致處停止，與本次 CSS 修改無關。
- 變更檔案：`web/app/admin-workspace.css`。
- 證據：commit `188333d`；GitHub Actions 的 CI、Hardened Pages deployment 與 No-JavaScript synthetic probe 均成功。Commercial readiness 仍因既有 50／49 系統數量稽核落差失敗。

### 2026-08-29｜派車申請新增表單改為可輸入選項

- 出發地、目的地與用途改為可自由輸入的 datalist 欄位；保留第二市場／市場處、第一市場／市場處、會勘／開會／考察等常用選項，空白與自訂文字／數字均可輸入。
- 手機版備註文字框改為填滿整列寬度，桌機版欄位配置維持不變。
- 驗證：`npm run typecheck:v2`、`npm run build:v2` 通過。
- 變更檔案：`web/app/systems/[system]/[module]/vehicle-workspace.tsx`、`web/app/systems/[system]/[module]/vehicle-request-form.css`。
- 補正：以更高選擇器優先權讓手機備註文字框確實跨滿兩欄；最終 commit `a4fdb6e`，Hardened Pages deployment #33249777785 的建置、部署與線上掃描均成功。

### 2026-08-29｜公務車主檔編輯表單手機版欄位排列

- 僅在手機寬度（≤800px）調整公務車編輯彈窗：車號／車名、廠牌／型號各自同列，座位數／目前里程／狀態同列；桌機維持原本共用雙欄表單版型。
- 備註欄在手機版跨滿整列，輸入框寬度隨欄位收斂，避免窄螢幕溢出。
- 變更檔案：`web/app/systems/[system]/[module]/vehicle-workspace.tsx`、`web/app/systems/[system]/[module]/vehicle-request-form.css`。
- 驗證：`npm run typecheck:v2`、`npm run build:v2`、`npm run test:page-headings` 通過。
- 線上手機寬度 375px 驗證三組欄位同列，`document.scrollWidth` 未超過視窗；最終修正 commit `ec871bf`，Hardened Pages deployment #33250314892 的建置、部署與線上掃描均成功。

### 2026-08-29｜修正 CI／Commercial readiness 全部阻塞項目

- 補上 `system/admin.html` 更改密碼表單確認欄位的 `autocomplete="new-password"`，通過 HTML Validate 的密碼自動填入規則。
- 公文傳送與報修 3D 平面圖加入後，V2 實際為 9 大系統／50 個子系統；同步更新按鈕規格稽核與資安設計稽核的數量契約，避免檢查仍誤判 49 個子系統。
- 驗證：`npm test`、`npm run security:audit`（錯誤 0／既有警告 4）、`npx html-validate index.html "system/*.html"`、`npm run typecheck:v2`、`npm run build:v2` 通過。

### 2026-08-30｜市場戰情儀表板與 112／113／114 年行情介接

- 市場營運分析新增獨立的「市場戰情儀表板」頁面，提供日／週／月／季／年一鍵切換，並以比較期同日數資料呈現趨勢與變化，避免決策者自行換算區間。
- 參考 Power BI 的操作邏輯，新增市場／品類／品項篩選與逐層下鑽；同一份分析可切換趨勢、長條、K 線、圓餅、甜甜圈、柏拉圖及明細表，圖表點選後會保留下鑽路徑與返回上一層。
- K 線以交易日為一根，使用中價作開盤、加權平均價作收盤、高／低價作影線，並在圖例明確註記；紅色 ▲／綠色 ▼ 僅表示相對比較期上漲／下跌，不宣稱漲停／跌停。
- 資料介接中心支援 CSV／JSON／XLSX（第一個工作表），可將 112、113、114 年台北農產全場交易行情檔依欄位映射匯入；市場與品類均由資料欄位驅動，不把蔬菜或水果寫死在畫面。
- 驗證：`npm test`、`npm run security:audit`（錯誤 0／既有警告 4）、乾淨 QA 依賴下 `tsc -p web/tsconfig.json --noEmit` 與 `npm run build:v2` 通過；新增路由 `/systems/marketanalytics/command-center`。提交 `2846dba1c`；CI #33316232739、Hardened Pages deployment #33316232760、CodeQL #33316232765、Commercial readiness #33316232740 均成功。

### 2026-08-30｜市場行情篩選連動與歷史檔匯入對應

- 戰情儀表板的「市場 → 蔬果大類 → 品項」改為連動查詢：品項目錄會同時帶入目前市場與蔬果大類，切換上層時清除不再相容的下層值，避免選擇蔬菜仍出現水果品項。
- 戰情儀表板補上行情方向圖例：紅色 ▲ 為上漲、綠色 ▼ 為下跌、— 為持平，並明確註記這是相較期別方向，不等同法定漲停／跌停。
- 資料介接中心新增北農歷史行情常見欄位別名自動對應（果菜類別、品名、品名代號、成交量、平均價、上／中／下價）；檔案沒有成交金額時，依平均價 × 成交量推估，仍保留人工覆寫欄位的能力。
- 本機檢查到 `C:\Users\jnfa\OneDrive\Desktop\台北農產_全場交易行情_112年01-12月.xlsx`（176,400 筆）與 `C:\Users\jnfa\OneDrive\Desktop\巡檢系統\進度說明\台北農產_全場交易行情_113年01-12月.xlsx`（176,840 筆）；114 年 CSV 曾在先前檢查到 182,237 筆，但目前不在 OneDrive 路徑，待檔案重新同步後即可由資料介接中心匯入。
- 驗證：`npm test`、`npm run security:audit`（錯誤 0／既有警告 4）、乾淨 QA 依賴下 `tsc -p web/tsconfig.json --noEmit` 與 `npm run build:v2` 通過。提交 `a8f872600`；CI #33317339962、Hardened Pages deployment #33317340008、CodeQL #33317340127、Commercial readiness #33317339989 均成功。

### 2026-08-31｜派車失敗其實是時段衝突，但訊息說不出原因

- 現場回報 `CAR-20260831-0016` 按「確認派車」失敗，畫面只有「失敗：操作失敗，請稍後再試」。
- **真正原因**：排除約束 `vehicle_dispatch_no_time_overlap` 正確擋下重疊——`AXJ-7830` 已被 `CAR-20260831-0017` 佔用 2026-08-31 13:00–16:30，與本單 13:30–16:30 重疊。**業務規則沒有壞，壞的是訊息。**
- **為什麼看不出來**：約束丟的是英文 `23P01 conflicting key value violates exclusion constraint`。`errorMessage()` 的規則裡沒有這一條（它不是 unique／duplicate），於是掉到最後的通用備援。那句「請稍後再試」還會誤導使用者以為是暫時性故障而重複嘗試。
- **追查方法（可重複用）**：`errorMessage()` 只在訊息是純 ASCII 且對不到任何規則時才回備援字串，所以「看到通用備援」＝「後端丟了未對應的英文錯誤」。用 `do $$ ... exception when others then raise exception 'sqlstate=[%] msg=[%]', sqlstate, sqlerrm; end $$;` 包住 RPC 呼叫，並以 `perform set_config('request.jwt.claims', '{"sub":"<auth_id>"}', true)` 假冒當事人身分，最後的 `raise` 會讓整筆交易回滾，**不會動到正式資料就能拿到原始錯誤**。
- **三層一起修，約束本身不動**：
  1. `vehicle_request_action` 的派車 `UPDATE` 包上 `exception when exclusion_violation`，轉成「公務車 AXJ-7830 在這個時段已被其他派車單使用：CAR-20260831-0017（2026-08-31 13:00–16:30），請改派其他車輛或調整用車時段」，直接指名是哪一張單。migration `20260831120000_vehicle_dispatch_overlap_message.sql`，已套用正式庫並實測訊息正確。
  2. 車輛下拉選單新增 `occupiedVehicleIds`：與本單時段重疊的車顯示「（該時段已被派用）」並停用，確認派車按鈕一併鎖住。半開區間 `[from, to)` 與資料庫的 `'[)'` 一致；只算得到自己讀得到的申請單，所以可能少偵測、但不會誤判成不可用，最終仍以約束為準。
  3. `errorMessage()` 補上 `exclusion constraint|23P01` 的對應，作為其他子系統（如會議室）的安全網。
- 驗證：`npm run typecheck:v2`、`build:v2`、完整 `npm test`、`security:audit`（錯誤 0／既有警告 4）通過；提交 `f8a7b58`，四個 workflow 全綠並已部署。
- **注意**：`CAR-20260831-0016` 本身仍是 `approved` 未派車——那台車那個時段真的被借走了，要嘛改派其他車，要嘛調整用車時段。

### 2026-08-31｜人員管理：直屬主管規則三層對齊，並補上主管被拔掉的防線

- 現場回報編輯人員存不了，訊息「直屬主管必須與人員屬於同一單位」。**根因是三層規則不一致**：

  | 層級 | 允許的主管 |
  |---|---|
  | 前端 `supervisorMatchesDepartment` | 本單位**或其上層** |
  | admin-api `validateSupervisor` | 本單位**或其上層**（訊息就寫「或其上層部／室」） |
  | 資料庫 `guard_user_supervisor_hierarchy` | **只准完全相同** ← 唯一的異類 |

  應用層把上層部室的主管列進選單也放行，最後才被資料庫退。以應用層為準改資料庫（migration `20260831140000_supervisor_hierarchy_alignment.sql`，已套用正式庫）。抽出 `supervisor_unit_allows()` 與 `department_root_row()` 兩支函式，秘書室→副總經理室的既有特例一併帶進 SQL。
- **不是放寬到沒有防線**：實測方向相反的組合（管理部人員配人事課主管）仍被拒，訊息改為「必須與人員屬於同一單位或其上層部／室」；原本 dept_id 比對與 department 文字比對兩條分支丟同一句話，現在分得開。
- **新增 `trg_guard_supervisor_still_needed`**：主管本人被降級／停用／換單位時，原本完全不檢查，下屬會掛著不合法的主管，要等有人去編輯那位下屬才爆錯。現在異動主管本人就擋，並指名還有誰要先改派。實測：停用有下屬的主管、把主管換到平行單位 → 擋下；**把主管往上層移（下屬仍在其管轄內）→ 照常允許**，不是一刀切。
- 前端：現任主管若已不符規則，選單標「（已不符所屬單位，請改派）」，不再看起來一切正常；送出前用與伺服器相同的規則先檢查，訊息講出是哪位主管、屬於哪個單位。
- **追查時另外發現的資料問題（尚未處理）**：15 個啟用帳號的 `rbac_role` 是空的、靠舊的 `role` 欄推導。其中 **`022443` 黃建發的 `role='admin'`，實際生效角色是 `sysadmin`**——一位總務課人員擁有系統管理員權限，這也是原始匯入檔會拿他當「主管」的原因。其餘 14 個推導結果合理（reporter／unit_supervisor）。**已回報使用者，等確認後再處理。**
- 驗證：`npm run typecheck:v2`、`build:v2`、完整 `npm test`、`security:audit`（錯誤 0／既有警告 4）通過；提交 `a6e7394`，四個 workflow 全綠並已部署。

## 2026-09-02 公開交易行情看板市場分色與字級調整

- `/v2/board/` 第一市場採藍色、第二市場採暖米色主題底色；統計區與行情表欄位依市場名稱綁定配色，科技版同步適配。
- 公開看板文字與圖表文字放大 1.1 倍；統計紅綠三角由 10px 改為 13px，圖例由 11px 改為 14.3px，避免與文字倍率疊乘。
- 數值允許完整顯示、單位可換行；提高小字與選中按鈕對比。登入版維持既有字級。
- 驗證：`npm run build:v2`、`npm run test:page-headings` 通過。以正式建置 CSS（包含基礎 chunk）及線上可見行情 DOM 製作本機靜態重現頁，驗證 light／tech、320／375／1280px 無整頁橫向溢出；light 文字最低對比 4.76:1。行情表維持區域內水平捲動。靜態重現不驗證 Chart.js 資料讀取；本機公開 API 因來源限制未載入。
- 提交證據：本紀錄與 `style: distinguish public board markets and enlarge typography` 同一提交；由 `Hardened Pages deployment` 推送自動部署。

## 2026-09-02 原始交易行情 XLS 直接匯入

- 資料來源頁新增 XLS／XLSX／XLSM 原始報表辨識、多檔選取、每檔摘要；預設選取既有正式行情來源。依報表內容讀取民國日期、市場及類別，不以檔名的 `(1)` 推斷市場。
- 使用固定版本 SheetJS 0.20.3（官方發行包及 lockfile integrity），選檔時動態載入並隨站台打包；不需要 Excel、手動轉檔或新增 CDN 執行權限。
- 彙整口徑與歷史行情一致：日期×市場×品類×品名，成交量加總、推估成交額加總、均價／中價加權，上價取最大、下價取最小，排序後品名代號集合為穩定品項鍵。保留來源檔名及品質註記。
- 空報表及完全相同內容略過；同範圍不同版本、重複但不一致明細、無效日期或數值阻止匯入。讀檔失敗先清除舊預覽，避免匯入上次檔案。
- 實檔驗證：使用者提供 5 份 XLS，9/2 二市 197 明細→88 品項（353,392 公斤）；9/1 一市 228→100（908,099 公斤）；9/1 二市 202→86（437,753 公斤）；8/31 兩份空報表略過。合計 627 明細→274 品項，1,699,244 公斤。
- 驗證通過：`test:market-file-import` 8 項、`test:page-headings`、`build:v2`（含 TypeScript）、`security:audit` 0 錯誤（4 個既有警告）。以正式 CSS chunk 及實際元件建立本機 fixture，320／375／1280px 無整頁水平溢出，11 個匯入欄位自動對應；模擬 API 收到 274 筆與正確日期、市場、總量、來源檔案。未寫入正式資料庫。
- 限制：Edge 擴充功能未允許本機檔案 URL，自動選檔測試受阻；內網 sources 頁目前需登入；IIS 設定讀取權限不足，需確認實體路徑後才能更新內網站台。
- 提交證據：與 `fix: import original market XLS reports directly` 同一提交；GitHub Pages 依既有 Hardened Pages workflow 自動部署，IIS 另行同步正式產物。

## 2026-09-02 北農每日中午行情排程

- 新增 `Market daily import`，臺灣時間每日 12:00 由 GitHub Actions 抓取官網全場交易行情，一市／二市、蔬菜／水果共四組，並回補最近三日。
- 量價別名去重、來源日期／條件／總筆數驗證；沿用正式來源與歷史自然鍵。所有資料在單一交易匯入與驗證，代碼集合衝突或來源品項不完整則回滾，不刪除歷史。
- 六項解析／加權／衝突／歷史鍵測試通過。官網試讀 9/1 共 270 筆、9/2 共 264 筆，8/31 明示尚未結帳或無資料，不寫成零成交量。
- 執行證據：`3433b4d9e`；[正式補跑 33637264188](https://github.com/jnfakimo/Inspection/actions/runs/33637264188) 成功，9/1 的 270 筆與 9/2 的 264 筆共 534 筆在正式資料庫完成寫入與驗證，並讀回完成紀錄。操作方式見 `docs/MARKET_DAILY_IMPORT.md`。

## 2026-09-02 兩站看板樣式一致與內網部署同步

- 實際瀏覽器請求確認內網 executive 頁使用雲端 app-api，與雲端公開頁都是 9/2 行情；本機 Docker 資料庫的 8/30 日期不是該頁資料來源，未覆蓋或合併本機資料庫。
- 原先市場底色與不截斷數字只套用 public variant，現改為共用看板樣式：一市藍底、二市橘底；全場均價依使用者要求採隔行淺藍、85% 透明（15% 色彩），含品項欄與兩市整列，文字保持不透明。
- `test:page-headings`、`build:v2` 通過。使用正式 CSS chunk 驗證 320／375／1280px 無整頁水平溢出、數值無截斷；淺色文字最低對比 5.92:1，科技主題亦正常。
- 新增 `sync-iis-cloud-site.ps1`：只取成功的正式部署，驗證簽章與檔案雜湊、先備份再同步，HTML 最後更新，保留舊 chunk、不修改 IIS 設定。部署與同步執行證據另追加。

## 2026-09-04 內網每日行情排程與完整交易日保護

- 排程稽核：雲端 `Market daily import` 於 9/3 成功匯入；API 比對顯示雲端最新 9/3、內網自架 API 僅到 8/30，確認 GitHub runner 無法更新內網資料庫。
- 擴充 `market_daily_import.py --sql-output` 與本機 wrapper，沿用來源日期／欄位／總筆數／別名去重、穩定鍵及單一交易驗證，不刪除資料。回補 9/1～9/3 共 785 筆後，本機最新日為 9/3。
- 9/4 中午來源分批結帳：一市已有 134 筆彙總，二市仍明示無資料。本機保留已取得資料，但 `market_source_date_ranges()` 對北農正式來源只選一市／二市 × 蔬菜／水果四組完整日期，因此看板仍顯示完整的 9/3，不會顯示半日統計。
- 建立 Windows `Inspection Daily Market Import`：每日 12:00 至 18:00 每 30 分鐘重試，共 13 個觸發時間；錯過時間會補跑、重複執行冪等，失敗留下本機紀錄。
- 驗證：7 項匯入測試通過；本機 migration 套用成功，完整日回傳 9/3／9/2；內網 API 待 Edge Function 快取更新後驗證。

### 2026-09-06｜管理 API 404 與全站回歸檢查

- 正式站帳號管理重現：`/api/local/admin/action` 回傳 404，但 users／roles／departments 三個 REST 查詢均為 200。根因是共用管理介面改用了不存在的 cookie 路由；恢復透過既有 Supabase client 呼叫 `admin-api`，沿用目前 origin 與 Bearer session，加入 15 秒逾時、繁中錯誤、成功讀取稽核，管理寫入失敗不自動重送。
- 人員清單與待審申請以 `Promise.allSettled` 分別處理，申請服務失敗不再吞掉成功的人員查詢；未取得申請數時顯示「未載入」，不再假報 0 筆。
- 舊版巡檢入口在跳轉 V2 前恢復遺留佇列隔離，僅解除巡檢自己的 Service Worker 與快取。
- CI 改為讓 npm test 失敗真正阻擋建置；更新已遷移至 V2 的登入、目前行情看板／Excel 解析器，以及 12 系統／58 路由測試盤點；新增可執行管理 API 回歸測試。
- 驗證通過：完整 npm test、V2/API typecheck、V2/API build、hardened packaging（126 頁）、scan:pages（38 舊版頁面／44 內嵌程式／18 自有 JS）、security:secrets、git diff --check。security:audit 為 0 錯誤／2 既有需人工審視警告（固定 HTML 注入與派車報表輸出）。資料契約檢查涵蓋 249 組／77 張表。
- 共享目錄更新：先備份 `InspectionMigration/backup-v2-20260906-admin-api`，再同步 hardened V2 與巡檢導向頁至使用者指定的 `InspectionMigration/site`；保留舊 chunk 供已開啟分頁使用，未寫入資料庫。
- **尚未宣告正式網址修復**：更新後瀏覽器仍讀取不同的舊版 JS，且管理 API 仍 404。已核對共享檔案雜湊與輸出一致；需要確認 HTTPS IIS 站台的實際實體路徑／代理來源後再部署並進行登入後驗收。未執行會新增正式報修、派車、人員資料的 E2E 寫入流程。提交證據可由本筆紀錄所在 commit 與 `git log -- tools/admin-api-check.mjs` 查得。

### 2026-09-06｜正式 IIS 來源定位與部署狀態

- 伺服器既有 `tools/sync-iis-cloud-site.ps1` 指定正式靜態目錄為 `C:\InspectionRuntime\site\Inspection`，同步狀態與备份在 `C:\InspectionRuntime\site-sync`。此設定解釋為何更新 `InspectionMigration/site` 不會立即反映於 HTTPS 正式站；已將既有同步腳本納入版控並通過 PowerShell 語法檢查。
- 修正提交 `a2f5efe383aec1dbb160b7fed7ad39dc10fa71e9` 的 Hardened Pages run `34027402092`、CI run `34027402116`、CodeQL run `34027402178` 均成功。Commercial readiness run `34027402225` 的既有靜態稽核失敗（原始 V1 導向頁 CSP 與 V2 路由檔案解析），不能宣稱所有 workflows 全綠。
- 正式頁面仍可重現舊 `/api/local/admin/action` 404；伺服器 C$ 拒絕存取，SSH／WinRM 關閉，遠端排程管理不可用。目前只能存取 Users 與 InspectionMigration 共享，無法從開發機寫入 InspectionRuntime。
- 待伺服器本機執行 `& 'C:\Users\jnfa\Inspection-local\tools\sync-iis-cloud-site.ps1' -Apply`。腳本會取最近成功的正式部署、驗證來源簽章與每檔雜湊、備份、先寫資產後寫 HTML，保留舊 chunks、不修改 IIS 或資料庫。完成後仍須在既有登入分頁驗證人員、設定與相關管理功能；本紀錄不代表正式站修復完成。

### 2026-09-06｜正式同步腳本 Windows PowerShell 5.1 編碼修復

- 現場執行回報 ParserError；原因是繁體中文腳本以無 BOM UTF-8 保存，在 Windows PowerShell 5.1 被系統 ANSI 編碼解讀，產生亂碼與錯誤引號。先前 PowerShell 7 的 ParseFile 檢查無法覆蓋此情況。
- `sync-iis-cloud-site.ps1` 加入 UTF-8 BOM，部署清單與狀態 JSON 讀取明確指定 UTF8；伺服器 `C:\Users\jnfa\Inspection-local\tools\sync-iis-cloud-site.ps1` 已備份原檔並直接覆寫新版，與版控來源 SHA-256 一致。
- 已用 Windows PowerShell 5.1.26100.9168 對伺服器共享路徑的實際檔案解析成功；新增 Windows CI 整合測試，以臨時 Ed25519 簽章產物驗證真實解壓、來源簽章、中文檔名、前後雜湊、備份與部署，舊 chunk／web.config 均保留。測試僅模擬 GitHub 下載，不修改正式站資料。
- 本次驗證通過 `node tools/iis-sync-compatibility-check.mjs`；正式部署仍需伺服器本機重跑同一條 `-Apply` 指令，再由登入分頁完成驗收。

### 2026-09-06｜臺灣氣象缺設定時的公開服務備援

- 正式內網氣象卡片回報未設定 CWA_API_KEY。檢查同系統既有雲端 `cwa-weather` health 為 configured=true，summary 實讀成功：22 縣市皆有溫度／預報，無來源錯誤、非過期快取。未讀取或搬移氣象金鑰。
- 新增獨立公開氣象 reader：正常維持原服務，只有 503 且 configured=false 時使用既有雲端氣象服務；不對 401／403 或一般服務異常切換來源。只傳 summary／town 與縣市，credentials=omit，使用各服務公開 anon key，無使用者 session／Cookie、无 CWA 私鑰，維持既有 CSP。
- 卡片顯示備援來源與更新時間，每 10 分鐘於可見分頁更新；失敗可重試、已有資料時標示更新失敗並保留上次內容。鄉鎮預報補上載入／錯誤提示，切換縣市清除前筆資料並阻止較慢舊請求覆蓋。
- 驗證：6 項 weather-api 回歸測試、完整 npm test、typecheck:v2、build:v2、hardened 126 頁封裝、scan:pages、security:secrets、diff check 通過；security:audit 0 錯誤／2 既有警告。瀏覽器使用實際 WeatherWidget 與建置 CSS，模擬內網缺設定並連線既有公開氣象 API，已顯示氣象地圖、溫度、特報及臺北市 12 行政區預報，並檢查縣市切換。
- 這是前端備援修正，不代表內網 CWA_API_KEY 已設定；正式 IIS 仍須以既有 sync-iis-cloud-site.ps1 -Apply 同步成功部署產物後，才能完成正式網址驗收。
- 發布證據：氣象修正 `b68300e`；Hardened Pages `34028485731` 與 CI `34028485733` 均成功，可由伺服器同步。正式帳號管理另已驗收恢復：顯示 10 筆當頁人員、待審 0 筆，未出現原本的管理服務錯誤；氣象頁仍待此次前端同步。

### 2026-09-06｜本次維修紀錄整理與目前狀態

- 已將帳號管理 `a2f5efe`、同步腳本 `8256c92`、氣象修正 `b68300e` 的原因、處理方式與驗證證據彙整至 [[06-發布前驗收表]]，並同步更新 [[05-待辦清單]]。
- **正式帳號管理已確認恢復；正式氣象仍待伺服器同步及登入後驗收。** 氣象測試頁通過與 GitHub 正式產物成功，不等同內網站台已更新。
- 所有紀錄只保存可公開的技術摘要、commit 與 workflow 連結，不保存金鑰、密碼或登入憑證。

### 2026-09-06 待辦逐項修正：發布稽核

- 修正 Commercial readiness 直接掃來源造成的錯誤網址解析；改先建置，再掃 `_site` 的 V1／V2 HTML 與 CSS。排除建置暫存及歷史來源，保留真正缺檔、CSP、SRI、noopener、脚本語法失敗門檻。
- 發現舊 V2 跳轉頁確實缺 CSP，已由打包器補上，且所有應用頁必須有 CSP 才能產出。看板版面編輯器補 GridStack 12.4.0 實際檔案 SHA-384 SRI 及外開連結 noopener。
- Commercial workflow 使用 `--unsigned-audit` 產生只供檢查、不上傳的產物；正式 Pages workflow 仍要求私鑰及簽章驗證。稽核不需要向 fork PR 提供秘密。
- 驗證：5 項正反案例通過；124 個應用 HTML 稽核 0 錯誤／0 警告；打包 126 頁與既有全站掃描通過；`npm audit --package-lock-only --omit=dev` 回報 0 漏洞。正式站登入階段待使用者完成，尚未將瀏覽器驗收標為通過。

## 2026-09-06 主機上線檢查與商用就緒修正

- 完成的修正、驗證範圍與仍未通過的上線項目，見 [[09-主機上線檢查-20260906]]。
- 本次未宣告完成正式上線驗收或 ISO/IEC 27001 認證。
### 2026-09-06 待辦逐項修正：公開看板公告與組織設定

- SYS-01 通知中心新增管理員專用公告管理：新增、編輯、啟用、停用、公開前確認，純文字標題 120 字／內文 200 字。以既有 notifications 的 board_notice／board_notice_inactive 區分啟停，保留內容與 audit_logs；無需變更資料表，也不建立正式測試公告。
- 後端必須先通過啟用帳號與系統管理員驗證；讀寫僅限無收件人的看板公告。公開版只讀啟用公告，登入看板也排除已停用公告。個人通知不會被編輯、公開或停用。寫入逾時不自動重送。
- 正式瀏覽器驗收找到系統設定仍使用已移除的 /api/local/admin/departments；改用與人員管理相同的 session Supabase 部門查詢與 RLS。新增全 web 舊 API 路由回歸檢查。
- 測試：公告真實 HTTP handler（僅替換 Auth／DB／限流）驗證未登入 401、非管理員 403、欄位限制、公開確認、個人通知隔離、建立／編輯／啟停、公開與登入看板篩選、稽核及 DB 失敗；完整 npm test、V2／API typecheck、126 頁建置、124 頁發布稽核、security／secrets 與頁首檢查通過。
- 公告實際元件配合正式建置 CSS，在隔離瀏覽器驗證 320／375／1280px 無水平溢出，一般與科技版表單文字清楚。僅使用本機 fixture，沒有發布公告或傳送通知。
- 正式站既有功能：氣象已恢復，19:05 驗證臺北市 24.4°C、特報、更新時間及備援來源，12 區預報與切換臺中 29 區通過；鄉鎮來源缺降雨機率以 — 顯示，未補假值。角色權限 6 角色、場域 71 筆、通知 29 筆唯讀載入正常。
- 公告新介面與系統設定修正仍需正式 IIS 同步後驗收；雲端發布不等於內網已同步。

### 2026-09-06 圖資實機驗收：修正整合標記切換主題後空白

- 正式 Edge 驗收平面圖、3D 模型與巡檢雲臺的一般／科技版：平面 B1 與 RF 可見，3D／巡檢均顯示七層，俯視、樓層選單、巡檢日期與 19 個巡檢點統計可操作。這是桌面操作冒煙，沒有宣稱完成慢網路或多裝置效能基準。
- 整合標記在科技版切一般版後出現空白，Console `TypeError: Cannot set properties of null (setting 'id')`。原因是 OpenSeadragon destroy 移除外部 navigator element，React 不會在後續 effect 自行補回同 ID 節點。
- 改為 React 保留外層縮圖容器，每次初始化建立可丟棄的內層元素傳給 navigatorElement；補初始化失敗的畫面訊息與 console 紀錄，避免空白但無提示。
- 使用相同元件、實際 OpenSeadragon 5.0.1 與正式 CSS 的隔離 fixture：原版切換一次可重現相同 TypeError；修正版往返／快速連續切換共六次並切至 1F，圖面及縮圖都恢復、僅一個 navigator canvas、無修正版主控台錯誤。未寫入正式圖資或標記。
- V2 typecheck、樓層一致性、頁首一致性、126 頁建置、124 頁發布稽核、全站掃描、security／secrets 通過。正式整合標記仍待 IIS 同步後重驗，原待辦保持未完成。
- 公告／部門修正 f3d9abb：CI #34029426603、Commercial #34029426735、CodeQL #34029426740、Pages #34029426620、Edge #34029426601 全部成功。
- 本機後端四個變更檔已放到主機使用者目錄 `Inspection-checklist-release-f3d9abb`，附 SHA-256 manifest；僅備妥、尚未部署。正式為 WSL `/opt/inspection/supabase`，舊 sync-local-edge-functions.ps1 的 Docker Desktop 預設不適用。遠端管理遭存取拒絕，待確認 WSL 發行版與本機部署通道，不能把雲端 Edge 成功當成本機更新。
- 其他待辦依現有證據保留：Storage 事件事後不可重現，既有逐張讀回驗證與請求識別碼仍在；件數 KPI 缺業務資料來源；WAF 缺自有網域／DNS 權限；完整復原需指定隔離專案。備份紀錄已有 08/28 的離線完整性核對，與真正應用程式復原演練不同。

### 2026-09-06 待辦逐項修正：備份排程

- 查明每日排程將不存在的手動輸入誤當 false，造成略過 Storage，已修正為預設包含 Storage，僅手動明確選 false 時排除。
- 兩次 30 分鐘取消缺乏中間進度；新增 60 秒完整請求期限、最多三次暫時錯誤重試及逐表進度。4 項 timeout／retry／403／敏感訊息測試與既有備份還原檢查通過。
- 詳見 [[07-資料庫備份與復原流程]]；完整雲端新備份待驗證，本機 WSL 備份需另行處理，兩者不能互相代替。

### 2026-09-06 教學工具包依賴修補與部署證據

- pypdf 由 6.15.0 更新至 6.16.1，處理同一 repository 的三筆 moderate 告警；獨立暫存安裝及 PDF 讀寫／書籤／合併重排／擷取測試通過，詳見 [[08-資安告警原因與修正報告]]。
- 圖臺修正 3e4d18b：CI #34029906368、Commercial #34029906338、CodeQL #34029906351、Pages #34029906336 全部成功。
- 備份修正 ce08738 已推送並從 main 手動執行完整備份 #34030072866，已確認「包含 Storage 附件」勾選；結果待工作完成後更新，不以啟動成功代替備份完成。
- 8fd94cc 的 CI、Commercial readiness、CodeQL、Hardened Pages 及 pip dependency graph 更新均成功；登入 GitHub 確認 Dependabot 0 Open／6 Closed，三筆 pypdf 告警已自動結案。

### 2026-09-06 備份查詢改善與正式主機唯讀診斷

- 新備份 #34030072866 日誌將慢點縮小到 market_data_points：其他多張表已完成，行情表從 11:24:51 UTC 至 11:29:29 匯出 20,000 列。尚無 EXPLAIN 證據，因此不把排序成本宣稱為唯一原因。
- 備份改讀取各表主鍵，利用主鍵索引順序分頁；無主鍵維持 ctid。先限制頁面資料再轉 JSON，並逐頁輸出筆數。新增單欄／複合主鍵、無主鍵與分頁不漏列不重複檢查，連同原有完整性及還原防護測試通過。
- 分頁請求仍各自使用讀取快照，並非跨表單一交易快照；離峰邏輯匯出不能取代 PostgreSQL 完整備份及隔離復原驗證。
- 新增 tools/inspect-wsl-edge-runtime.ps1：唯讀列出 WSL 發行版、Edge 容器、掛載及四個應用檔雜湊，不輸出環境變數、不重啟或部署。Windows PowerShell 5.1 parser 通過；腳本已同步至正式主機 Inspection-local/tools 並核對雜湊，尚待主機實際執行。
- 主鍵查詢首次以 information_schema 取得時，Management API 唯讀角色看不到 constraint，#34030513756 顯示所有表落回 ctid；改讀 pg_catalog 的主鍵索引中繼資料，保持 read_only，不擴權限。

### 2026-09-06 CodeQL 三筆開放告警修正

- #37／#38 指向發布稽核的 HTML regex 剝除；這段是建置稽核，並非正式端 XSS sanitizer。改用 parse5 解析 raw-text 節點並以空白遮罩內容，保留真正 script src 的資源檢查；7 項稽核測試、124 頁產物檢查 0 錯誤／0 警告。
- #34 公文逾時 API 不再將原始例外 message 或資料庫錯誤回傳；改固定繁中訊息及 request ID，服務端僅記相同追蹤碼。實際 handler 隔離測試涵蓋 Error、資料庫錯誤、回應／日誌無敏感內容及 401，沒有觸發正式公文通知。
- parse5 僅為開發稽核依賴，npm audit 0 漏洞。待 GitHub 重新掃描與 Edge 發布，正式 WSL 的相同函式仍需同步，不能把雲端部署當成本機已修復。
- 43136ca 驗證結果：CI #34030643463、Commercial #34030643505、CodeQL #34030643489、Pages #34030643472、Edge #34030643494 全部成功；CodeQL 介面確認 0 Open／38 Closed，三筆自動結案。
- 正式主機最新待部署來源改為 `Inspection-checklist-release-43136ca`，包含原公告四檔及公文逾時函式共五檔、SHA-256 manifest。唯讀診斷腳本亦更新為五檔。正式整合標記仍引用舊 JS chunk，保持待同步狀態。

### 2026-09-06 備份續查：移除主鍵分頁 OFFSET

- #34030655657 已能正確辨識主鍵，但行情表後續分頁仍慢；進一步移除 OFFSET，改以每頁最後的主鍵接續讀取，避免反覆掃過已匯出的列。無主鍵表維持既有回退方式。
- 游標由資料庫將主鍵轉成文字傳回，保留 bigint 精度；支援複合主鍵及引號／反斜線跳脫，遇到格式錯誤或未前進中止，不產生成功 artifact。每頁記錄耗時，不記錄游標或資料內容。
- 備份完整性、主鍵／複合主鍵／無主鍵分頁、不遺漏不重複、bigint／跳脫及無效游標測試通過。仍需新完整備份成功驗證。
- 另外使用暫存 PGlite（PostgreSQL 引擎）執行實際 SQL：bigint、含引號／反斜線的文字、日期＋bigint 複合主鍵、UUID、無主鍵共五組每頁一列測試全部通過；未接觸正式資料，未加入產品依賴，這不是應用程式復原演練。
- 工作期間同步腳本出現其他未提交變更，移除 JSON 的 `-Encoding UTF8`；相容性測試重現中文資源檔名亂碼，補回兩處讀取設定後 Windows PowerShell 5.1 完整簽章／備份／雜湊／中文路徑檢查恢復通過。正式主機腳本亦確認保留 UTF8 讀取。其他 BOM／註解變更及 run/ 保留未併入提交。

### 2026-09-06 雲端資料量與平台備份現況更正

- 登入 Supabase Dashboard 唯讀查核：market_data_points 估計 449,794 列、563 MB；這是平台估計值，完整匯出以新 manifest 為準。
- 雲端組織已顯示 Pro，Scheduled backups 列出 08/30～09/05 UTC 的七份每日 physical backup，最新 09/06 03:53:54 台北時間；PITR 尚未啟用。沒有升級、付費設定變更或 Restore 操作。
- 平台頁面明示不包含 Storage 物件，因此持續完成獨立 public／Storage 加密匯出；同步修正 AGENTS.md、執行手冊及 [[07-資料庫備份與復原流程]] 的舊 Free 現況。雲端平台備份與正式 WSL 備份分開記錄。
- 因行情表已約 45 萬列，後續工作總期限由 30 分鐘調為 60 分鐘，預留成長與附件下載時間；單次請求 60 秒／最多三次仍保留。正在執行的 #34031066449 沿用原版 30 分鐘上限，不為調整期限重新啟動。

### 2026-09-06 完整備份驗收成功

- 70f63fb 的 [完整備份 #34031066449](https://github.com/jnfakimo/Inspection/actions/runs/34031066449) 於台北 20:08 成功，總執行約 24 分 23 秒，沿用原 30 分鐘期限即可完成。環境日誌確認 BACKUP_INCLUDE_STORAGE=true。
- 實際匯出 76 張表、479,056 列（14 張空表），其中 market_data_points 449,928 列；71 個 Storage 物件共 32,809,742 bytes。完整性檢查、AES256 加密、密文確認、artifact 上傳全部 success。
- 加密 artifact `inspection-backup-20260906T120747Z.tar.gz.gpg`，ID 9988975646，101,853,046 bytes，保存至 2026-12-05。上傳 ZIP 的 SHA-256：`801fa3f3392aa33c977281e71386fabfbc089bfe9ddbfc067e47d90d31d0d3b9`。未將明文備份下載至本機或放入 repository。
- 70f63fb 的 CI #34031059391、Commercial #34031059437、CodeQL #34031059455、Pages #34031059438 均成功；後續 60 分鐘期限的 YAML 語法／數值檢查通過。
- 本次完成的是雲端 public／Storage 的完整加密匯出，並非正式 WSL 部署、Auth 完整復原、跨表交易快照或隔離應用程式復原；上述待辦保持未完成。

## 2026-09-06 持續驗收：備份附件與還原精度

- 實際隔離 PostgreSQL（PGlite 0.5.8）重現與驗證 bigint／numeric 精度、文字跳脫、複合主鍵、重複還原及 insert-missing 不覆蓋行為；8 項測試與備份一致性檢查通過。
- 修正原還原工具 JSON.parse 後重新序列化造成數字失真；無主鍵非空表在任何寫入前中止。目標專案必須明確指定，寫入請求限 60 秒且不盲目重試。
- 發現既有 verifyBackup 只核對資料表；補上 Storage 逐檔 SHA-256、大小、數量、缺檔與清單外檔案檢查，manifest 升 v2。舊 v1 只能比對附件數量／總大小，不宣稱有逐檔雜湊。
- 加入 AES256 封裝的解密往返比對；新版雲端完整備份結果待 workflow 實測後追加。
- 本次是工具與合成資料的隔離 PostgreSQL 驗證；不等於正式 PostgreSQL／Auth／Storage／API 完整還原演練。未寫入正式資料庫。

## 2026-09-06 Firebase 跨應用查核

- 已以雜湊在記憶體核對現行 Firebase 設定與 repository 的瀏覽器金鑰一致，未輸出或寫入金鑰值。
- 巡檢通知與教學工具共用同一 Firebase 專案及瀏覽器金鑰。App Check 尚在「開始使用」狀態；Firestore 有供教學示範的匿名讀寫規則，其餘路徑預設拒絕。
- 既有 Google Cloud API 限制已確認 25 項且不含 Generative Language API，但這不能取代資料規則與 App Check。告警維持開放；不逕改其他教學應用的資料規則或撤銷共用金鑰。
- 地端移轉範圍納入巡檢通知呼叫端與排程；Firebase 平台本身仍屬外部供應者。應用隔離與規則調整須先確認教學工具使用情況。
- 官方依據：[Firebase API keys](https://firebase.google.com/docs/projects/api-keys)、[App Check](https://firebase.google.com/docs/app-check)。

## 2026-09-06 新版完整備份與地端保存驗證

- 提交 `10b0b0c` 的完整備份 [#34034638089](https://github.com/jnfakimo/Inspection/actions/runs/34034638089) 於台北 21:18 成功：76 表／479,056 列／14 張空表，71 個附件／32,809,742 bytes。
- manifest v2 的附件逐檔 SHA-256、大小、數量、缺檔及清單外檔案檢查通過；AES256 加密後使用同一受控密語解密，與加密前壓縮檔逐位元組比對通過。日誌未輸出密語或業務資料。
- artifact 9990082913（ZIP 101,856,991 bytes）已保存於 192.168.50.192 的 `C:\Users\jnfa\Inspection-maintenance\migration-20260906\encrypted-backups`。下載 SHA-256 `68728117c6c51b3e9623f28d8fde9105b0a05c0180fc1129128eeb205dc21d3c` 與 GitHub digest 相符。
- 完整程式碼 `10b0b0c` 另保存於同一移轉目錄的 `source`，核對 Git HEAD 與乾淨工作樹。既有正式資料、舊來源及雲端服務均保留。
- `10b0b0c` 的 CI #34034633070、Pages #34034633064、Commercial #34034633050、CodeQL #34034633046 全部成功；本機完整測試、Secret 掃描及 IIS PowerShell 5.1 相容性測試通過。
- 地端尚未解密／還原這份快照；Auth、資料衝突比對、WSL／IIS 部署、排程切換及完整隔離還原仍待完成，見 [[10-地端移轉與驗收]]。備份加密驗證成功不等於應用還原成功。

### 地端前端發布包與診斷補充

- 已保存 `10b0b0c` 的正式前端 artifact 9989767944 至主機 `C:\Users\jnfa\Inspection-maintenance\migration-20260906\frontend-10b0b0c`。ZIP SHA-256 `49168dbed8a1690819ca314f88b0fa93cfadf17b0978065daee1be7f2095a0f5` 與 GitHub digest 相符。
- 安全路徑檢查、repository／commit 核對、Ed25519 發布簽章及 2,120 個檔案 SHA-256 全部通過。發布包目前為 staged-not-deployed，沒有覆蓋正式 IIS。
- 主機診斷補入資料表 RLS、列數估計、Auth／Storage 總數與設定存在狀態，使用程式實際讀取的 GOOGLE_CALENDAR_* 及 FIREBASE_SERVICE_ACCOUNT_B64 名稱。Windows PowerShell 5.1 語法、shell 語法及合成設定的 set／empty／missing 與值遮蔽驗證通過；正式主機執行結果仍待取得。

## 2026-09-06 21:29 正式 WSL 診斷與限定修復準備

- 已讀取使用者在正式主機執行的診斷報告：Edge 容器 supabase-edge-functions 正在執行，映像 supabase/edge-runtime:v1.74.0；正式掛載為 /opt/inspection/supabase/volumes/functions → /home/deno/functions。
- Auth 精確總數 126、Storage metadata 物件數 71；五個 bucket 中業務附件四個為私有，inspection-photos 為公開。報告列出的 public 資料表均啟用 RLS。附件總數一致不代表逐檔內容已比對。
- CWA、LINE webhook、Google Calendar 與 Firebase 所需設定在容器中缺漏；CRON_SECRET 存在。僅保存存在狀態，未讀取設定值。
- 三個需修復的入口仍為舊版，admin-api/board-notices.ts 與 app-api/market-board-notices.ts 不存在。多數其他入口與原始碼的 Windows 換行版本雜湊相同；不可將 LF／CRLF 差異誤認為程式改動。ipcam-proxy、cwa-weather 與主機 runtime 入口保留，不列入本次五檔更新。
- 新增 tools/repair-wsl-edge-checklist.ps1／.sh：固定已確認掛載與五個已審核來源雜湊，拒絕未審核地端變更，核對共用相依檔；先確認既有公開 API／未登入管理 API 拒絕，再備份與讀回比對、逐檔原子替換、重啟及再次檢查。失敗時回復原有檔案；新增輔助模組保留，但舊入口不引用它們。
- 五項合成檔案測試（試跑、正常套用、來源損壞、地端改動、更新後失敗回復）全部通過，Windows PowerShell 5.1／shell 語法與 Secret 掃描通過。測試的 Docker／HTTP／flock 為模擬，沒有宣稱正式主機健康檢查已完成。
- 工具已同步至主機 Inspection-local/tools 並核對 SHA-256，執行報告將存 Inspection-maintenance/edge-repair-latest.txt。成功套用後會重跑唯讀診斷，新增 public 每表精確 count，避免把 n_live_tup=0 當成空表。
- 正式套用、IIS、登入功能及資料移轉仍待執行結果，不勾選完成。

## 2026-09-06 相依檔案核對失敗與診斷補強

- 使用者執行正式 WSL 修復時，共用 security-monitor、password-policy、client-ip 三檔雜湊不同，floor 相同。已讀回 21:43:46 的主機報告；失敗發生於相依檢查，尚未到 API 探測、程式替換或容器重啟。原報告已另存維護目錄。
- 原工具只輸出 FAILED，無法分辨 Windows／Linux 換行與真正內容差異。新增原始與僅 CRLF→LF 正規化雜湊的雙重比對；只接受事先固定的已審核內容，不改寫共用檔案，也不接受任意不同版本。
- 若正規化後仍不同，繼續停止部署，列出實際雜湊，並將四個共用模組及主機 main 入口的限定原始碼快照保存至主機 Inspection-maintenance/edge-dependency-snapshots。快照只留在主機維護目錄，禁止提交公開 repository；不讀 .env、容器設定值或業務資料。
- 七項隔離回歸測試通過：原五項部署保護，加上換行差異可接受且保留原檔、真正相依內容差異拒絕並保存快照。Shell 語法通過。Docker／HTTP／flock 仍為模擬，正式差異原因及更新結果待重跑取得。
- 新工具已備份舊版後同步至原主機路徑，讀回 SHA-256 一致。未跳過安全檢查，未宣稱三個差異已證實都是換行，也未將正式部署勾選完成。

### CI 測試機短路徑修正

- 94307ed 的 Windows CI 以短路徑 TEMP 建立測試資料夾，Git Bash／Python 還原路徑名稱不同，導致隔離測試誤判；本機七項已通過不代表該 CI 亦通過。
- 測試改先解析暫存資料夾的正式路徑，再傳入 Git Bash。正式部署工具的相依內容與路徑防護保持不變。本機一般測試與 GetShortPathNameW 測試流程通過，但該 API 在本機回傳原長名稱，不能宣稱已在本機重現真正 8.3 路徑；原 Windows CI 問題由下列雲端重跑驗證。

- 修正提交 `162baf4` 的 CI [#34037340481](https://github.com/jnfakimo/Inspection/actions/runs/34037340481) 成功，包含 Windows 七項修復回歸測試與 PowerShell 5.1 IIS 測試；Commercial #34037340487、Pages #34037340484、CodeQL #34037340497 亦全部成功。正式主機報告仍停在首次相依核對失敗，未宣稱正式修復完成。

## 2026-09-06 已查明地端密碼政策差異

- 使用者重跑後保存 snapshot-56wx_w7z；已讀回並比對：security-monitor、client-ip 僅換行不同，floor 完全相同；password-policy 為現行地端固定 8 位 ASCII 數字規則，與 repository 的 8～200 字元／至少 3 類字元不同。
- 已核對該檔原始 SHA-256 109c9a971e4286c24982181d6fa952828f9a81fc5e8fd04762fdfb2d14d3a60c、LF SHA-256 03e6ded839a04f3916eb8b2811a92fdb1920103203302eba62aadb9eef0b73ce，並以原快照純函式驗證七個合成值；不修改帳號或密碼。
- 修復工具只對 _shared/password-policy.ts 接受這個已檢視的地端版本，保留檔案原狀；其他內容差異仍拒絕，同一雜湊放在其他相依路徑也拒絕。這是保留既有主機規則，不將較弱規則推送到雲端，也不改 repository 的密碼政策模組。
- 已檢視主機 main 路由：可能先要求 JWT，原無標頭的健康探測會在到達公開 API 前被拒絕。改由主機既有公開 config 取 LOCAL_SUPABASE_ANON_KEY，先確認 JWT role=anon，再帶入公開看板及未登入管理 API 探測；不讀 service-role／JWT secret、不停用 VERIFY_JWT、不停用外部 TLS。
- 十項隔離檔案回歸測試通過，含地端政策保留、例外限於正確檔案與高權限金鑰拒絕。Docker／HTTP／flock 為模擬；正式套用結果仍待使用者啟動後讀回。
- 新工具已放回主機原路徑，舊版備份保留並讀回雜湊一致。主機公開 config 的 anon 角色已唯讀確認，未輸出金鑰值。
- **IIS 更新前新增阻擋項目**：repository 的帳號表單／個人資料提示與 HTML pattern 要求 8 位數字，但 web/lib/password-policy.ts 仍要求混合字元，且與地端後端不同。需分清雲端與地端既有規則，驗證建立／匯入／重設／本人修改後再發布前端；不盲目以雲端規則覆蓋地端。

## 2026-09-06 修復啟動方式改為桌面捷徑

- 使用者將 PowerShell 提示字元、錯誤文字及省略號一起貼入終端，造成 ParserError／Get-Process 參數錯誤；主機 edge-repair 報告仍停在先前相依核對，這批貼上操作尚未啟動新版工具。
- 新增 tools/start-wsl-edge-repair.cmd，以 PowerShell -File 明確傳入 -Apply，省去互動終端的引號／& 與提示字元複製問題；保留原退出碼並停留畫面。不設定 ExecutionPolicy Bypass。
- 已在主機 OneDrive/Desktop 建立「巡檢正式站修復（192.168.50.192）」捷徑，指向主機原工具目錄；目標路徑與檔案 SHA-256 已讀回確認。
- -Apply 增加本機 IP 必須包含 192.168.50.192 的保護，避免 OneDrive 同步後在其他電腦誤執行。以目前維護工作站實測確實在呼叫 WSL 前拒絕。
- 隔離啟動測試確認含空白路徑、-File／-Apply 參數與非零退出碼正確；部署腳本使用替身，沒有在測試中操作正式服務。
- 正式執行仍須由主機使用者雙擊捷徑啟動；本次未建立新的遠端執行權限，也未宣稱已完成正式部署。

## 2026-09-06 正式 WSL 修復成功與密碼表單修正

- 22:06 正式主機修復報告確認五檔 SHA-256、備份、重啟、公開 API 與匿名管理拒絕通過；主機完整統計為 76 表／82,033 列，Auth 126／Storage 71，詳見 [[10-地端移轉與驗收]]。
- 前端新增／匯入／重設／本人修改／忘記密碼共用與 API origin 對應的既有政策；修復數字 pattern 與混合字元驗證互斥問題，雲端政策不變。臨時密碼使用安全亂數並符合各環境規則。
- 驗證：新增 5 項回歸測試、完整 npm test、typecheck:v2、security:secrets 與 build:v2（86 頁）通過。IIS 尚未同步本版，不將前端程式驗證當成正式登入流程驗收。
- 前次提交 ec8399f 四個 workflow 均成功；本版提交後追蹤 CI／Pages 結果。待辦同步至 [[05-待辦清單]]。

## 2026-09-06 IIS 指定版本更新入口

- 密碼表單修正已 PUSH：`91d74ae`。新增 IIS 桌面啟動工具，驗證主機 IP、要求完整的已審核 commit，將同步結果與備份位置保存到主機 Inspection-maintenance/iis-sync-latest.txt。
- 同步腳本新增 ExpectedCommit：最新成功 Pages 版本不同時，在下載及網站寫入前拒絕，避免確認後被其他發布取代。保留既有 Ed25519 簽章、逐檔雜湊、先備份及保留舊資產與 web.config 行為。
- Windows PowerShell 5.1 實際整合測試通過：錯誤版本拒絕且舊站未變、正確版本簽章／中文路徑／備份／檔案讀回通過。維護工作站執行新入口確認 IP 保護在呼叫同步前拒絕；沒有透過此測試寫正式站。
- 正式版本待 CI／Pages 成功後固定於主機私有 iis-release-request.json；此紀錄不表示已執行 IIS 更新。

## 2026-09-06 地端寫入移除舊 Node 優先路徑

- 查核密碼流程時發現 invokeAppApi 仍可能因 NEXT_PUBLIC_APP_API_URL，先將寫入送至舊 Node 端點。IP／localhost 地端現在直接沿用登入所用的 Supabase app-api，不再採用該舊端點；雲端部署的既有相容路徑保留。
- 實際 adapter 隔離測試通過：地端 IP、轉發埠、localhost 的密碼及業務寫入均只呼叫地端一次；失敗也不轉送遠端；雲端相容路由仍正常。HTTP／session 為測試替身，沒有更改正式帳號或資料。型別檢查通過。
- IIS 更新工具已提交（2514035，換行正規化 612047f），但正式更新必須使用包含本次地端路由修正的最新已驗證版本；尚未交付或啟動舊版 IIS 捷徑。

## 2026-09-07 重開機後正式站驗證與 3D 手機取景修正

- 正式主機 00:16 後端修復報告再次成功：五檔／公開 API／匿名管理拒絕均通過；443、445 可連線，SSH／WinRM 仍不可用。這不等於已驗證無人登入時的開機自啟動。
- 瀏覽器工具已恢復。重新載入後登入狀態與行情資料正常；帳號清單每頁 10 筆，組織設定 40 個單位正常，通知中心 29 筆可讀，新增公開公告表單可開啟後取消。沒有新增公告、改密碼或改業務資料。
- 個人資料密碼欄位 minLength／maxLength 均為 8、pattern 為 [0-9]{8}、numeric 輸入模式；提示相符。正式 floor2d HTML 的 SHA-256 為 966e21b5a449f20959b88bfabdc051fab82fda3ccc0eacb9af567ccceb014657，與 057463a 已簽署產物完全一致。因此不能僅因 iis-sync-latest.txt 不存在就判定 IIS 未更新；整站 2,120 檔主機讀回報告仍未取得。
- 正式平面圖與整合標記：實際 375px、CPU 4 倍降速、150ms 延遲／200KiB/s 驗證；一般／科技切換正常，頁寬與 scrollWidth 均為 375，B1 顯示 26 標記、整合標記切到 1F 顯示 28 標記且圖面更新。最初 viewport 工具未生效的 1463px 結果未算入手機驗收，改用 CDP 確認實際尺寸後重測。
- 正式 3D 模型手機初始視角發現樓層被裁切。共用 FloorStack3D 改依實際樓層邊界與水平／垂直視野計算相機距離，涵蓋初始、重置、俯視及未手動操作時的尺寸變更，連同巡檢雲臺共用修正。未提供 level 時不再被 NaN 誤判為有效高度，避免七層重疊在 0。
- 驗證：Three.js 真實投影核對 192 組視窗／樓層高度／方向／平移組合、每組八角點均在畫面內；另驗證窄螢幕所需距離增加。實際共用元件隔離頁在 320／375／1280px、一般／科技、重置／俯視與尺寸切換可見完整七層，無 JavaScript error。合成圖資只用於隔離驗證，不代表新版已套用正式 IIS。
- 完整 npm test、86 頁正式 build:v2、型別與秘密掃描通過。上一版 057463a 的 CI #34038733939、商用 #34038733974、CodeQL #34038733998、Pages #34038734043 全數成功。新 3D 修正版另追蹤發布與正式驗收。

## 2026-09-07 正式 3D 修正版版本確認

- 正式 `/Inspection/v2/systems/structuremap/floor3d/` 回應 HTTP 200；瀏覽器讀取原始 HTML 的 SHA-256 為 `e20cc9a763b6aa5f4dbed6d9c1f2382181f0470b19c891480203076c544f535e`，與已驗證 Ed25519 簽章的 `19af7a99b267915b42d623b921fc41cef46d95fb` 發行檔完全一致。新程式 chunk 亦已回應 200。
- 該版本已 PUSH，CI #34045523738、Commercial #34045523758、CodeQL #34045523767、Pages #34045523762 全數成功；發行 artifact 9992980997 的 2,120 檔已在主機維護暫存目錄逐檔核對。暫存核對不等於 IIS 正式根目錄全檔核對。
- iis-sync-latest.txt 仍未取得，但正式 HTML 已證明新版頁面可讀，不要求使用者為此重跑修復。WSL 修復報告 00:17:08 顯示成功。
- 本次瀏覽器尚未登入，故新版正式樓層畫面、手機取景與巡檢雲臺仍保留未完成；沒有以靜態 HTML 版本證據代替登入功能驗收。讀取 HTML 時暫停的 JavaScript 已恢復，頁面回到正常登入流程。
- 完整地端移轉仍待資料差異合併、Auth、附件內容一致性、隔離復原、排程與必要設定驗證，未宣告全部完成。
## 2026-09-07 登入後 3D／公文驗收與移轉診斷工具

- 已接上使用者完成登入的 Edge 正式站。模型圖於 375px 一般／科技顯示完整七層，俯視、重置及 320px 尺寸變更通過；巡檢雲臺於 375px 顯示七層及 0／19 當日打卡，兩主題、文字標籤、樓層 7→6→7 篩選正常。寬度與 scrollWidth 相同，未見 JavaScript error。臨時視窗覆寫已清除，主題恢復一般版。
- 公文實際建立一筆類別「其他」、主旨明確標示驗收的待送出文件，文號 `11509070001`。自動取號、建立／產生文號兩筆時間軸、查詢、文號預覽與整頁重新載入後保存均通過。保留測試草稿，未送出會辦／陳核或觸發流程通知；因此不宣稱全流程驗收完成。
- 個人資料新密碼與確認欄 minLength／maxLength 8、pattern [0-9]{8} 已核對，準備交由本人輸入及提交。密碼更新後重新登入仍待本人完成；未讀取或保存密碼。
- 新增 inspect-local-cutover.ps1／.py 與桌面啟動入口：只能在正式主機執行，以 REPEATABLE READ READ ONLY 交易核對 Auth 關聯缺口、Storage metadata、主鍵／RLS／精確筆數及 pg_cron；另讀 Windows 工作排程、WSL cron／systemd、容器重啟政策、掛載及環境設定存在狀態。LINE 推播 token／group 在 system_settings 的存在狀態另查，不以環境缺值直接斷定整個 LINE 設定缺漏。
- 報告不含帳號名單、密碼、金鑰值或排程完整命令；排程只輸出 SHA-256 與工作類別／雲地端引用摘要。蒐集失敗明確標 unavailable 並回傳非零，不將工作設定存在當成開機自啟動驗證。
- 五項隔離測試驗證秘密遮蔽、錯誤遮蔽、限定掛載及只讀交易；PGlite 真實 PostgreSQL 引擎以合成資料核對 Auth 關聯、附件大小、主鍵、精確列數、pg_cron 存在／不存在及設定遮蔽，均通過。Windows PowerShell 5.1 解析及非正式主機拒絕實測通過。這些是工具測試，不是正式資料庫復原演練。
- 工具已複製至主機獨立 cutover-check-20260907 目錄並核對雜湊，桌面「巡檢移轉驗收（只讀檢查）」已建立；local-cutover-latest.json 尚待主機執行。未新增遠端執行權限或改正式排程。
- 雲端夜間備份排程 #34056307689（台北 03:53～04:13）成功：資料與 Storage 匯出、完整性、加密、密文確認及 artifact 上傳步驟均成功。業務排程仍為 active；地端替代、去重及成功證據未齊備前不切換。
- 正式氣象仍顯示「使用備援氣象服務」。資料合併／Auth／附件實體一致性／完整隔離復原／排程切換／必要金鑰仍未全部完成。