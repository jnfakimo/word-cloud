# 巡檢系統地端移轉與驗收

返回：[[00-專案首頁]] · [[05-待辦清單]] · [[07-資料庫備份與復原流程]]

## 2026-09-06 使用者指定

正式系統的資料與執行工作統一移到 **192.168.50.192**。既有修復、驗證、PUSH 與 Obsidian 紀錄持續進行。GitHub 暫保留程式版控、建置與程式檢查；資料庫、附件、應用函式、業務排程與備份是本次地端移轉範圍。

目前階段：**盤點與移轉前快照；尚未切換完成**。

## 已確認的部署邊界

- 正式網址：`https://192.168.50.192/Inspection/v2/`。
- 正式 IIS 目錄：`C:\InspectionRuntime\site\Inspection`。
- 正式後端：主機 WSL 內的 Supabase，既有維護紀錄指向 `/opt/inspection/supabase`。須由新版主機診斷再次核對實際容器、掛載與檔案雜湊。
- `InspectionMigration/site`、舊 Docker Desktop 與 SQL Server staging 是不同環境，不能以其成功狀態代替正式站驗收。
- 目前維護端可存取伺服器 Users／InspectionMigration 共享；正式目錄不可直接寫入，SSH／WinRM 不可用。執行主機診斷與部署仍需可用的主機執行通道。

## 逐項移轉表

| 項目 | 雲端依賴／目前狀態 | 地端目標與完成條件 |
| --- | --- | --- |
| PostgreSQL 業務資料 | 雲端 public 76 表；地端也已有正式使用紀錄 | 先取得兩端精確筆數、主鍵及更新時間差異；保留地端較新紀錄，差異有衝突先列清單，不直接整庫覆蓋 |
| Auth | 雲端與地端身分資料需分別核對；邏輯 public 備份不含 auth.users | 核對帳號映射、登入、重設、角色、RLS；完整 Auth 移轉需受控備份與隔離還原，不能只還原 public.users |
| Storage | 加密快照包含附件；v2 起逐檔驗 SHA-256 | 地端逐檔大小與雜湊一致、私有 bucket／signed URL／上傳讀回通過，既有同路徑不同內容列衝突 |
| Edge Functions | GitHub push 自動部署到雲端，不會更新 WSL | 比對全部已部署入口與相依檔案、備份、更新 WSL 掛載、驗證函式與 API；保留地端 runtime 的入口與代理設定 |
| IIS 前端 | 雲端建置產物與正式站仍有版本差異 | 驗證發布簽章、先備份、資產先於 HTML 更新、逐檔雜湊讀回，登入後功能與四種圖臺驗收 |
| 氣象 | 地端缺 CWA_API_KEY，目前使用雲端公開備援 | 在地端設定受控金鑰、驗證縣市／鄉鎮／特報，再移除雲端備援依賴；中央氣象署仍是外部資料供應者 |
| 巡檢／會議室／公文／個人行事曆排程 | GitHub Actions 呼叫雲端函式 | 建立地端受控排程與鎖定／逾時／稽核；單一排程來源切換，避免兩端同時推播；驗證後停用對應雲端排程 |
| 行情匯入 | GitHub 每日 12:00；地端已有 Windows／WSL 匯入工具 | 核對地端排程、執行結果、資料日及原始列歸檔；完成後停用雲端寫入排程 |
| 錯誤監控／日誌／探針 | 雲端 Management API 日誌查詢不能套到自架服務 | 改讀地端 IIS／Supabase／容器日誌，驗證告警去重與保留；外部探針是否保留另按監控需求判斷 |
| 備份與復原 | 雲端加密快照；WSL 有同機 pg_dump 與附件備份 | 地端 PostgreSQL／Auth／Storage 一致備份、加密、保留、失敗紀錄及隔離復原。單機備份不能當成異地備份 |
| LINE／Firebase／Google | 外部通知與行事曆供應者；Firebase 與教學工具共用專案 | 將呼叫程式與排程移到地端；外部供應者本身不能安裝到 WSL。Firebase 應用隔離與規則需避免影響教學工具 |

## 主機診斷

在 192.168.50.192 的 PowerShell 執行：

```powershell
& 'C:\Users\jnfa\Inspection-local\tools\inspect-wsl-edge-runtime.ps1'
```

新版只讀取 WSL／容器 metadata、函式雜湊、設定是否存在、資料表 RLS／估計列數及 Auth／Storage 總數，不輸出設定值、不呼叫業務通知、不更新資料庫。
報告另存 `C:\Users\jnfa\Inspection-maintenance\local-runtime-latest.txt`，維護端可經既有 Users 共享讀取。
診斷只有通過語法檢查且尚無正式主機輸出時，不得宣告 WSL 部署完成。

## 2026-09-06 已完成的移轉前保存

- 完整程式碼已複製至主機 `C:\Users\jnfa\Inspection-maintenance\migration-20260906\source`，核對 HEAD 為 `10b0b0c77f97672938bf8ba1225418cff7613166`，工作樹乾淨。此為獨立移轉來源；保留原 `Inspection-local` 的較舊版本及未提交修改。
- 新版完整備份 [#34034638089](https://github.com/jnfakimo/Inspection/actions/runs/34034638089) 全部成功：76 表、479,056 列、71 附件／32,809,742 bytes。manifest v2 逐檔 SHA-256、大小、數量與加密解密往返比對均通過。
- 新快照保存至主機 `C:\Users\jnfa\Inspection-maintenance\migration-20260906\encrypted-backups\9990082913.zip`，ZIP 101,856,991 bytes，SHA-256 `68728117c6c51b3e9623f28d8fde9105b0a05c0180fc1129128eeb205dc21d3c`，與 GitHub artifact digest 一致。旁存不含秘密的驗證 JSON。
- 前一份成功備份 artifact 9988975646 也保存於同目錄，SHA-256 `801fa3f3392aa33c977281e71386fabfbc089bfe9ddbfc067e47d90d31d0d3b9`。舊 manifest v1 未提供逐檔附件雜湊。
- 未在地端解密或寫入正式資料庫。這兩份快照均不含 Auth 帳號，不是跨表一致交易快照，也不能代替完整還原演練。
- 正式公開看板完成 375px、CPU 4 倍降速、150ms 延遲及 200KB/s 下載測試；地端 API 200、143 品項、資料日 2026-09-06，沒有頁面橫向溢出、JavaScript 錯誤或內部通知內容。登入圖臺另待驗收。
- 新版診斷工具通過 Windows PowerShell 5.1 語法檢查，且已同步至主機原工具路徑並核對 SHA-256；目前尚未收到正式主機診斷報告。

## 切換驗收順序

1. 保存雲端及地端移轉前備份，確認雜湊與可解密性。
2. 在隔離環境驗證 schema、帳號映射、資料衝突處理與附件還原。
3. 更新地端後端、設定與前端，驗證帳號／權限／公告／公文／氣象／四圖臺。
4. 地端排程以受控方式驗證，核對避免重複執行的機制後，切換業務排程來源。
5. 驗證地端日誌、錯誤監控與完整備份還原，才停止雲端正式工作。
6. 留存可回復的雲端快照與前版檔案；不因切換而刪除資料或取消尚需回復的服務。

## 尚需取得的證據

- [x] 正式主機新版診斷輸出（2026-09-06 21:29）。遠端命令通道仍不可用，由使用者在主機啟動已準備的修復工具。
- [ ] 雲端／地端資料差異與隔離完整還原結果。
- [ ] 地端 CWA 與通知／OAuth 設定存在且功能可用（紀錄不得包含金鑰值）。
- [ ] 正式 IIS／WSL 版本與登入後驗收結果。
- [ ] 地端排程／監控／備份成功，以及雲端業務工作停止的證據。

### 地端前端發布包與診斷補充

- 已保存 `10b0b0c` 的正式前端 artifact 9989767944 至主機 `C:\Users\jnfa\Inspection-maintenance\migration-20260906\frontend-10b0b0c`。ZIP SHA-256 `49168dbed8a1690819ca314f88b0fa93cfadf17b0978065daee1be7f2095a0f5` 與 GitHub digest 相符。
- 安全路徑檢查、repository／commit 核對、Ed25519 發布簽章及 2,120 個檔案 SHA-256 全部通過。發布包目前為 staged-not-deployed，沒有覆蓋正式 IIS。
- 主機診斷補入資料表 RLS、列數估計、Auth／Storage 總數與設定存在狀態，使用程式實際讀取的 GOOGLE_CALENDAR_* 及 FIREBASE_SERVICE_ACCOUNT_B64 名稱。Windows PowerShell 5.1 語法、shell 語法及合成設定的 set／empty／missing 與值遮蔽驗證通過；正式主機執行結果仍待取得。

## 2026-09-06 21:29 正式 WSL 診斷與限定修復準備

- 已讀取使用者在正式主機執行的診斷報告：Edge 容器 supabase-edge-functions 正在執行，映像 supabase/edge-runtime:v1.74.0；正式掛載為 /opt/inspection/supabase/volumes/functions → /home/deno/functions。
- Auth 精確總數 126、Storage metadata 物件數 71；五個 bucket 中業務附件四個為私有，inspection-photos 為公開。報告列出的 public 資料表均啟用 RLS。附件總數一致不代表逐檔內容已比對。
- CWA、LINE webhook、Google Calendar 與 Firebase 所需設定在容器中缺漏；CRON_SECRET 存在。僅保存存在狀態，未讀取設定值。
- 三個需修復的入口仍為舊版，admin-api/board-notices.ts 與 app-api/market-board-notices.ts 不存在。多數其他入口與原始碼的 Windows 換行版本雜湊相同；不可將 LF／CRLF 差異誤認為程式改動。ipcam-proxy、cwa-weather 與主機 runtime 入口保留，不列入本次五檔更新。
- 新增 tools/repair-wsl-edge-checklist.ps1／.sh：固定已確認掛載與五個已審核來源雜湊，拒絕未審核地端變更，核對共用相依檔；先確認既有公開 API／未登入管理 API 拒絕，再備份與讀回比對、逐檔原子替換、重啟及再次檢查。失敗時回復原有檔案；新增輔助模組保留，但舊入口不引用它們。
- 五項合成檔案測試（試跑、正常套用、來源損壞、地端改動、更新後失敗回復）全部通過，Windows PowerShell 5.1／shell 語法與 Secret 掃描通過。測試的 Docker／HTTP／flock 為模擬，沒有宣稱正式主機健康檢查已完成。
- 工具已同步至主機 Inspection-local/tools 並核對 SHA-256，執行報告將存 Inspection-maintenance/edge-repair-latest.txt。成功套用後會重跑唯讀診斷，新增 public 每表精確 count，避免把 n_live_tup=0 當成空表。
- 正式套用、IIS、登入功能及資料移轉仍待執行結果，不勾選完成。
